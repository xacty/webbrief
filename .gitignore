node_modules/
.env
.env.*
!.env.example
!.env.production.example
*.db
dist/
.DS_Store
.claude/worktrees/
.claude/plans/
.claude/skills/
# Claude Agent SDK skill installer (local only — not used by VPS)
.agents/
skills-lock.json
# superpowers skill state (brainstorm/, etc. — local-only, per-session)
.superpowers/
