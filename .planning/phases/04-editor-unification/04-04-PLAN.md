---
phase: 04-editor-unification
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02]
files_modified:
  - frontend/src/components/editor/CommentMarginCards.module.css
  - frontend/src/components/editor/CommentsUI.module.css
  - frontend/src/components/editor/EditorContextMenu.module.css
autonomous: false
requirements: [UI-06]
must_haves:
  truths:
    - "CommentMarginCards.module.css, CommentsUI.module.css, EditorContextMenu.module.css contain zero hex literals from the editor parallel palette OR the Phase 1 palette"
    - "All three files consume tokens for color, spacing, radius, shadow"
    - "Comments idle / active / resolved states render identically to pre-commit"
    - "Right-click context menu (text / image / table cells) renders identically"
    - "Mentions popover and CommentComposerPopover render identically"
    - "Inline comment popover (viewport <900px) renders identically"
  artifacts:
    - path: "frontend/src/components/editor/CommentMarginCards.module.css"
      provides: "Tokenized margin comment cards"
      min_lines: 300
    - path: "frontend/src/components/editor/CommentsUI.module.css"
      provides: "Tokenized comment composer / inline / mentions popovers"
      min_lines: 350
    - path: "frontend/src/components/editor/EditorContextMenu.module.css"
      provides: "Tokenized right-click context menu"
      min_lines: 80
  key_links:
    - from: "components/editor/*.module.css"
      to: "tokens.css"
      via: "var(--wb-comment-* / --wb-color-* / --wb-shadow-* / --wb-radius-*)"
      pattern: "var\\(--wb-(comment|color|shadow|radius)-"
---

<objective>
Migrate the three CSS modules under `frontend/src/components/editor/` that carry non-trivial styling: `CommentMarginCards.module.css`, `CommentsUI.module.css` (shared composer + inline popover + mentions autocomplete), and `EditorContextMenu.module.css`. **This is Step 4 of 9** of the UI-SPEC migration sequence.

> Note: `CommentComposerPopover.jsx`, `CommentInlinePopover.jsx`, `MentionsAutocomplete.jsx` import their styles from `./CommentsUI.module.css` (verified at planning time); there is no `CommentComposerPopover.module.css` / `CommentInlinePopover.module.css` / `MentionsAutocomplete.module.css` to migrate. The UI-SPEC scope statement that lists `CommentComposerPopover.module.css` etc. is fulfilled by migrating `CommentsUI.module.css`. If during execution a per-component module file is found, migrate it the same way and add it to `files_modified` in the SUMMARY.

Purpose: Tokenize the comments and right-click context surfaces. The comments invariants (anchoring, idle/active/resolved highlight, 15-min edit window, mentions, fake selection, right-click, single-open menu) are preserved 1:1 — this is CSS only. After this plan, `<span data-comment-id>` and the right-click menu surfaces consume `--wb-comment-*` and Phase 1 tokens exclusively.

Output: Three migrated CSS files. No JSX changes. No z-index changes (plan 07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-editor-unification/04-CONTEXT.md
@.planning/phases/04-editor-unification/04-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/editor/CommentMarginCards.module.css
@frontend/src/components/editor/CommentsUI.module.css
@frontend/src/components/editor/EditorContextMenu.module.css
@frontend/src/components/editor/CommentMarginCards.jsx
@frontend/src/components/editor/CommentComposerPopover.jsx
@frontend/src/components/editor/CommentInlinePopover.jsx
@frontend/src/components/editor/MentionsAutocomplete.jsx
@frontend/src/components/editor/EditorContextMenu.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify component-to-CSS-module mapping and migrate CommentMarginCards.module.css</name>
  <files>frontend/src/components/editor/CommentMarginCards.module.css</files>
  <read_first>
    - frontend/src/components/editor/CommentMarginCards.module.css (370 lines, full read)
    - frontend/src/components/editor/CommentMarginCards.jsx (verify which classes the JSX consumes — DO NOT modify the JSX)
    - frontend/src/components/editor/CommentComposerPopover.jsx (verify it imports from `./CommentsUI.module.css`)
    - frontend/src/components/editor/CommentInlinePopover.jsx (verify it imports from `./CommentsUI.module.css`)
    - frontend/src/components/editor/MentionsAutocomplete.jsx (verify it imports from `./CommentsUI.module.css`)
    - .planning/phases/04-editor-unification/04-UI-SPEC.md (sections "Comments — Visual + Token Contract", "Color > Light-canvas palette")
    - frontend/src/styles/tokens.css
  </read_first>
  <action>
    First, verify the import map: run `grep -nE "from '\\./[^']*\\.module\\.css'" frontend/src/components/editor/Comment*.jsx frontend/src/components/editor/Mentions*.jsx`. Confirm `CommentComposerPopover.jsx`, `CommentInlinePopover.jsx`, `MentionsAutocomplete.jsx` all import from `./CommentsUI.module.css`. If any imports from a different `*.module.css` file in the same directory, add that file to this plan's scope and migrate it the same way.

    Then migrate `CommentMarginCards.module.css` per UI-SPEC §Comments — Visual + Token Contract:
    - Card bg → `var(--wb-surface)`
    - Card border idle → `var(--wb-border)`; active → `var(--wb-color-primary-300)`
    - Card shadow idle → `var(--wb-shadow-sm)`; active → `var(--wb-shadow-md)`
    - Card radius → `var(--wb-radius-3)` (12px)
    - Reply nesting indent → `var(--wb-space-4)` (16px)
    - Mention chip in card body, real profile → `color: var(--wb-color-primary-700); text-decoration: underline;`
    - Mention chip non-real (no matching profile) → `color: var(--wb-color-neutral-700); text-decoration: none;`
    - Author name → `var(--wb-color-neutral-800)` `var(--wb-weight-semibold)`
    - Body text → `var(--wb-color-neutral-700)` `var(--wb-text-sm)` `var(--wb-leading-normal)`
    - Timestamp text → `var(--wb-color-neutral-500)` `var(--wb-text-xs)`
    - Resolved badge text → `var(--wb-color-success-700)`
    - ⋮ menu icon idle → `var(--wb-color-neutral-500)`; hover → `var(--wb-color-neutral-700)`
    - ⋮ menu open popover → bg `var(--wb-surface)`, border `var(--wb-border)`, shadow `var(--wb-shadow-md)`, radius `var(--wb-radius-2)`, item hover `var(--wb-color-neutral-100)`, danger item text `var(--wb-color-danger-700)`
    - ✓ resolve icon → `var(--wb-color-neutral-500)` idle, `var(--wb-color-success-600)` on hover

    Apply replacement map from plan 02 verbatim for any literal not covered above. DO NOT change z-index. DO NOT touch the JSX.

    Layout constants in this file (margin-card geometry: card width, max-height, position offsets) — keep as raw px and document if non-obvious.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|fef08a|fef3c7|0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' frontend/src/components/editor/CommentMarginCards.module.css &amp;&amp; grep -q -- '--wb-shadow-sm' frontend/src/components/editor/CommentMarginCards.module.css &amp;&amp; grep -q -- '--wb-color-primary-300' frontend/src/components/editor/CommentMarginCards.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches of editor parallel palette + Phase 1 palette literals in `CommentMarginCards.module.css`.
    - File contains `--wb-shadow-sm` AND `--wb-shadow-md` (idle vs active card).
    - File contains `--wb-color-primary-300` (active card border).
    - File contains `--wb-color-primary-700` (mention chip real-profile color).
    - JSX file `CommentMarginCards.jsx` is byte-identical to pre-commit (`git diff frontend/src/components/editor/CommentMarginCards.jsx` empty).
    - z-index unchanged (`grep -nE '^\s*z-index:' frontend/src/components/editor/CommentMarginCards.module.css` matches the same line numbers as before this plan).
    - Build succeeds.
  </acceptance_criteria>
  <done>CommentMarginCards.module.css consumes tokens for every color/shadow/radius. JSX and z-index untouched. Card behavior identical.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CommentsUI.module.css and EditorContextMenu.module.css</name>
  <files>frontend/src/components/editor/CommentsUI.module.css, frontend/src/components/editor/EditorContextMenu.module.css</files>
  <read_first>
    - frontend/src/components/editor/CommentsUI.module.css (414 lines, full read)
    - frontend/src/components/editor/EditorContextMenu.module.css (100 lines, full read)
    - frontend/src/components/editor/CommentComposerPopover.jsx (200 lines)
    - frontend/src/components/editor/CommentInlinePopover.jsx (212 lines)
    - frontend/src/components/editor/MentionsAutocomplete.jsx (101 lines)
    - frontend/src/components/editor/EditorContextMenu.jsx (285 lines)
    - .planning/phases/04-editor-unification/04-UI-SPEC.md (sections "Comments — Visual + Token Contract" rows for `Comment composer popover`, `MentionsAutocomplete`, `EditorContextMenu`; "Floating Tooltip Contract"; "Tables — visual contract preserved")
  </read_first>
  <action>
    Migrate **`CommentsUI.module.css`** per UI-SPEC §Comments rows for composer / mentions:
    - Comment composer popover: bg `var(--wb-surface)`, border `var(--wb-border)`, shadow `var(--wb-shadow-md)`, radius `var(--wb-radius-3)`.
    - Comment composer textarea: text `var(--wb-color-neutral-800)`, placeholder `var(--wb-color-neutral-500)`, focus border `var(--wb-color-primary-300)`.
    - Comment composer mention button: ghost — `color: var(--wb-color-primary-700)` on transparent, hover `background: var(--wb-color-neutral-100)`.
    - Composer Submit CTA: delegate to shared `<Button variant="primary" size="sm">` styling — but JSX swap is OUT of scope here. In CSS, ensure the existing `submit` class consumes `var(--wb-color-primary-900)` bg, `var(--wb-editor-text-on-dark)` text, `var(--wb-radius-2)` radius. (If the JSX already uses `<Button>` shared, this rule is dead and can be deleted.)
    - MentionsAutocomplete popover: bg `var(--wb-surface)`, border `var(--wb-border)`, shadow `var(--wb-shadow-md)`, radius `var(--wb-radius-2)`.
    - Mentions selected/highlighted item: bg `var(--wb-color-primary-100)`, text `var(--wb-color-primary-900)`.
    - Mentions empty state: text `var(--wb-color-neutral-500)`.
    - CommentInlinePopover (viewport &lt;900px): same look as the margin card body — bg `var(--wb-surface)`, border `var(--wb-border)`, shadow `var(--wb-shadow-md)`, radius `var(--wb-radius-3)`. Backdrop overlay (semi-transparent veil) — `rgb(15 23 42 / 24%)` LITERAL with comment `/* alpha-on-light, kept literal per UI-SPEC §Color */`.

    Migrate **`EditorContextMenu.module.css`** per UI-SPEC §Comments row for `EditorContextMenu`:
    - Menu bg → `var(--wb-surface)`
    - Border → `var(--wb-border)`
    - Shadow → `var(--wb-shadow-md)`
    - Radius → `var(--wb-radius-2)`
    - Item text default → `var(--wb-color-neutral-800)` `var(--wb-text-sm)`
    - Item hover bg → `var(--wb-color-neutral-100)`
    - Danger item text → `var(--wb-color-danger-700)` (per UI-SPEC §Comments table)
    - Divider → `var(--wb-border)`
    - Item with shortcut: shortcut chip text → `var(--wb-color-neutral-500)`, monospace font kept as `font-family: ui-monospace, SFMono-Regular, monospace` (kept literal per UI-SPEC §Floating Tooltip Contract).

    Apply replacement map from plan 02 verbatim for any literal not covered. DO NOT change z-index in either file. DO NOT touch JSX.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|fef08a|fef3c7|0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' frontend/src/components/editor/CommentsUI.module.css frontend/src/components/editor/EditorContextMenu.module.css &amp;&amp; grep -q -- '--wb-color-primary-100' frontend/src/components/editor/CommentsUI.module.css &amp;&amp; grep -q -- '--wb-color-danger-700' frontend/src/components/editor/EditorContextMenu.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches of editor parallel palette + Phase 1 palette literals in both files.
    - `CommentsUI.module.css` references `--wb-color-primary-100` (mentions selected item) and `--wb-color-primary-700` (mention chip).
    - `EditorContextMenu.module.css` references `--wb-color-danger-700` (danger item) and `--wb-color-neutral-100` (item hover).
    - JSX files (`CommentComposerPopover.jsx`, `CommentInlinePopover.jsx`, `MentionsAutocomplete.jsx`, `EditorContextMenu.jsx`) byte-identical to pre-commit.
    - z-index unchanged in both files (`grep -nE '^\s*z-index:'` line numbers stable).
    - Build succeeds.
  </acceptance_criteria>
  <done>CommentsUI.module.css and EditorContextMenu.module.css migrated; comments composer / inline popover / mentions autocomplete / right-click menu all consume tokens. JSX and z-index untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual QA of comments, mentions, and right-click menu</name>
  <what-built>
    Three CSS modules migrated: `CommentMarginCards.module.css`, `CommentsUI.module.css`, `EditorContextMenu.module.css`. Comments idle/active/resolved states, mentions popover, comment composer popover, inline popover (viewport &lt;900px), and right-click context menu (text / image / table) consume tokens.
  </what-built>
  <how-to-verify>
    Open a project with at least 3 sections, 2 comments (one resolved, one active), 1 reply, 1 @mention to a real profile, 1 image, 1 table.

    1. Margin cards on the right — idle card shows light shadow + light border, active card shows stronger shadow + primary-300 border. Click an active card → ⋮ menu opens with item hover styles correct, danger item ("Eliminar") in red.
    2. Click ✓ on a comment → resolved badge turns green; the highlight on the span turns transparent (background gone, mark stays in DOM per invariant).
    3. Hover an unresolved `<span data-comment-id>` in the editor — highlight intensifies to active.
    4. Click into the editor at a position; type `@` — MentionsAutocomplete popover opens. Hover or arrow-key through items — selected item gets `primary-100` bg, `primary-900` text. Empty-state text appears in neutral-500.
    5. Highlight some text and click "Comment" in the right-click menu OR open the composer otherwise — composer popover shows light surface, light border, medium shadow, comment-radius. Type a message; mention button is ghost primary; submit button uses primary.
    6. Resize viewport to &lt; 900px — margin cards hide; click an existing comment span; CommentInlinePopover opens with backdrop overlay (alpha-on-light literal).
    7. Right-click in text → EditorContextMenu opens — surface, hover, danger item all token-driven.
    8. Right-click on the inserted image → image context menu (handled by ProjectEditor.module.css per plan 02) — confirm still renders correctly.
    9. Right-click in a table cell → table context menu (handled by ProjectEditor.module.css) — same.

    No visual regression vs. pre-commit.
  </how-to-verify>
  <resume-signal>Type "approved" when all 9 steps render identically; otherwise paste the failing step number and a description.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CSS bundle | Token replacement; bundle reshipped |
| editor user → comment system | No JS/JSX touched; all comment invariants preserved by virtue of CSS-only edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | components/editor/*.module.css | mitigate | CSS-only changes; explicit `git diff` check on JSX files asserts they are untouched. |
| T-04-09 | Repudiation | comment-highlight token mapping | mitigate | UI-SPEC §Comments table embedded in plan; reviewers can audit any token swap against the table. |
</threat_model>

<verification>
- See acceptance criteria of Tasks 1 and 2.
- Visual checkpoint of Task 3 must pass before proceeding.
- Comment invariants from CONTEXT.md `target=editor.comments` are NOT touched at the JSX layer; CSS preserves visual idle/active/resolved/orphan-resolved states.
</verification>

<success_criteria>
- Three CSS modules in `components/editor/` consume tokens for color/shadow/radius.
- JSX in the same directory is untouched.
- z-index in those CSS modules unchanged (deferred to plan 07).
- Comments and right-click context menu render identically.
</success_criteria>

<output>
Create `.planning/phases/04-editor-unification/04-04-SUMMARY.md` with: import-map verification result (which JSX consumes which CSS module), per-file count of literals replaced, list of `rgb(... / NN%)` literals kept (with their CSS comments), checkpoint result.
</output>
