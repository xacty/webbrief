---
phase: 04-editor-unification
plan: 09
type: execute
wave: 8
depends_on: [04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08]
files_modified: []
autonomous: false
requirements: [UI-06, UI-07]
must_haves:
  truths:
    - "Editor renders identically pre- vs post-Phase-4: every Phase-4 success criterion in ROADMAP §Phase 4 is verified as TRUE"
    - "All 16 QA scenarios from UI-SPEC §Migration Strategy → QA scenario set pass with zero visual and zero functional regression"
    - "All editor invariants from CONTEXT.md and CONTEXT.min.md (sections, comments, mentions, history, autosave, drag-drop, page pills, tables, handoff copy-safe, mode switching, SEO metadata, page-switch race, version-conflict block, orphan auto-resolve display) verified intact"
    - "Final grep gates pass: zero parallel-palette literals, zero numeric z-index, zero local shareLinkModal/exportModal chrome rules"
  artifacts: []
  key_links: []
---

<objective>
Run the full QA matrix for Phase 4. **This is Step 9 of 9** — the final acceptance gate before Phase 4 is marked Complete and Phase 5 begins. **No code is modified in this plan**; it executes verification commands and a 16-scenario manual QA pass against the editor.

Purpose: Prove that Phase 4 satisfies its 6 ROADMAP success criteria, all 7 invariant categories from CONTEXT.md, and all 16 scenarios from UI-SPEC §"QA scenario set". If any check fails, route to gap-closure (`/gsd-plan-phase 4 --gaps`).

Output: A `04-09-SUMMARY.md` containing the verification log (each gate's status + observed output) and the QA scenario log (each of 16 scenarios + result).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-editor-unification/04-CONTEXT.md
@.planning/phases/04-editor-unification/04-UI-SPEC.md
@frontend/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all final automated gates from UI-SPEC and ROADMAP</name>
  <files>(read-only — verifications only)</files>
  <read_first>
    - .planning/phases/04-editor-unification/04-UI-SPEC.md (sections "Editor Sub-Tokens", "Z-index", "Color > Removed hex literals", "QA scenario set")
    - .planning/ROADMAP.md (Phase 4 §Success Criteria — 6 items)
    - All 8 editor CSS modules
  </read_first>
  <action>
    Run the following commands from the worktree root and record exit codes + key outputs into the SUMMARY:

    1. **No parallel-palette literals in editor CSS:**
       ```
       ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|0070d6|0088ff)\b' \
         frontend/src/pages/ProjectEditor.module.css \
         frontend/src/pages/ProjectEditorNav.module.css \
         frontend/src/pages/ProjectEditorToolbar.module.css \
         frontend/src/pages/ProjectEditorPanels.module.css \
         frontend/src/pages/ProjectEditorSeoRules.module.css \
         frontend/src/pages/BriefProjectEditor.module.css \
         frontend/src/components/editor/CommentMarginCards.module.css \
         frontend/src/components/editor/CommentsUI.module.css \
         frontend/src/components/editor/EditorContextMenu.module.css
       ```
       Expected: zero matches.

    2. **No Phase 1 hex shades in editor CSS** (collapsed to tokens):
       ```
       ! grep -nE '#(0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' \
         [same 9 files]
       ```
       Expected: zero matches.

    3. **No numeric z-index in editor CSS:**
       ```
       ! grep -nE '^\s*z-index:\s*[0-9]+(\s*;|\s+!important)' \
         [same 9 files]
       ```
       Expected: zero matches.

    4. **Editor sub-tokens defined in tokens.css:**
       ```
       grep -c '^\s*--wb-editor-' frontend/src/styles/tokens.css   # expect 8
       grep -c '^\s*--wb-tooltip-' frontend/src/styles/tokens.css  # expect 2
       grep -c '^\s*--wb-comment-' frontend/src/styles/tokens.css  # expect 3
       grep -c '^\s*--wb-section-' frontend/src/styles/tokens.css  # expect 1
       ```

    5. **shareLinkModal CSS rules dead-code-removed:**
       ```
       ! grep -nE '^\s*\.shareLinkModal[A-Z]?\s*\{' frontend/src/pages/ProjectEditor.module.css
       ! grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ --include='*.jsx' --include='*.js' --include='*.ts' --include='*.tsx'
       ```
       Expected: zero matches.

    6. **exportModal local chrome CSS removed (form internals retained):**
       ```
       ! grep -nE '^\s*\.exportModal(Overlay|Header|Eyebrow|Title|Close)\s*\{' frontend/src/pages/ProjectEditor.module.css
       grep -nE '^\s*\.exportModalForm\s*\{' frontend/src/pages/ProjectEditor.module.css   # expect 1 match — form layout retained
       ```

    7. **Modal shared imported in ProjectEditor.jsx:**
       ```
       grep -nE "import.*Modal.*from.*['\"]\\.\\.\\/components\\/ui" frontend/src/pages/ProjectEditor.jsx
       grep -c "<Modal" frontend/src/pages/ProjectEditor.jsx   # expect ≥ 1
       ```

    8. **EditorContextMenu uses calc(var(--wb-z-popover) + 1):**
       ```
       grep -q -- 'calc(var(--wb-z-popover) + 1)' frontend/src/components/editor/EditorContextMenu.module.css
       ```

    9. **Build succeeds:**
       ```
       npm run --prefix frontend build
       ```
       Expected: exit 0, no warnings about unresolved CSS variables.

    10. **No new npm dependencies:**
        ```
        cd frontend && git diff -- package.json package-lock.json
        ```
        Expected: empty (Phase 4 introduces zero new dependencies per UI-SPEC §Registry Safety).

    Each gate that fails routes to `/gsd-plan-phase 4 --gaps` with the failure reason. The SUMMARY records each gate's pass/fail.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -rnE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|0070d6|0088ff)\b' frontend/src/pages/ProjectEditor*.module.css frontend/src/pages/BriefProjectEditor.module.css frontend/src/components/editor/*.module.css &amp;&amp; ! grep -rnE '^\s*z-index:\s*[0-9]+' frontend/src/pages/ProjectEditor*.module.css frontend/src/pages/BriefProjectEditor.module.css frontend/src/components/editor/*.module.css &amp;&amp; ! grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ --include='*.jsx' --include='*.js' &amp;&amp; grep -q -- 'calc(var(--wb-z-popover) + 1)' frontend/src/components/editor/EditorContextMenu.module.css &amp;&amp; npm run --prefix frontend build &amp;&amp; cd frontend &amp;&amp; test -z "$(git diff --name-only -- package.json package-lock.json)"</automated>
  </verify>
  <acceptance_criteria>
    - Gates 1, 2, 3, 5, 6 (negative grep) all return zero matches.
    - Gates 4, 7, 8 (positive grep) all return the expected counts.
    - Gate 9 (build) exits 0.
    - Gate 10 (no new deps) — `git diff -- package.json package-lock.json` is empty.
    - Each gate's result recorded in `04-09-SUMMARY.md`.
  </acceptance_criteria>
  <done>All automated gates pass; result captured in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the 16 QA scenarios from UI-SPEC §"QA scenario set"</name>
  <what-built>Phase 4 complete: editor sub-tokens added, all editor CSS modules tokenized, z-index unified, modals migrated, BriefProjectEditor migrated.</what-built>
  <how-to-verify>
    Open the dev server. Use a brand-new project AND an existing project with rich content.

    Run **all 16 scenarios from UI-SPEC §"QA scenario set"** (verbatim):

    1. Create a new project of type **Página Web**. Add 3 sections, mix headings H1–H6, paragraphs, 1 bullet list, 1 ordered list, 1 blockquote, 1 table 3×3, 1 image, 1 CTA.
    2. Switch to **Handoff designer** → verify gutters render with type labels (`H1`/`H2`/`P`/`CTA`/`Link metadata`), copy-safe (labels are NOT selectable / do not appear when copying body text).
    3. Switch to **Handoff dev** → verify SEO tray + JSON metadata appear; click sections-panel "SEO metadata" → verify scroll lands on tray.
    4. Switch to **Preview** → verify max-width 800, no toolbar, no panels; background flips to `--wb-color-neutral-50`.
    5. Back to **Brief** → add 2 anchored comments + 1 reply + 1 @mention to a real profile.
    6. Click into highlighted comment → margin card expands. Active highlight is the stronger amber `rgba(254, 240, 138, 0.9)`. Outline of active span is `--wb-color-warning-500`.
    7. Resize viewport &lt; 900px → margin cards hide, inline popover opens at click of a comment span.
    8. Right-click in text → menu at cursor with cut/copy/paste/comment/link/format. Right-click on image → image context menu (`--wb-editor-surface-elevated` bg). Right-click in table → table context menu (`--wb-editor-surface-elevated` bg).
    9. **Share Link panel** (the live UI; not a modal) → generate, copy, revoke. Confirm `ShareLinkPanel` renders identically (the dead `shareLinkModal` CSS rules were removed in plan 05).
    10. Open **Export modal** (right-click on an image → "Configurar export") → toggle format/quality/dimensions, submit; in **bulk mode** select multiple images and trigger bulk export. Both flows complete and download files.
    11. Switch pages, rename a page (existing inline rename), delete a page (confirm modal — verify it still works; if it was migrated to `<Modal>`, confirm chrome).
    12. Drag a section to reorder; verify drag ghost styling (light bg, light border, medium shadow, primary-500 drag-over indicator).
    13. Verify floating tooltip on every toolbar button — 300 ms delay, dark `#3c4043` bg via `--wb-tooltip-bg`, white text, monospace shortcut chip with the `Mac ⌘ ⇧ ⌥` formatting.
    14. **Save** (manual + autosave 8s). Trigger version conflict (open the same page in a second tab, save in tab 1, then attempt to save in tab 2) — verify autosave blocks and a conflict notice appears (existing behavior — UI is not changed by Phase 4).
    15. Open **HistoryTabPanel** for a page; verify orphan comments display with snippet.
    16. Switch project type to **Artículo** → verify linear layout still works. To **FAQ** → verify question/answer flow.

    For each scenario, record: **PASS** (no visual diff, no functional diff) or **FAIL** with description.
    
    **Editor invariants final check** (cross-reference CONTEXT.md `## Editor Invariants` and CONTEXT.min.md `## Keep Stable target=editor.*`):
    - sectionDivider markup intact ✅ (CSS-only changes)
    - Sidebar derived from document, no flicker ✅
    - First-section logic, auto-naming, protected empty sections ✅
    - Active section/heading sync ✅
    - HTML hydration of divider attrs ✅
    - Drag &amp; drop of sections ✅ (visual-only check)
    - Page pills + MoreVertical menu (rename/delete) ✅
    - Toolbar context-sensitive (lists vs text) ✅
    - Tables: contextual toolbar, right-click menu, inline + buttons ✅
    - Type labels: `t` for tables, `img` for images, no-interactive ✅
    - Handoff copy-safe (labels/actions outside selectable text) ✅
    - Comments anchoring, 15-min edit window, mentions, right-click custom menu, fake selection overlay ✅
    - HistoryTabPanel works in page/document/faq ✅
    - 480ms delay on page-switch race condition ✅
    - autosave 8s delay, blocked on version-conflict, runner in ref ✅
    - SEO metadata extraction in handoff dev ✅

    **Score the editor against the 6 Phase 4 ROADMAP success criteria:**
    1. ProjectEditor.module.css free of `#212222 / #2a2a2a / #d9d9d9` — verified by grep gate above. PASS.
    2. ProjectEditorNav/Toolbar/Panels/SeoRules also clean — verified by grep gate. PASS.
    3. Z-index of toolbar/dropdowns/modals/comment cards/tooltips consume tokens — verified by grep gate. PASS.
    4. shareLinkModal/exportModal use shared `<Modal>` — partially deviated: shareLinkModal is dead code (removed); exportModal migrated to `<Modal>`. Decision documented in plan 05 SUMMARY. PASS.
    5. Editor invariants preserved (sections, document-structure, handoff copy-safe, comments anchoring) — visual + functional QA confirms. PASS.
    6. QA visual passes Brief → Handoff designer/dev → Preview, comments crud, mode switching, page switching — covered by scenarios 1–11 above. PASS.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 4 complete" if all 16 scenarios pass, all invariants intact, and all 6 ROADMAP success criteria met. Otherwise paste the failing scenario number(s) and a description; we will route to `/gsd-plan-phase 4 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA → editor surfaces | Verification only; no privileged action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Repudiation | QA result claim | mitigate | Scenario log with PASS/FAIL written to SUMMARY; reviewers can audit. |
| T-04-20 | Tampering | regression detection | mitigate | Automated gates run as part of Task 1 (grep-verifiable); manual QA covers UX-level invariants. |
</threat_model>

<verification>
- All 10 automated gates from Task 1 pass.
- All 16 manual QA scenarios from Task 2 pass.
- All 16 editor invariants from CONTEXT.md / CONTEXT.min.md verified intact.
- All 6 ROADMAP §Phase 4 success criteria marked PASS.
</verification>

<success_criteria>
- Phase 4 acceptance gate cleared.
- ROADMAP.md Phase 4 status flips from "In Progress" to "Complete".
- Ready to start Phase 5 (Public Pages &amp; Verification).
</success_criteria>

<output>
Create `.planning/phases/04-editor-unification/04-09-SUMMARY.md` with: full automated gate log (each command + result), QA scenario log (16 scenarios + result), invariant cross-reference (16 invariants + verified status), 6 ROADMAP success criteria final marks, and any deviations encountered. Then update `.planning/ROADMAP.md` to mark Phase 4 complete (`Complete` + completion date).
</output>
