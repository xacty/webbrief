---
phase: 04-editor-unification
plan: 05
type: execute
wave: 5
depends_on: [04-02]
files_modified:
  - frontend/src/pages/ProjectEditor.module.css
autonomous: true
requirements: [UI-06, UI-07]
must_haves:
  truths:
    - "shareLinkModal CSS rules (.shareLinkModal, .shareLinkModalHeader, .shareLinkModalTitle, .shareLinkModalNote) are deleted from ProjectEditor.module.css because they are dead code"
    - "ShareLinkPanel JSX is unchanged (it does not use these classes)"
    - "Editor render is byte-identical to pre-commit (no consumer = no visual diff)"
  artifacts:
    - path: "frontend/src/pages/ProjectEditor.module.css"
      provides: "ProjectEditor.module.css minus dead shareLinkModal CSS rules"
  key_links: []
---

<objective>
Delete the dead `shareLinkModal*` CSS rules from `ProjectEditor.module.css`. **This is Step 5 of 9** of the UI-SPEC migration sequence.

**Deviation from UI-SPEC:** The UI-SPEC §"Modal Migration Contract" describes a JSX swap from local `shareLinkModal` rules to `<Modal>` shared. **At planning time it has been verified that no JSX consumer references `styles.shareLinkModal` / `.shareLinkModalHeader` / `.shareLinkModalTitle` / `.shareLinkModalNote` in `frontend/src/pages/ProjectEditor.jsx` or anywhere else in `frontend/src/`** (`grep -rn "shareLinkModal" frontend/src/` returns matches only inside `ProjectEditor.module.css` itself — the 4 rule definitions, lines ~404-435). The actual share UI uses `ShareLinkPanel` (`ProjectEditor.jsx` line ~8877), which renders inline via `panelStyles.shareBox` and consumes `.shareLinkRow`, `.shareLinkUrl`, `.shareLinkCopyBtn`, `.shareLinkOpenBtn`, `.shareLinkRevokeBtn`, `.shareLinkCopiedBadge`, `.shareCardHeader` (different classes — these are NOT dead).

Therefore Step 5 collapses to **dead-code deletion**, not a JSX migration. No `<Modal>` swap. No JSX touched.

If during execution a hidden consumer is discovered (e.g., a dynamic class reference like `styles[modalClassName]`), STOP and escalate — the dead-code assumption is wrong and a true JSX migration is needed instead.

Output: `ProjectEditor.module.css` shrinks by ~32 lines (the 4 rule blocks). Editor renders identically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-editor-unification/04-CONTEXT.md
@.planning/phases/04-editor-unification/04-UI-SPEC.md
@frontend/src/pages/ProjectEditor.module.css
@frontend/src/pages/ProjectEditor.jsx
@frontend/src/components/ui/Modal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify shareLinkModal is dead code, then delete the four CSS rule blocks</name>
  <files>frontend/src/pages/ProjectEditor.module.css</files>
  <read_first>
    - frontend/src/pages/ProjectEditor.module.css (lines 400-440 — the four `shareLinkModal*` rules; full file already loaded after plan 02)
    - frontend/src/pages/ProjectEditor.jsx (full search for `shareLinkModal`)
    - All other JSX in `frontend/src/` for `shareLinkModal` references
  </read_first>
  <action>
    1. **Verify dead-code assumption** — run:
       ```
       grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ \
         --include='*.jsx' --include='*.js' --include='*.ts' --include='*.tsx'
       ```
       Expected output: zero matches. If any match appears in a JSX/JS/TS file, STOP. Do NOT delete the CSS rules. Mark this task as failed and escalate to the planner — a true JSX-to-`<Modal>` migration is required instead.

    2. If verification passes, delete the four rule blocks from `ProjectEditor.module.css`:
       - `.shareLinkModal { ... }` (12 lines)
       - `.shareLinkModalHeader { ... }` (6 lines)
       - `.shareLinkModalTitle { ... }` (5 lines)
       - `.shareLinkModalNote { ... }` (6 lines)

       Total ~32 lines removed (including blank lines between rule blocks). Use the Edit tool to delete each rule block. Preserve the surrounding rule (`.shareLinkRow`, `.shareLinkUrl`, `.shareLinkCopyBtn`, etc.) — those are LIVE and consumed by `ShareLinkPanel`.

    3. After deletion, confirm `grep -nE '^\s*\.shareLinkModal[A-Z]?' frontend/src/pages/ProjectEditor.module.css` returns no matches.

    4. DO NOT touch `ProjectEditor.jsx`. DO NOT touch any other CSS class. DO NOT introduce a `<Modal>` import.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ --include='*.jsx' --include='*.js' --include='*.ts' --include='*.tsx' &amp;&amp; ! grep -nE '^\s*\.shareLinkModal[A-Z]?' frontend/src/pages/ProjectEditor.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ --include='*.jsx' --include='*.js' --include='*.ts' --include='*.tsx'` returns NO matches.
    - `grep -nE '^\s*\.shareLinkModal[A-Z]?' frontend/src/pages/ProjectEditor.module.css` returns NO matches (4 rule blocks deleted).
    - `git diff frontend/src/pages/ProjectEditor.jsx` is empty (JSX untouched).
    - Build succeeds.
    - Visual smoke: open the editor, open the share-link panel via the existing UI flow — `ShareLinkPanel` (the actual share UI) renders identically (it never used the deleted rules).
  </acceptance_criteria>
  <done>Dead `shareLinkModal*` CSS rules removed. Build clean. ShareLinkPanel functionality untouched. UI-SPEC §Modal Migration Contract for shareLinkModal satisfied via dead-code deletion (deviation documented in objective).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CSS bundle | CSS rule deletion |
| editor user → share-link UI | ShareLinkPanel (live) is untouched; deleted rules were dead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | ProjectEditor.module.css | mitigate | Dead-code grep gate runs BEFORE deletion; if any consumer exists the task aborts. |
| T-04-11 | Repudiation | dead-code claim | mitigate | The grep command and its expected zero-output is recorded in `<verify>` and rerun in CI. |
</threat_model>

<verification>
- `grep -rnE 'shareLinkModal(Header|Title|Note)?' frontend/src/ --include='*.jsx' --include='*.js' --include='*.ts' --include='*.tsx'` returns 0 matches.
- `grep -nE '^\s*\.shareLinkModal[A-Z]?' frontend/src/pages/ProjectEditor.module.css` returns 0 matches.
- Build succeeds.
- `git log -p -1 frontend/src/pages/ProjectEditor.jsx` does NOT include this commit.
</verification>

<success_criteria>
- 4 dead CSS rule blocks removed from `ProjectEditor.module.css`.
- ShareLinkPanel renders identically; share-link create/copy/revoke flows untouched.
- No `<Modal>` import added in this plan (the live share UI is a panel, not a dialog — Modal swap is not applicable).
</success_criteria>

<output>
Create `.planning/phases/04-editor-unification/04-05-SUMMARY.md` with: dead-code verification grep output (must be empty), the 4 rule blocks deleted (line counts), confirmation that `ProjectEditor.jsx` byte-identical, build status, and the **explicit deviation note** from UI-SPEC §Modal Migration Contract for shareLinkModal — the live share UI is `ShareLinkPanel` (a panel, not a dialog). If during execution a hidden consumer was discovered, document it and the new plan that was created in its place.
</output>
