---
phase: 04-editor-unification
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - frontend/src/pages/ProjectEditorNav.module.css
  - frontend/src/pages/ProjectEditorToolbar.module.css
  - frontend/src/pages/ProjectEditorPanels.module.css
  - frontend/src/pages/ProjectEditorSeoRules.module.css
autonomous: false
requirements: [UI-06]
must_haves:
  truths:
    - "ProjectEditorNav.module.css, ProjectEditorToolbar.module.css, ProjectEditorPanels.module.css, ProjectEditorSeoRules.module.css contain zero hex literals from the editor parallel palette (#212222, #2a2a2a, #d9d9d9, #1a1a1a, #1d4ed8, #2563eb)"
    - "All four files consume tokens (--wb-editor-* / --wb-color-* / --wb-space-* / --wb-radius-* / --wb-shadow-*)"
    - "Visual diff in editor (navbar, toolbar, sections panel, updates panel, SEO rules tray) shows zero regression"
  artifacts:
    - path: "frontend/src/pages/ProjectEditorNav.module.css"
      provides: "Tokenized navbar styles"
      min_lines: 500
    - path: "frontend/src/pages/ProjectEditorToolbar.module.css"
      provides: "Tokenized toolbar styles"
      min_lines: 200
    - path: "frontend/src/pages/ProjectEditorPanels.module.css"
      provides: "Tokenized panels styles"
      min_lines: 1000
    - path: "frontend/src/pages/ProjectEditorSeoRules.module.css"
      provides: "Tokenized SEO rules tray styles"
      min_lines: 300
  key_links:
    - from: "ProjectEditorNav.module.css"
      to: "tokens.css"
      via: "var(--wb-editor-bg / --wb-editor-text-on-dark / --wb-editor-text-on-dark-muted)"
      pattern: "var\\(--wb-editor-"
    - from: "ProjectEditorPanels.module.css"
      to: "tokens.css"
      via: "var(--wb-color-neutral-* / --wb-color-primary-* / --wb-shadow-* / --wb-radius-*)"
      pattern: "var\\(--wb-(color|shadow|radius)"
---

<objective>
Migrate the four ancillary editor CSS modules — `ProjectEditorNav.module.css`, `ProjectEditorToolbar.module.css`, `ProjectEditorPanels.module.css`, `ProjectEditorSeoRules.module.css` — to consume Phase 4 sub-tokens and Phase 1 global tokens. **This is Step 3 of 9** of the UI-SPEC migration sequence.

Purpose: Continue the parallel-palette elimination begun in plan `04-02` for `ProjectEditor.module.css`, applying the same replacement map to the four supporting CSS modules. After this plan, all five non-component editor CSS modules consume tokens exclusively for their colors. Spacing, radius, and shadow scales also tokenized where they match the global scale. Layout constants (sections-panel widths, navbar height, floating-bar height, SEO tray collapsed/expanded heights) are documented as exceptions and stay as raw px.

Output: Four migrated CSS files. JSX is **untouched** in this plan. z-index is **untouched** (plan 07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editor-unification/04-CONTEXT.md
@.planning/phases/04-editor-unification/04-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/pages/ProjectEditor.module.css
@frontend/src/pages/ProjectEditorNav.module.css
@frontend/src/pages/ProjectEditorToolbar.module.css
@frontend/src/pages/ProjectEditorPanels.module.css
@frontend/src/pages/ProjectEditorSeoRules.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProjectEditorNav.module.css and ProjectEditorToolbar.module.css to tokens</name>
  <files>frontend/src/pages/ProjectEditorNav.module.css, frontend/src/pages/ProjectEditorToolbar.module.css</files>
  <read_first>
    - frontend/src/pages/ProjectEditorNav.module.css (560 lines — full read)
    - frontend/src/pages/ProjectEditorToolbar.module.css (240 lines — full read)
    - frontend/src/styles/tokens.css (canonical token list)
    - .planning/phases/04-editor-unification/04-UI-SPEC.md (sections "Page Pills (navbar)", "Color > Dark-chrome palette (token map)", "Spacing Scale", "Floating Tooltip Contract")
    - frontend/src/pages/ProjectEditor.module.css (already migrated — reference for token-mapping decisions established in plan 02)
  </read_first>
  <action>
    Apply the same replacement map from plan `04-02` Task 1 (verbatim) to **both** files. The map covers `#212222`, `#2a2a2a`, `#d9d9d9`, `#1a1a1a`, `#3c4043`, `#1d4ed8`, `#2563eb`, `#0070d6`, `#0088ff`, `#fff`, `#fff` (light), Phase 1 neutrals, danger, success, primary; `rgb(... / NN%)` and `rgba(0/255/255/255, X)` stay as literals with comments.

    **Specific concerns for the navbar file** (`ProjectEditorNav.module.css`):
    - Page pill bg active uses `rgb(255 255 255 / 12%)` — keep literal, comment.
    - MoreVertical icon idle → `var(--wb-editor-text-on-dark-muted)`; hover → `var(--wb-editor-text-on-dark)`.
    - Page-pill rename input bg → `var(--wb-surface)`; text → `var(--wb-color-neutral-800)`.
    - Save status pill colors: idle → `var(--wb-editor-text-on-dark-muted)`, active → `var(--wb-editor-text-on-dark)`, success state → `var(--wb-color-success-700)` if it appears on light surface OR keep `--wb-editor-text-on-dark` if on dark chrome.
    - The `:global` rules for `lucide-react` icon sizing/coloring stay as-is unless they have hex literals.
    - Spacing migration: replace `padding: 4px`, `8px`, `12px`, `16px`, `20px`, `24px`, `32px`, `48px` with `var(--wb-space-*)` where these are pure rhythm. Keep raw px for layout constants: `height: 56px` (navbar height — documented).

    **Specific concerns for the toolbar file** (`ProjectEditorToolbar.module.css`):
    - Toolbar bg → `var(--wb-editor-bg)`.
    - Toolbar button hover bg → `rgb(255 255 255 / 12%)` LITERAL with comment `/* alpha-on-dark, kept literal per UI-SPEC §Color */`.
    - Toolbar button active bg → `var(--wb-editor-surface)` (NOT a hardcoded `#2a2a2a`).
    - Toolbar separator → `rgb(255 255 255 / 16%)` LITERAL with comment.
    - Dropdown panel bg (rendered on dark chrome but its own surface is light) → `var(--wb-surface)`; border → `var(--wb-border)`; shadow → `var(--wb-shadow-md)`; radius → `var(--wb-radius-2)`.
    - Dropdown item hover → `var(--wb-color-neutral-100)`.
    - Toolbar height → keep `48px` raw (layout constant).
    - Heading dropdown active item → `var(--wb-color-primary-100)` bg + `var(--wb-color-primary-900)` text.

    DO NOT touch any `z-index: NNN` (plan 07).
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|0070d6|0088ff|fef08a)\b' frontend/src/pages/ProjectEditorNav.module.css frontend/src/pages/ProjectEditorToolbar.module.css &amp;&amp; ! grep -nE '#(0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171)\b' frontend/src/pages/ProjectEditorNav.module.css frontend/src/pages/ProjectEditorToolbar.module.css &amp;&amp; grep -q -- '--wb-editor-' frontend/src/pages/ProjectEditorNav.module.css &amp;&amp; grep -q -- '--wb-editor-' frontend/src/pages/ProjectEditorToolbar.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for the editor parallel palette literals in both files.
    - Zero matches for the Phase 1 hex shades (`#0f172a` … `#f8fafc`, `#dc2626`, `#f87171`) in both files.
    - Both files reference at least one `--wb-editor-*` token.
    - Build succeeds (`npm run --prefix frontend build` exits 0).
    - Layout constant `56px` (navbar height) preserved as raw px in `ProjectEditorNav.module.css`.
    - Layout constant `48px` (toolbar height) preserved as raw px in `ProjectEditorToolbar.module.css`.
    - At least one CSS comment containing `kept literal` exists in each file justifying any remaining `rgb(... / NN%)`.
  </acceptance_criteria>
  <done>Navbar and toolbar CSS modules migrated to tokens; layout constants and z-index preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ProjectEditorPanels.module.css and ProjectEditorSeoRules.module.css to tokens</name>
  <files>frontend/src/pages/ProjectEditorPanels.module.css, frontend/src/pages/ProjectEditorSeoRules.module.css</files>
  <read_first>
    - frontend/src/pages/ProjectEditorPanels.module.css (1167 lines — full read)
    - frontend/src/pages/ProjectEditorSeoRules.module.css (347 lines — full read)
    - frontend/src/styles/tokens.css
    - .planning/phases/04-editor-unification/04-UI-SPEC.md (sections "Drag &amp; Drop, Sections Panel", "SEO Rules tray", "Spacing Scale", "Color > Light-canvas palette")
  </read_first>
  <action>
    Apply the replacement map from plan `04-02` Task 1 (verbatim) to both files. Both panels live on the **light** side of the editor — sections panel left, updates panel right, SEO tray top — so the dominant token family is `--wb-color-neutral-*` and `--wb-color-primary-*`, NOT the editor sub-tokens.

    **Specific for `ProjectEditorPanels.module.css`** (per UI-SPEC §Drag &amp; Drop, Sections Panel):
    - Sections panel item bg → `var(--wb-color-neutral-50)`
    - Active section item left-border (3px) → `var(--wb-color-primary-300)`
    - Active section item text → `var(--wb-color-primary-900)` weight `var(--wb-weight-semibold)`
    - Drag-ghost bg → `var(--wb-surface)`; border → `var(--wb-border)`; shadow → `var(--wb-shadow-md)`; grip icon → `var(--wb-color-neutral-500)`.
    - Drag-over indicator (2px line) → `var(--wb-color-primary-500)`.
    - Updates panel row hover → `var(--wb-color-neutral-100)`.
    - Sections-panel width: `min-width: 220px` and `max-width: 320px` stay as raw px (layout constants).
    - Updates-panel width: `width: 360px` stays raw px (layout constant).
    - Comments-visible left-shift `transform: translateX(-300px)` stays raw px (layout constant; documented in CONTEXT.md as "Canvas se shifta 300px a la izquierda cuando hay comments visibles").
    - Type-label colors: dimmed → `var(--wb-color-neutral-400)`; default text → `var(--wb-editor-text)` (per UI-SPEC §Light-canvas palette).
    - Section pill rounded corners → `var(--wb-radius-2)` if it currently uses 8px; otherwise leave at the existing value.

    **Specific for `ProjectEditorSeoRules.module.css`** (per UI-SPEC §SEO Rules tray):
    - Tray collapsed bg → `var(--wb-color-neutral-50)`; expanded bg → `var(--wb-surface)`.
    - Top border → `var(--wb-border)`.
    - Title → `var(--wb-text-sm)` `var(--wb-weight-semibold)` `var(--wb-color-neutral-800)`.
    - Field label → `var(--wb-text-xs)` `var(--wb-weight-medium)` `var(--wb-color-neutral-700)`.
    - Word/char count text → `var(--wb-text-xs)` `var(--wb-color-neutral-600)`.
    - Reading-time pill → `var(--wb-color-neutral-100)` bg, `var(--wb-color-neutral-700)` text, `var(--wb-radius-full)`.
    - Tray heights: `40px` (collapsed) / `240px` (expanded) stay raw px (layout constants documented).
    - DO NOT replace SEO field markup with `<Input>` shared component — that JSX swap is OUT of scope per UI-SPEC §SEO Rules tray "Optional consideration (deferred unless trivial during execution)". This plan is CSS-only.

    DO NOT touch any `z-index: NNN` (plan 07).
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|0070d6|0088ff|fef08a)\b' frontend/src/pages/ProjectEditorPanels.module.css frontend/src/pages/ProjectEditorSeoRules.module.css &amp;&amp; ! grep -nE '#(0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' frontend/src/pages/ProjectEditorPanels.module.css frontend/src/pages/ProjectEditorSeoRules.module.css &amp;&amp; grep -q -- '--wb-color-' frontend/src/pages/ProjectEditorPanels.module.css &amp;&amp; grep -q -- '--wb-color-' frontend/src/pages/ProjectEditorSeoRules.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for the editor parallel palette literals in both files.
    - Zero matches for the Phase 1 hex shades in both files.
    - Both files reference `--wb-color-*` tokens.
    - Layout constants preserved: `220px`, `320px`, `360px`, `transform: translateX(-300px)` in panels file; `40px` (collapsed tray) and `240px` (expanded tray) in SEO file.
    - SEO field markup is unchanged in JSX — this task is CSS-only.
    - Build succeeds (`npm run --prefix frontend build` exits 0).
  </acceptance_criteria>
  <done>Panels and SEO Rules CSS modules migrated to tokens; layout constants and z-index preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual QA of nav, toolbar, panels, SEO tray after migration</name>
  <what-built>All four ancillary editor CSS modules migrated to tokens. JSX untouched. z-index untouched.</what-built>
  <how-to-verify>
    Open a project in the editor (any existing project with a few sections, comments, and an image):

    1. Navbar — back button hover, project name editable, page pills (active vs idle vs hover), save indicator state cycle, profile / bell icons, undo/redo. Confirm muted-text uses the light gray (`--wb-editor-text-on-dark-muted`).
    2. Toolbar — every button group, separators, dropdowns (heading, color, highlight, align, indent, spacing, table picker). Active state on dark chrome uses `--wb-editor-surface`.
    3. Sections panel — idle, hover, active, drag &amp; drop a section, drag-over indicator visible.
    4. Updates panel — list rows, hover, badges if any.
    5. SEO rules tray — collapsed look, click to expand, title + field labels, word/char count, reading-time pill, all using neutral / primary tokens.
    6. Switch to Handoff mode (designer audience) — gutters with type labels render correctly. Switch to Handoff (dev) — SEO tray + JSON metadata appear.
    7. Switch to Preview — toolbar and panels hidden.

    No visual diff vs. pre-commit state.
  </how-to-verify>
  <resume-signal>Type "approved" if every panel/toolbar element renders identically; otherwise paste step number + description.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CSS bundle | Token replacement; bundle reshipped to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | 4 editor CSS modules | mitigate | Same block-by-block strategy as plan 02; visual checkpoint at the end. |
| T-04-07 | Repudiation | Layout constant preservation | mitigate | Acceptance criteria assert preservation of specific px values (`220`, `320`, `360`, `300`, `56`, `48`, `40`, `240`); reviewers can audit. |
</threat_model>

<verification>
- See acceptance criteria of Tasks 1 and 2.
- Visual checkpoint of Task 3 must pass.
</verification>

<success_criteria>
- Four CSS modules tokenized; literals from the parallel editor palette and the Phase 1 palette eliminated.
- Layout constants preserved.
- z-index preserved (plan 07).
- JSX untouched.
- Editor renders identically to pre-commit state.
</success_criteria>

<output>
Create `.planning/phases/04-editor-unification/04-03-SUMMARY.md` with: count of literals replaced (per file, per category), list of `rgb(... / NN%)` literals kept (with their CSS comments), checkpoint result, and any deviations encountered.
</output>
