---
phase: 04-editor-unification
plan: 08
type: execute
wave: 7
depends_on: [04-01]
# Wave 7 with plan 04-07: no file overlap — 04-07 touches 8 main editor CSS modules; 04-08 touches BriefProjectEditor.{jsx,module.css}. Parallel-safe.
files_modified:
  - frontend/src/pages/BriefProjectEditor.jsx
  - frontend/src/pages/BriefProjectEditor.module.css
autonomous: false
requirements: [UI-06, UI-07]
must_haves:
  truths:
    - "BriefProjectEditor.module.css contains zero hex literals from the editor parallel palette OR the Phase 1 palette"
    - "BriefProjectEditor.module.css consumes Phase 1 tokens (--wb-color-* / --wb-space-* / --wb-radius-* / --wb-shadow-*)"
    - "BriefProjectEditor.module.css contains no numeric z-index — it consumes var(--wb-z-*)"
    - "If BriefProjectEditor.jsx renders any local modal, it migrates to <Modal> shared (or, if no modals exist, JSX is byte-identical)"
    - "Brief view loads in dashboard/preview contexts and renders identically"
  artifacts:
    - path: "frontend/src/pages/BriefProjectEditor.module.css"
      provides: "Migrated compact-editor CSS"
      min_lines: 600
    - path: "frontend/src/pages/BriefProjectEditor.jsx"
      provides: "Compact-editor JSX with Modal migration if any local modal exists"
  key_links:
    - from: "frontend/src/pages/BriefProjectEditor.module.css"
      to: "tokens.css"
      via: "var(--wb-color-* / --wb-space-* / --wb-radius-* / --wb-shadow-* / --wb-z-*)"
      pattern: "var\\(--wb-(color|space|radius|shadow|z)-"
---

<objective>
Migrate the **compact Brief variant** of the editor: `BriefProjectEditor.jsx` (707 lines) and `BriefProjectEditor.module.css` (755 lines, ~115 hex literals, 1 numeric z-index). **This is Step 8 of 9** of the UI-SPEC migration sequence.

Per UI-SPEC §"Brief variant": this variant follows the **light shell** palette, NOT the dark editor chrome. It is closer to a Phase 3 admin page than a Phase 4 dark editor. Migration rule: every hex literal → Phase 1 token; modals (if any) → `<Modal>` shared. **No editor sub-tokens (`--wb-editor-*`) needed unless the executor finds a divergence (e.g., a panel reusing `#212222`)** — at that point the divergence is flagged in the SUMMARY.

Output: BriefProjectEditor consumes Phase 1 tokens for all colors / spacing / shadows / radius / z-index. JSX is unchanged unless local modals exist and need swapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-editor-unification/04-CONTEXT.md
@.planning/phases/04-editor-unification/04-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/Modal.jsx
@frontend/src/pages/BriefProjectEditor.jsx
@frontend/src/pages/BriefProjectEditor.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detect any local modal in BriefProjectEditor.jsx and decide migration path</name>
  <files>(read-only — no file changes in this task)</files>
  <read_first>
    - frontend/src/pages/BriefProjectEditor.jsx (707 lines — full read)
    - frontend/src/pages/BriefProjectEditor.module.css (search for `Modal` / `Overlay` class definitions)
  </read_first>
  <action>
    1. Run:
       ```
       grep -nE "(className|class).*[Mm]odal" frontend/src/pages/BriefProjectEditor.jsx
       grep -nE "^\s*\.[a-zA-Z]*[Mm]odal" frontend/src/pages/BriefProjectEditor.module.css
       grep -nE "^\s*\.[a-zA-Z]*[Oo]verlay" frontend/src/pages/BriefProjectEditor.module.css
       ```
    2. Three possible outcomes:
       - **No local modal classes found** → Task 2 is CSS-only.
       - **Local modal classes found AND consumed by JSX** → Add a sub-task to Task 2 swapping the JSX to `<Modal>` shared (same pattern as plan 06's exportModal migration), and delete the chrome CSS rules.
       - **Local modal classes found but no JSX consumer (dead code)** → Add a sub-task to Task 2 deleting the CSS rules (same pattern as plan 05's shareLinkModal deletion).
    3. Document the outcome in the SUMMARY. The decision affects how Task 2 proceeds.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; grep -cE "(className|class).*[Mm]odal" frontend/src/pages/BriefProjectEditor.jsx; grep -cE "^\s*\.[a-zA-Z]*[Mm]odal" frontend/src/pages/BriefProjectEditor.module.css; echo "(non-blocking — both grep counts are recorded)"</automated>
  </verify>
  <acceptance_criteria>
    - Inventory recorded: number of local modal classes in CSS, number of JSX consumers, decision (CSS-only / JSX migration / dead-code deletion).
  </acceptance_criteria>
  <done>Decision recorded; Task 2 proceeds based on the outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BriefProjectEditor.module.css colors + z-index, and swap any local modal to <Modal>—if applicable</name>
  <files>frontend/src/pages/BriefProjectEditor.module.css, frontend/src/pages/BriefProjectEditor.jsx</files>
  <read_first>
    - frontend/src/pages/BriefProjectEditor.module.css (full read — 755 lines, ~115 hex literals, 1 z-index)
    - frontend/src/pages/BriefProjectEditor.jsx (any block touching modal classes from Task 1)
    - frontend/src/styles/tokens.css
    - .planning/phases/04-editor-unification/04-UI-SPEC.md §"Brief variant"
  </read_first>
  <action>
    **Color migration (CSS):**

    Apply the **same replacement map from plan 02 Task 1** verbatim. The Brief variant is in the light-shell family, so the dominant token families are `--wb-color-neutral-*`, `--wb-color-primary-*`, `--wb-color-success-*`, `--wb-color-danger-*`, `--wb-surface`, `--wb-border`. Editor sub-tokens (`--wb-editor-*`) should NOT appear unless the Brief variant has a dark surface (verify by reading the file — most likely it does not).

    - `#fff` / `#ffffff` → `var(--wb-surface)`
    - Phase 1 neutrals (`#0f172a` … `#f8fafc`) → corresponding `--wb-color-neutral-*`
    - Phase 1 primary (`#091223`, `#0b1220`, `#1d4ed8`, `#2563eb`) → `--wb-color-primary-900` / `-700`
    - Phase 1 danger / success → `--wb-color-danger-*` / `--wb-color-success-*`
    - Borders matching `#dbe3f0` → `var(--wb-border)`
    - Borders matching `#c7d2e5` → `var(--wb-border-strong)`
    - Spacing on the 4-pt grid (`4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 / 64 / 96 px`) → `var(--wb-space-*)` where applicable
    - Radius (`8 / 10 / 12 / 14 / 16 / 18 / 24 px`) → `var(--wb-radius-*)` where applicable; preserve legacy `--wb-radius-sm`/`--wb-radius-md`/`--wb-radius-lg` aliases per Phase 1 (10/14/18 px)
    - Shadows where they match → `var(--wb-shadow-*)`
    - Alpha overlays `rgb(... / NN%)` and `rgba(...)` stay as literals with comments

    **z-index migration (CSS):**

    The single `z-index: 100` (line 24) maps to `var(--wb-z-sticky)` (since it's a sticky chrome layer per UI-SPEC table; confirm the surrounding selector before mapping — if the selector is for a dropdown, use `var(--wb-z-dropdown)`).

    **Modal migration (JSX + CSS) — only if Task 1 detected local modals:**

    - If **JSX consumers found**: Swap to `<Modal>` shared using the exact pattern from plan 06 Task 1 (import, replace overlay+card+header+close JSX, delete chrome CSS rules, keep form internals). Preserve modal-specific behavior (Escape close, overlay close, body-scroll lock — all delegated to `<Modal>`).
    - If **dead-code modal CSS only**: Delete the dead rules (same pattern as plan 05).
    - If **no local modals**: skip the JSX edit; this task is CSS-only.

    **DO NOT** change the Brief variant's layout geometry (panel widths, canvas widths, sticky positioning) — those are the variant's identity.
  </action>
  <verify>
    <automated>cd /Users/adrian/GitHub/webbrief/.claude/worktrees/refactor-ui-system &amp;&amp; ! grep -nE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|fef08a|fef3c7|0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' frontend/src/pages/BriefProjectEditor.module.css &amp;&amp; ! grep -nE '^\s*z-index:\s*[0-9]+' frontend/src/pages/BriefProjectEditor.module.css &amp;&amp; grep -q -- '--wb-color-' frontend/src/pages/BriefProjectEditor.module.css &amp;&amp; grep -q -- '--wb-z-' frontend/src/pages/BriefProjectEditor.module.css &amp;&amp; npm run --prefix frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '#(212222|2a2a2a|d9d9d9|1a1a1a|3c4043|1d4ed8|2563eb|0f172a|1e293b|334155|475569|64748b|94a3b8|cbd5e1|e2e8f0|f1f5f9|f8fafc|dc2626|f87171|fef2f2|fee2e2|15803d|16a34a|dcfce7|091223|0b1220)\b' frontend/src/pages/BriefProjectEditor.module.css` returns 0.
    - `grep -nE '^\s*z-index:\s*[0-9]+' frontend/src/pages/BriefProjectEditor.module.css` returns 0.
    - File contains `--wb-color-*` and `--wb-z-*` token references.
    - If a modal swap was performed: `frontend/src/pages/BriefProjectEditor.jsx` imports `Modal` from `../components/ui` AND `<Modal` JSX exists.
    - If no modal swap was needed: `git diff frontend/src/pages/BriefProjectEditor.jsx` is empty.
    - Build succeeds.
  </acceptance_criteria>
  <done>BriefProjectEditor CSS tokenized; z-index tokenized; modals migrated or confirmed absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual QA of the Brief compact view</name>
  <what-built>BriefProjectEditor.module.css colors + z-index migrated. Modals (if present) swapped to shared `<Modal>` or dead rules removed.</what-built>
  <how-to-verify>
    Render the Brief view in every context the app supports it:

    1. **Dashboard preview** (if BriefProjectEditor is rendered as a project preview card / list item) — verify it appears with the same compact look.
    2. **Public share / brief mode** (if BriefProjectEditor is reused in the public share flow before Phase 5 fully migrates SharePage) — verify nothing broke pre-Phase 5.
    3. **In-app preview pane** wherever Brief variant is invoked — check headings, body text, lists, tables, image rendering. Layout geometry (column widths) unchanged.
    4. If a modal was migrated: open it (e.g., via the action that triggers it) — confirm the shared `<Modal>` chrome renders, Escape close, overlay close, and submit work.

    No visual regression vs. pre-commit.
  </how-to-verify>
  <resume-signal>Type "approved" if the Brief variant renders identically in every context; otherwise paste step number and description.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CSS bundle | Token replacement |
| user → Brief view consumers | Variant invariants preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | BriefProjectEditor.module.css | mitigate | Replacement map identical to plan 02; Task 1 verifies modal scope before changing JSX. |
| T-04-18 | Repudiation | modal-detection decision | mitigate | Inventory recorded in SUMMARY; reviewers can audit. |
</threat_model>

<verification>
- See acceptance criteria of Tasks 1 and 2.
- Visual checkpoint of Task 3 must pass.
</verification>

<success_criteria>
- BriefProjectEditor.module.css consumes tokens for all colors and z-index.
- Local modals (if any) migrated; dead-code modals (if any) removed.
- Variant layout untouched.
- All consumers render identically.
</success_criteria>

<output>
Create `.planning/phases/04-editor-unification/04-08-SUMMARY.md` with: outcome of Task 1 (number of modals + decision path), count of literals replaced, modal-migration result if applicable, checkpoint result.
</output>
