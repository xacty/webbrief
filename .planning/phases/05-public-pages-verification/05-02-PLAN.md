---
phase: 05-public-pages-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/BriefPage.jsx
  - frontend/src/pages/BriefPage.module.css
autonomous: true
requirements:
  - UI-08

must_haves:
  truths:
    - "BriefPage.jsx renders the brief form (identity card, fieldGroups, choice/file fields, submit) using shared <Card>, <Input>, <Button> primitives; <textarea>, <input type='file'>, and radio/checkbox controls remain native and tokenized in-place"
    - "BriefPage.module.css contains zero hardcoded #hex literals (target: 38 → 0)"
    - "BriefPage.jsx contains zero inline `style={{...}}` blocks (FileUploadField inline styles moved to module.css)"
    - "Form submission, validation per question, file upload (Subiendo… → list with Quitar), success state ('¡Gracias por completar el brief!'), and error state ('Brief no disponible' or server message) preserve current behavior byte-for-byte"
    - "Brief checkmark color upgraded from #16a34a (4.0:1) to var(--wb-color-success-700) (#15803d, 4.5:1) — documented a11y win in SUMMARY"
    - "BriefPage smoke flow console clean (no React warnings, no missing-key/propType warnings) under preview tools or `npm run build`"
  artifacts:
    - path: "frontend/src/pages/BriefPage.jsx"
      provides: "Public brief form view consuming shared Button/Input/Card; FileUploadField with no inline styles"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/BriefPage.module.css"
      provides: "Layout + tokenized typography/color/spacing; native textarea/file/radio/checkbox CSS in-place; success/error/loading state styles tokenized"
      min_lines: 80
  key_links:
    - from: "BriefPage.jsx"
      to: "/api/public/brief/:token (GET)"
      via: "publicFetch in useEffect — preserved from current implementation"
      pattern: "publicFetch.*api/public/brief"
    - from: "BriefPage.jsx"
      to: "/api/public/brief/:token/responses (POST)"
      via: "submitBrief handler — preserved verbatim"
      pattern: "/api/public/brief/.*/responses"
    - from: "BriefPage.jsx"
      to: "/api/public/brief/:token/uploads (POST)"
      via: "FileUploadField handleUpload — preserved verbatim"
      pattern: "/api/public/brief/.*/uploads"
---

<objective>
Migrate `BriefPage.jsx` (incl. `FileUploadField`) and `BriefPage.module.css` to consume Phase 1 tokens and Phase 2 shared primitives (`Button`, `Input`, `Card`). Per `05-UI-SPEC.md` §"Migration Strategy" Step 2, this is the larger public-page migration (38 hardcoded hex literals, 386 jsx lines incl. inline styles in `FileUploadField`, 307 css lines).

Purpose: Address requirement UI-08 for the brief-form surface. Public respondent sees zero functional change; the visual delta is limited to the locked snaps in `05-UI-SPEC.md` (formTitle 28→30, sectionHeaderTitle 17→18, identityTitle 15→14, page bg `#f4f7fb`→`#f8fafc`, focus border `#212222`→`#091223`, submit hover `#000`→`#0b1220`, page padding 40/16/80 → 48/16/64, success checkmark color upgrade to AA). Form submit, validation, file upload, success/error states preserved.
Output: BriefPage migrated; success criteria 1–5 of `05-UI-SPEC.md` §"Migration Strategy" pass for BriefPage's two files; FileUploadField inline styles eliminated; total Phase 5 hex count drops from 43 → 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/05-public-pages-verification/05-CONTEXT.md
@.planning/phases/05-public-pages-verification/05-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/index.js
@frontend/src/components/ui/Button.jsx
@frontend/src/components/ui/Input.jsx
@frontend/src/components/ui/Card.jsx
@frontend/src/pages/BriefPage.jsx
@frontend/src/pages/BriefPage.module.css

<interfaces>
<!-- Shared UI public surface (from frontend/src/components/ui/index.js): -->
import { Button, Input, Card } from '../components/ui';

<!-- Button props: variant ('primary' | 'secondary' | 'ghost' | 'danger'), size ('sm' | 'md' | 'lg'), type, disabled, loading, fullWidth, onClick, aria-label, children -->
<!-- Input props: type ('text' | 'email' | ...), label, helperText, error, required, disabled, id, ...native input props. NOTE: NO textarea support and NO file support in Phase 2 — keep <textarea> and <input type='file'> native; tokenize their CSS in-place. -->
<!-- Card props: padding, shadow, radius, className, children -->

<!-- Tokens used in this plan (from frontend/src/styles/tokens.css §Phase 1): -->
- Layout/background: var(--wb-bg) (#f8fafc), var(--wb-surface) (#ffffff)
- Spacing: var(--wb-space-1..16)
- Typography: --wb-text-{xs,sm,base,lg,xl,2xl,3xl}; --wb-leading-{normal,xl,2xl,3xl,relaxed}; --wb-weight-{regular,medium,semibold,bold}
- Color: var(--wb-color-neutral-{200,400,500,600,700,800,900}), var(--wb-color-primary-{200,700,900}), var(--wb-color-danger-{50,200,500,600}), var(--wb-color-success-{100,700})
- Radius: var(--wb-radius-2) for inputs, var(--wb-radius-3) for cards (12px on-scale)
- Shadow: var(--wb-shadow-sm)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate BriefPage.jsx imports, JSX, and FileUploadField inline styles</name>
  <files>frontend/src/pages/BriefPage.jsx</files>
  <read_first>
    - frontend/src/pages/BriefPage.jsx (current full file — preserve every fetch, every state setter, every useEffect, every Spanish string verbatim; FileUploadField inline styles must move to CSS)
    - frontend/src/components/ui/Button.jsx (variants/sizes/loading API; size='lg' for submit per UI-SPEC §"Typography" Exception 6)
    - frontend/src/components/ui/Input.jsx (label prop, type='text'/'email', required prop, helperText/error API; NO textarea, NO file types — keep those native)
    - frontend/src/components/ui/Card.jsx (padding/shadow/radius/className API)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Pages × Components Coverage Matrix" row for BriefPage
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Copywriting Contract" BriefPage rows (server-provided strings pass through; static strings preserved verbatim)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Color" Per-element token map (Brief rows; checkmark upgrade locked)
  </read_first>
  <action>
    Refactor `frontend/src/pages/BriefPage.jsx` to consume shared primitives. CONCRETE replacements (preserve every handler, every fetch URL, every Spanish string verbatim):

    1. **Imports.** At the top, add: `import { Button, Input, Card } from '../components/ui'`.

    2. **Identity card (`.identityCard`).** Wrap the identity area in `<Card padding="md" shadow="sm" radius="md" className={styles.identityCard}>`. Inside:
       - Keep `<h2 className={styles.identityTitle}>Tus datos</h2>` (CSS tokenized in Task 2: 14px / weight 700 per locked snap from 15px/700).
       - Replace the two raw inputs for `respondentName` and `respondentEmail` with `<Input>` from Phase 2:
         ```jsx
         <Input
           label="Nombre completo"
           required
           placeholder="Tu nombre"
           value={respondentName}
           onChange={(event) => setRespondentName(event.target.value)}
         />
         <Input
           type="email"
           label="Correo electrónico"
           required
           placeholder="tu@email.com"
           value={respondentEmail}
           onChange={(event) => setRespondentEmail(event.target.value)}
         />
         ```
         If the current JSX has its own labels above the inputs, remove them — `<Input>` renders the label internally.

    3. **Question fieldGroups.** For each question rendered, wrap each fieldGroup in `<Card padding="md" shadow="sm" radius="md" className={styles.fieldGroup}>`. Inside:
       - Keep the `<label className={styles.questionLabel}>{label}{required && <span className={styles.required} aria-hidden="true">*</span>}</label>` pattern. Add `aria-hidden="true"` to the `*` span (new a11y addition per UI-SPEC).
       - Optional `<p className={styles.hint}>` stays AS-IS.
       - Per question type:
         - `short_text` → `<Input type="text" required={question.required} value={...} onChange={...} aria-required={question.required} />` — no label prop here (the question label is rendered ABOVE the input via `.questionLabel`); pass `aria-label` from the question label OR use the question id pattern (`<label htmlFor={...}>` + `<Input id={...}>`). **Lock:** keep `<label className={styles.questionLabel}>` rendering the title above and pass `<Input>` without its own `label` prop — set `aria-label={question.label}` for SR.
         - `long_text` → keep native `<textarea className={styles.textarea} required={question.required} value={...} onChange={...} aria-required={question.required} aria-label={question.label} rows={4} />`. Phase 2 has no textarea primitive; keep native and tokenize CSS in Task 2.
         - `single_choice` → keep native `<input type="radio" className={styles.optionInput} ...>` with surrounding `<label className={styles.optionLabel}>`. Tokenize in Task 2 — accent-color: `var(--wb-color-primary-900)`.
         - `multiple_choice` → same pattern with `<input type="checkbox">`.
         - `file_upload` → keep `<FileUploadField>` component; it gets refactored in step 5 below.

    4. **Section headers.** `<h3 className={styles.sectionHeaderTitle}>` and `<div className={styles.sectionHeader}>` stay AS-IS structurally; CSS tokenized in Task 2 (sectionHeaderTitle 17→18 snap, border-bottom 2px var(--wb-color-neutral-200)).

    5. **`FileUploadField` component (must lose inline styles).** Find every `style={{...}}` in the JSX and move all declarations to CSS. CONCRETE actions:
       - Find each `style={{ color: '#dc2626', ... }}` for the per-file remove "Quitar" button → replace `<button>` with `<Button variant="ghost" size="sm" type="button" onClick={() => removeFile(index)} aria-label={`Quitar archivo ${file.name}`} className={styles.removeFile}>Quitar</Button>`. The `.removeFile` className in CSS sets `color: var(--wb-color-danger-600)` (overrides Phase 2 ghost default neutral color — minimal scope override per UI-SPEC §"Color" Per-element token map "FileUploadField 'Quitar'" row lock).
       - Find each `style={{ color: '#94a3b8', ... }}` for the KB-size suffix → move to `.kbSize { color: var(--wb-color-neutral-400); font-size: var(--wb-text-xs); }`. JSX becomes `<span className={styles.kbSize}>{kb} KB</span>`.
       - **CRITICAL:** after this step, `grep -E "style=\\{" frontend/src/pages/BriefPage.jsx` MUST return zero hits.
       - Preserve all upload state machine (idle / Subiendo… / list / error) and all backend POSTs to `/api/public/brief/:token/uploads`.

    6. **Submit button.** Replace `<button type="submit" className={styles.submitButton} disabled={submitting}>...</button>` with:
       ```jsx
       <Button type="submit" variant="primary" size="lg" disabled={submitting} loading={submitting}>
         {submitting ? 'Enviando...' : 'Enviar brief'}
       </Button>
       ```
       Per UI-SPEC §"Typography" Exception 6, this delegates geometry to Phase 2 (height 48 / padding-X 20 / `--wb-text-base` / weight 600), accepting the tradeoff (tighter padding 28→20, weight 700→600).

    7. **Submit error.** `{submitError && <p className={styles.submitError}>{submitError}</p>}` stays AS-IS structurally; CSS tokenized in Task 2 (color: var(--wb-color-danger-600); bg: var(--wb-color-danger-50); border: 1px solid var(--wb-color-danger-200); radius: var(--wb-radius-2)).

    8. **Loading state.** `<div className={styles.loadingText}>Cargando brief...</div>` stays AS-IS; CSS tokenized.

    9. **Error state (`Brief no disponible`).** `<div className={styles.errorState}><h2 className={styles.errorTitle}>Brief no disponible</h2><p className={styles.errorText}>{err.message || 'No se pudo cargar el brief'}</p></div>` — wrap in `<Card padding="lg" shadow="sm" radius="md" className={styles.errorState}>` (Card surface; local class keeps centered-content layout).

    10. **Success state.** `<div className={styles.successState}><div className={styles.successIcon}>✓</div><h2 className={styles.successTitle}>¡Gracias por completar el brief!</h2><p className={styles.successText}>Tu información fue recibida correctamente. El equipo se pondrá en contacto contigo pronto.</p></div>` — wrap in `<Card padding="lg" shadow="sm" radius="md" className={styles.successState}>`. The `.successIcon` keeps the ✓ glyph; CSS tokenized in Task 2 (bg: var(--wb-color-success-100); color: var(--wb-color-success-700) — locked AA upgrade from #16a34a 4.0:1 to #15803d 4.5:1 per UI-SPEC §"Accessibility Baseline").

    11. **Required note.** `<p className={styles.requiredNote}>Los campos marcados con * son obligatorios.</p>` stays AS-IS; CSS tokenized.

    12. **Preserve everything else byte-for-byte:**
       - All `publicFetch` calls and URL paths
       - Validation error template `Por favor responde: "{label}"` (do NOT rephrase)
       - File upload state machine (Subiendo…, Quitar, error fallback `No se pudo subir el archivo`)
       - Submit error fallback `No se pudo enviar el formulario`
       - Server-provided `brief.formTitle`, `brief.formDescription`, `err.message` pass through unmodified

    Do NOT introduce: a Modal, a Select, a Badge, an EmptyState component, new copy, English fallbacks, new dependencies, or any change to backend POST shapes.
  </action>
  <verify>
    <automated>
      cd frontend && npm run build 2>&1 | tail -20 &&
      grep -E "from '\\.\\./components/ui'" src/pages/BriefPage.jsx &&
      ! grep -E "style=\\{" src/pages/BriefPage.jsx &&
      ! grep -E "className=\\{styles\\.submitButton\\}" src/pages/BriefPage.jsx &&
      grep -E "<Button " src/pages/BriefPage.jsx &&
      grep -E "<Input " src/pages/BriefPage.jsx &&
      grep -E "publicFetch.*api/public/brief" src/pages/BriefPage.jsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/BriefPage.jsx` contains `import { Button, Input, Card } from '../components/ui'`
    - `grep -E "style=\\{" frontend/src/pages/BriefPage.jsx` returns zero hits (FileUploadField inline styles eliminated)
    - `grep -E "className=\\{styles\\.submitButton\\}" frontend/src/pages/BriefPage.jsx` returns zero hits
    - `grep -E "<Button " frontend/src/pages/BriefPage.jsx` returns at least 2 hits (submit + Quitar per file in FileUploadField)
    - `grep -E "<Input " frontend/src/pages/BriefPage.jsx` returns at least 2 hits (respondentName + respondentEmail; plus short_text questions if any)
    - `grep -E "<textarea" frontend/src/pages/BriefPage.jsx` returns hits ONLY for `long_text` questions (preserved native)
    - `grep -E "<input type=\"file\"" frontend/src/pages/BriefPage.jsx` returns at least 1 hit (file_upload questions, native)
    - `grep -E "<input type=\"radio\"" frontend/src/pages/BriefPage.jsx` may return hits (single_choice native — preserved)
    - `grep -E "publicFetch.*api/public/brief" frontend/src/pages/BriefPage.jsx` returns at least 3 hits (load + submit + uploads)
    - All Spanish copy strings present verbatim (greppable individually): `Tus datos`, `Nombre completo`, `Correo electrónico`, `Tu nombre`, `tu@email.com`, `Subiendo…`, `Quitar`, `Enviar brief`, `Enviando...`, `Cargando brief...`, `Brief no disponible`, `¡Gracias por completar el brief!`, `Tu información fue recibida correctamente. El equipo se pondrá en contacto contigo pronto.`, `Los campos marcados con * son obligatorios.`
    - Validation error template present verbatim: `Por favor responde:`
    - Error fallbacks present: `No se pudo subir el archivo`, `No se pudo enviar el formulario`, `No se pudo cargar el brief`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>BriefPage.jsx consumes Button + Input + Card; FileUploadField inline styles moved to CSS; native textarea/file/radio/checkbox preserved; every backend call, server-provided string, and validation message preserved verbatim; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize BriefPage.module.css; eliminate 38 hex literals; preserve native control styling</name>
  <files>frontend/src/pages/BriefPage.module.css</files>
  <read_first>
    - frontend/src/pages/BriefPage.module.css (current 307 lines — 38 hardcoded hex; classify every selector: keep as layout / tokenize / delete because delegated to Phase 2)
    - frontend/src/styles/tokens.css (target tokens; full inventory)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Spacing Scale" Exception 3 (.page padding 40/16/80 → 48/16/64)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Typography" Exceptions 2–6 (Brief snaps)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Color" Special Remappings + Per-element token map (Brief rows)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Accessibility Baseline" (success checkmark AA upgrade #16a34a → var(--wb-color-success-700))
  </read_first>
  <action>
    Reduce `frontend/src/pages/BriefPage.module.css` to layout + tokenized typography/color, deleting selectors now provided by Phase 2 components, AND adding tokenized rules for the native textarea / file / radio / checkbox controls that stay native:

    1. **DELETE the following selector blocks entirely** (now provided by `<Button>` / `<Input>` from Phase 2):
       - `.input` and `.input:focus`, `.input::placeholder`, `.input:disabled` variants (Phase 2 `<Input>` owns these for the migrated short_text/email cases)
       - `.submitButton` and any `:hover`/`:disabled` variants (Phase 2 `<Button variant="primary" size="lg">` owns these)

    2. **KEEP and tokenize the following layout-bearing selectors** (replace every #hex / px with a token per the UI-SPEC mappings):
       - `.page` — replace `background: #f4f7fb` → `var(--wb-bg)` (#f8fafc per UI-SPEC §"Color" Special Remappings); replace `padding: 40px 16px 80px` → `var(--wb-space-12) var(--wb-space-4) var(--wb-space-16)` (48/16/64 per UI-SPEC §"Spacing Scale" Exception 3); add `min-height: 100vh; color: var(--wb-text);` if currently missing.
       - `.formContainer` (or equivalent wrapper) — keep `max-width: 760px; margin: 0 auto;` if literal; tokenize any hex.
       - `.formTitle` — replace any `color: #...` → `var(--wb-color-primary-900)`; replace `font-size: 28px` → `var(--wb-text-3xl)` (30 per locked snap from UI-SPEC §"Typography"); set `font-weight: var(--wb-weight-bold)` (700); set `line-height: var(--wb-leading-3xl)` (1.2).
       - `.formDescription` — replace `color: #...` → `var(--wb-color-neutral-600)`; replace `font-size: 15px` → `var(--wb-text-base)` (16 per UI-SPEC Exception 3); replace `line-height: 1.6` → `var(--wb-leading-normal)` (1.5 — Exception 3 acceptable shift).
       - `.requiredNote` — replace `color: #...` → `var(--wb-color-neutral-500)`; replace `font-size: ...` → `var(--wb-text-xs)`; weight: `var(--wb-weight-regular)`.
       - `.identityCard, .fieldGroup, .errorState, .successState` — these wrap `<Card>` from Phase 2; DELETE `background`, `border`, `border-radius`, `box-shadow` declarations from any of these (Card props provide them). KEEP layout (display, grid-template, gap, custom margins).
       - `.identityCard` (instance) — keep margin layout; replace any `padding: 24px` → DELETE (Card prop provides); replace `margin-bottom: 32px` (or whatever) → `var(--wb-space-8)`.
       - `.identityTitle` — replace `color: #091223` → `var(--wb-color-primary-900)`; replace `font-size: 15px` → `var(--wb-text-sm)` (14 per UI-SPEC Exception 2); set `font-weight: var(--wb-weight-bold)` (700); set `line-height: var(--wb-leading-normal)`; `margin: 0 0 var(--wb-space-3)` or similar.
       - `.identityGrid` — keep grid; replace gap → `var(--wb-space-4)` (16) per UI-SPEC §"Spacing Scale" row.
       - `.field` (label wrapper) — keep flex/column; replace gap → `var(--wb-space-2)` (8); replace `font-size: 14px` → `var(--wb-text-sm)`; weight 500 → `var(--wb-weight-medium)`; replace any `color: #...` → `var(--wb-color-neutral-800)` (questionLabel) or `var(--wb-color-neutral-700)` (others).
       - `.fieldGroup` (instance) — DELETE surface chrome; keep layout; tokenize gap/padding internals.
       - `.questionLabel` — `color: var(--wb-color-neutral-800); font-size: var(--wb-text-sm); font-weight: var(--wb-weight-semibold);` and any `margin-bottom: 8px` → `var(--wb-space-2)`.
       - `.required` (the `*` span) — `color: var(--wb-color-danger-500);`.
       - `.hint` — `color: var(--wb-color-neutral-500); font-size: var(--wb-text-xs); font-weight: var(--wb-weight-regular);`.
       - `.sectionHeader` — replace any literal border colors `#e2e8f0` → `var(--wb-color-neutral-200)`; keep `border-bottom: 2px solid var(--wb-color-neutral-200);`.
       - `.sectionHeaderTitle` — replace `color: #...` → `var(--wb-color-neutral-900)`; replace `font-size: 17px` → `var(--wb-text-lg)` (18 per UI-SPEC snap); weight 700 → `var(--wb-weight-bold)`.
       - `.optionLabel` — `color: var(--wb-color-neutral-800); font-size: var(--wb-text-sm);`.
       - `.optionInput` — keep `width: 16px; height: 16px;` (UI-SPEC §"Spacing Scale" Exception 4 — already on-scale); add `accent-color: var(--wb-color-primary-900);` (per UI-SPEC §"Color" Per-element token map; replaces any current `accent-color: #212222`).
       - `.submitError` — `color: var(--wb-color-danger-600); background: var(--wb-color-danger-50); border: 1px solid var(--wb-color-danger-200); border-radius: var(--wb-radius-2); padding: var(--wb-space-3) var(--wb-space-4);` (or similar — preserve current padding style, just tokenize).
       - `.loadingText` — `color: var(--wb-color-neutral-500); font-size: var(--wb-text-base);` text-align center if currently set.
       - `.errorTitle` — replace `color: #...` → `var(--wb-color-neutral-900)`; replace `font-size: 22px` → `var(--wb-text-2xl)` (24 per UI-SPEC Exception 5 — accept 2px shift); weight: `var(--wb-weight-bold)`; line-height: `var(--wb-leading-2xl)`.
       - `.errorText` — `color: var(--wb-color-neutral-500); font-size: var(--wb-text-base);`.
       - `.successIcon` — keep `width: 56px; height: 56px;` (control glyph size, not on spacing scale; allowed); replace `background: #dcfce7` → `var(--wb-color-success-100)`; **CRITICAL A11Y UPGRADE:** replace `color: #16a34a` → `var(--wb-color-success-700)` (#15803d) — locked per UI-SPEC §"Accessibility Baseline" (4.0:1 → 4.5:1 AA); keep `font-size: 26px; font-weight: 700;` (glyph sizing literals — UI-SPEC Exception 4); `border-radius: 50%; display: grid; place-items: center;` literals OK.
       - `.successTitle` — replace `color: #...` → `var(--wb-color-neutral-900)`; replace `font-size: 24px` → `var(--wb-text-2xl)`; weight: `var(--wb-weight-bold)`; line-height: `var(--wb-leading-2xl)`.
       - `.successText` — `color: var(--wb-color-neutral-600); font-size: var(--wb-text-base);`.
       - `.errorState`, `.successState` — DELETE surface chrome (Card provides); keep layout (display: grid; place-items: center; gap; padding tokenized via Card prop instead).

    3. **ADD tokenized rules for native controls that stay native** (per UI-SPEC §"Pages × Components Coverage Matrix" lock — `<textarea>` and `<input type='file'>` stay native because Phase 2 has no primitives for them):
       ```css
       .textarea {
         width: 100%;
         border: 1px solid var(--wb-border);
         border-radius: var(--wb-radius-2);
         padding: var(--wb-space-2) var(--wb-space-3);
         background: var(--wb-surface);
         color: var(--wb-text);
         font: inherit;
         line-height: var(--wb-leading-normal);
         resize: vertical;
       }
       .textarea:focus {
         outline: none;
         border-color: var(--wb-color-primary-900);    /* was #212222; per UI-SPEC §"Color" Special Remappings */
         box-shadow: 0 0 0 3px var(--wb-color-primary-200);
       }
       .textarea::placeholder {
         color: var(--wb-color-neutral-400);
       }

       /* Native <input type="file"> — keep browser default chrome but tokenize the surrounding label/list */
       .fileInput {
         /* let the browser style the picker; just space it */
         margin-top: var(--wb-space-2);
       }

       /* FileUploadField list classes (replacing former inline styles) */
       .fileList {
         list-style: none;
         padding: 0;
         margin: var(--wb-space-3) 0 0;
         display: flex;
         flex-direction: column;
         gap: var(--wb-space-2);
       }
       .fileItem {
         display: flex;
         align-items: center;
         gap: var(--wb-space-2);
         color: var(--wb-color-neutral-700);
         font-size: var(--wb-text-sm);
       }
       .kbSize {
         color: var(--wb-color-neutral-400);
         font-size: var(--wb-text-xs);
       }
       .removeFile {
         /* override Phase 2 Button ghost color to danger — minimal scope */
         color: var(--wb-color-danger-600);
       }
       .removeFile:hover {
         color: var(--wb-color-danger-700);   /* if exists in tokens; else fall back to var(--wb-color-danger-600) — verify */
       }
       ```

    4. **REPLACE the page-bg literal `#f4f7fb`** anywhere it still appears in `.page`, `.errorState`, `.successState` background, etc. with `var(--wb-bg)` per UI-SPEC §"Color" Special Remappings (4-unit channel shift accepted).

    5. **REPLACE any remaining `#212222` (focus borders, accent-color)** with `var(--wb-color-primary-900)` (#091223) per UI-SPEC Special Remappings.

    6. **REPLACE any `#000` (submitButton hover bg)** — the submit button is now `<Button>` so this rule should be DELETED with the rest of `.submitButton`. If a stray `#000` remains anywhere else, replace with `var(--wb-color-primary-700)` per UI-SPEC.

    7. **`@media (max-width: 560px)` block.** Keep layout adjustments (grid → 1fr); replace any hex literals; tokenize spacing.

    8. After all edits, the file MUST satisfy `05-UI-SPEC.md` §"Migration Strategy" success criteria 1–4 for BriefPage.module.css.
  </action>
  <verify>
    <automated>
      ! grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/pages/BriefPage.module.css &&
      ! grep -E "^\\.(input|submitButton)\\b" frontend/src/pages/BriefPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/BriefPage.module.css &&
      grep -E "\\.textarea" frontend/src/pages/BriefPage.module.css &&
      grep -E "var\\(--wb-color-success-700\\)" frontend/src/pages/BriefPage.module.css &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/BriefPage.module.css` returns ZERO hits (38 → 0)
    - `grep -E "^\\.(input|submitButton)\\b" frontend/src/pages/BriefPage.module.css` returns zero hits (Phase 2 owns these)
    - `grep -E "z-index:\\s*[0-9]+" frontend/src/pages/BriefPage.module.css` returns zero hits
    - `grep -E "\\.textarea" frontend/src/pages/BriefPage.module.css` returns at least 1 hit (.textarea selector exists, tokenized)
    - `grep -E "var\\(--wb-color-success-700\\)" frontend/src/pages/BriefPage.module.css` returns at least 1 hit (.successIcon AA upgrade)
    - `grep -E "accent-color:\\s*var\\(--wb-color-primary-900\\)" frontend/src/pages/BriefPage.module.css` returns at least 1 hit (.optionInput uses tokenized accent)
    - `grep -E "var\\(--wb-bg\\)" frontend/src/pages/BriefPage.module.css` returns at least 1 hit (.page background replaced)
    - `cd frontend && npm run build` exits 0
    - Manual smoke (recorded in SUMMARY): open `/brief/:token`, fill form, upload file (Quitar), submit, see success state with checkmark in AA-compliant color
  </acceptance_criteria>
  <done>BriefPage.module.css has zero hex literals; native textarea/file/radio/checkbox tokenized in-place; .successIcon a11y upgrade applied; per-cohort gates 1–4 pass; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public respondent (unauthenticated) → WeBrief backend | Anyone with a brief token can hit `/api/public/brief/:token`, `/responses`, `/uploads`. Migration changes only JSX/CSS — backend boundary unchanged. |
| Browser → file upload endpoint | `<input type="file">` flow uploads to `/api/public/brief/:token/uploads`. Refactor moves inline styles to CSS only — upload logic unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Brief submit POST `/responses` | accept | Migration preserves the existing fetch payload shape (respondentName, respondentEmail, answers[]) byte-for-byte. No new attack surface. |
| T-05-05 | Information disclosure | File upload `/uploads` | accept | Existing flow preserved — file size/type validation lives in the backend; refactor only restyles the picker / list. |
| T-05-06 | Spoofing | Public brief token route | accept | Token validation lives in the backend (`/api/public/brief/:token`); refactor cannot change auth posture. CONTEXT.md decisions lock "no Supabase Auth introduced" for public flows. |
</threat_model>

<verification>
BriefPage migration gates (per `05-UI-SPEC.md` §"Migration Strategy" success criteria 1–4, restricted to this plan's two files):

```bash
# Gate 1: ZERO hardcoded #hex (38 → 0)
grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/BriefPage.module.css   # expect: zero

# Gate 2: zero inline styles in BriefPage.jsx (FileUploadField cleanup)
grep -E "style=\\{" frontend/src/pages/BriefPage.jsx   # expect: zero

# Gate 3: zero local Button/Input selectors that Phase 2 owns
grep -E "^\\.(input|submitButton)\\b" frontend/src/pages/BriefPage.module.css   # expect: zero

# Gate 4: a11y upgrade locked
grep -E "var\\(--wb-color-success-700\\)" frontend/src/pages/BriefPage.module.css   # expect: at least 1 hit

# Gate 5: build green
cd frontend && npm run build
```

Manual QA (per `05-UI-SPEC.md` §"Per-page QA" BriefPage row):
- Open `/brief/:token` (valid token) → loading → renders form
- Required-marker `*` shows on required fields with red color
- Type into name/email + question fields → state updates
- Upload a PDF → "Subiendo…" then file appears in list with KB suffix
- Click "Quitar" → file removed
- Submit with required field empty → submit error appears with the validation template `Por favor responde: "{label}"`
- Submit complete → success state with checkmark icon in AA color and message
- Open with invalid token → error state shows server message OR `No se pudo cargar el brief`

Console-cleanliness gate: zero new errors/warnings introduced.
</verification>

<success_criteria>
- BriefPage.jsx imports from `../components/ui` and consumes Button + Input + Card; native textarea/file/radio/checkbox preserved with tokenized CSS
- FileUploadField inline styles eliminated (zero `style={...}` in BriefPage.jsx)
- BriefPage.module.css has ZERO hex literals (38 → 0)
- .successIcon color upgraded to var(--wb-color-success-700) (AA 4.5:1)
- All five gates above pass
- Atomic commit: `refactor(05): migrate BriefPage to shared UI primitives + tokens`
- UI-08 fully satisfied (combined with Plan 05-01)
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-pages-verification/05-02-SUMMARY.md` recording:
- Gate results (grep outputs with counts; emphasize 38 → 0 hex)
- Selectors deleted from BriefPage.module.css
- Visual deltas applied (formTitle 28→30, identityTitle 15→14, formDescription 15→16, sectionHeaderTitle 17→18, errorTitle 22→24; page bg `#f4f7fb`→`#f8fafc`; focus border `#212222`→`#091223`; page padding 40/16/80 → 48/16/64; success checkmark color `#16a34a`→`#15803d`)
- A11y upgrade evidence: contrast ratio before/after on .successIcon
- Inline-styles eliminated: list of selectors that absorbed FileUploadField inline styles (.fileList, .fileItem, .kbSize, .removeFile)
- Any pre-existing warnings tolerated
- Screenshots before/after (if preview tools available)
</output>
