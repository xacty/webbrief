---
phase: 05-public-pages-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/SharePage.jsx
  - frontend/src/pages/SharePage.module.css
autonomous: true
requirements:
  - UI-08

must_haves:
  truths:
    - "SharePage.jsx renders identity gate, viewer header, comment form, approvals panel, and the document body using shared <Card>, <Input>, <Button> primitives instead of raw .primaryButton/.secondaryButton/.dangerButton/.linkButton classes"
    - "SharePage.module.css contains zero hardcoded #hex outside the @media print block"
    - "@media print continues to hide the identity gate, the feedback panel, and the 'Exportar PDF' button — print preview shows ONLY document pages and forces page-break-after between pageBlocks"
    - "Email gate flow preserved: invalid token shows error state; identifying viewer persists name+email in localStorage `share-viewer-${token}`; 'Cambiar datos' clears viewer and re-shows the identity card"
    - "Comment submission, Aprobar action, and Pedir cambios action preserve their backend POSTs and feedback strings (Comentario enviado. / Aprobación registrada. / Pedido de cambios registrado.) byte-for-byte"
    - "SharePage smoke flow console clean (no React warnings, no missing-key/propType warnings) under preview tools or `npm run build`"
  artifacts:
    - path: "frontend/src/pages/SharePage.jsx"
      provides: "Public document share view consuming shared Button/Input/Card; identity gate, viewer panel, document flow"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/SharePage.module.css"
      provides: "Layout-only module: page chrome, identity-grid layout, feedback-panel grid, document gap, mobile media-query, @media print rules. NO Button/Input/Card visual rules remain."
      min_lines: 40
  key_links:
    - from: "SharePage.jsx"
      to: "/api/public/share/:token (GET)"
      via: "publicFetch in useEffect — preserved from current implementation"
      pattern: "publicFetch.*api/public/share"
    - from: "SharePage.jsx"
      to: "/api/public/share/:token/comments (POST)"
      via: "submitComment handler — preserved verbatim"
      pattern: "/api/public/share/.*/comments"
    - from: "SharePage.jsx"
      to: "/api/public/share/:token/approvals (POST)"
      via: "submitApproval handler — preserved verbatim"
      pattern: "/api/public/share/.*/approvals"
    - from: "SharePage.module.css"
      to: "@media print"
      via: ".printHide / migrated selectors hide identity + feedback + Exportar PDF wrapper"
      pattern: "@media print"
---

<objective>
Migrate `SharePage.jsx` and `SharePage.module.css` to consume Phase 1 tokens and Phase 2 shared primitives (`Button`, `Input`, `Card`). Per `05-UI-SPEC.md` §"Migration Strategy" Step 1, this is the lower-risk public-page migration (5 hardcoded hex literals, 219 jsx lines, 251 css lines) and validates the print/PDF preservation pattern before BriefPage.

Purpose: Address requirement UI-08 for the document-share surface. Public viewer sees zero functional change; the visual delta is limited to the locked snaps in `05-UI-SPEC.md` §"Deviations" (eyebrow 13→12px, h2 22→20px, h1 32→30px, padding 36/44 → 32/48). Email gate, comments, approvals/change-requests, and print/PDF flow stay identical.
Output: SharePage migrated; print rules updated; success criteria 1–5 of `05-UI-SPEC.md` §"Migration Strategy" pass for SharePage's two files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/05-public-pages-verification/05-CONTEXT.md
@.planning/phases/05-public-pages-verification/05-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/index.js
@frontend/src/components/ui/Button.jsx
@frontend/src/components/ui/Input.jsx
@frontend/src/components/ui/Card.jsx
@frontend/src/pages/SharePage.jsx
@frontend/src/pages/SharePage.module.css

<interfaces>
<!-- Shared UI public surface (from frontend/src/components/ui/index.js): -->
import { Button, Input, Card } from '../components/ui';

<!-- Button props: variant ('primary' | 'secondary' | 'ghost' | 'danger'), size ('sm' | 'md' | 'lg'), type, disabled, loading, icon, iconPosition, fullWidth, onClick, aria-label, children -->
<!-- Input props: type ('text' | 'email' | 'password' | ...), label, helperText, error, required, disabled, id, fullWidth (default true), ...native input props. NOTE: Input does NOT support type='textarea' — keep <textarea> native and tokenize CSS in-place. -->
<!-- Card props: as ('div' | 'button' | etc.), padding ('sm' | 'md' | 'lg'), shadow ('sm' | 'md' | 'lg' | 'xl'), radius ('sm' | 'md' | 'lg'), interactive, onClick, className, children -->

<!-- Tokens used in this plan (from frontend/src/styles/tokens.css §Phase 1): -->
- Layout/background: var(--wb-bg), var(--wb-surface)
- Spacing: var(--wb-space-1..16) on the 4-multiples scale
- Typography sizes: --wb-text-xs (12) / --wb-text-sm (14) / --wb-text-base (16) / --wb-text-lg (18) / --wb-text-xl (20) / --wb-text-3xl (30)
- Typography line-heights: --wb-leading-normal (1.5), --wb-leading-relaxed (1.75), --wb-leading-xl (1.4), --wb-leading-3xl (1.2)
- Typography weights: --wb-weight-medium (500), --wb-weight-semibold (600)
- Color: var(--wb-color-neutral-{500,600,700}), var(--wb-color-primary-{200,900}), var(--wb-color-danger-{50,200,600}), var(--wb-color-success-700), var(--wb-text-muted), var(--wb-border), var(--wb-border-strong), var(--wb-success), var(--wb-primary)
- Radius: var(--wb-radius-2) for inputs/cards (8px on-scale)
- Shadow: var(--wb-shadow-sm) for the card surface
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SharePage.jsx imports and JSX to shared primitives</name>
  <files>frontend/src/pages/SharePage.jsx</files>
  <read_first>
    - frontend/src/pages/SharePage.jsx (current shape — preserve every fetch, every state setter, every useEffect, every error/feedback string verbatim)
    - frontend/src/components/ui/Button.jsx (variants/sizes/fullWidth/loading API; aria-label rule for icon-only)
    - frontend/src/components/ui/Input.jsx (label prop, type='text'/'email', aria-required, helperText/error API; NO textarea support — keep <textarea> native)
    - frontend/src/components/ui/Card.jsx (padding/shadow/radius/className props; how to use as a wrapper while keeping styles.identityCard / styles.feedbackPanel as layout shells)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Pages × Components Coverage Matrix" row for SharePage
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Copywriting Contract" SharePage rows (every CTA / label / state string is locked verbatim)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Print/PDF Protection" — `.printHide` wrapper pattern for the Exportar PDF button
  </read_first>
  <action>
    Refactor `frontend/src/pages/SharePage.jsx` to consume shared primitives. CONCRETE replacements (preserve every handler, every fetch, every state setter, every Spanish string verbatim):

    1. **Imports.** At the top, add: `import { Button, Input, Card } from '../components/ui'`.

    2. **Header "Exportar PDF" button.** Replace `<button className={styles.secondaryButton} onClick={() => window.print()}>Exportar PDF</button>` with a `.printHide` wrapper around a Phase 2 Button:

       ```jsx
       <div className={styles.printHide}>
         <Button variant="secondary" size="md" onClick={() => window.print()} aria-label="Exportar a PDF">
           Exportar PDF
         </Button>
       </div>
       ```

       The `.printHide` class is added to `SharePage.module.css` in Task 2 and keys the `@media print` `display: none` rule.

    3. **Identity gate card (no viewer).** Wrap the existing `<form className={styles.identityCard} onSubmit={handleIdentify}>` in `<Card padding="md" shadow="sm" radius="sm" className={styles.identityCard}>`. Inside the form:
       - Replace the `<h2 className={styles.cardTitle}>Identifícate para comentar o aprobar</h2>` — keep the heading element but the local `.cardTitle` styles will be tokenized in Task 2.
       - Replace the two `<label className={styles.field}>...<input ... required /></label>` pairs with `<Input>` (type="text" for Nombre, type="email" for Email):
         ```jsx
         <Input
           label="Nombre"
           required
           value={name}
           onChange={(event) => setName(event.target.value)}
         />
         <Input
           type="email"
           label="Email"
           required
           value={email}
           onChange={(event) => setEmail(event.target.value)}
         />
         ```
         Phase 2 `<Input>` renders the label + native input + helper/error slots; do NOT wrap it in another `<label>`.
       - Replace `<button className={styles.primaryButton} type="submit">Continuar</button>` with `<Button type="submit" variant="primary" size="md">Continuar</Button>`.

    4. **Viewer-identified panel.** Wrap the existing `<aside className={styles.feedbackPanel}>` in `<Card padding="md" shadow="sm" radius="sm" className={styles.feedbackPanel}>`. Inside:
       - Keep `<p className={styles.viewerText}>Comentando como {viewer.name} · {viewer.email}</p>` exactly.
       - Replace `<button className={styles.linkButton} onClick={clearViewer}>Cambiar datos</button>` with `<Button variant="ghost" size="sm" type="button" onClick={clearViewer}>Cambiar datos</Button>`.
       - Comment form: keep the `<label className={styles.field}>Comentario<textarea ... /></label>` pattern AS-IS (Phase 2 `<Input>` does not support textarea). The `.field` class stays in CSS as layout-only (label-text + textarea stack); textarea visuals (border/focus/radius) get tokenized in Task 2.
       - Replace `<button className={styles.secondaryButton} type="submit" disabled={submitting}>Enviar comentario</button>` with `<Button type="submit" variant="secondary" size="md" disabled={submitting}>Enviar comentario</Button>`.
       - Approval box: keep the textarea pattern as-is for "Nota de aprobación o cambios" (same reason).
       - Replace the two action buttons:
         ```jsx
         <Button variant="primary" size="md" type="button" disabled={submitting} onClick={() => submitApproval('approved')}>
           Aprobar
         </Button>
         <Button variant="danger" size="md" type="button" disabled={submitting} onClick={() => submitApproval('changes_requested')}>
           Pedir cambios
         </Button>
         ```
       - `{feedback && <p className={styles.feedback}>{feedback}</p>}` stays untouched — `.feedback` is tokenized in Task 2 (keeps the `var(--wb-success)` teal alias per UI-SPEC §"Color" lock).

    5. **Document flow.** The `<main className={styles.document}>` block with `pages.map(...)` and `<section className={styles.pageBlock}>` stays as-is structurally. Wrap the `<section className={styles.pageBlock}>` in `<Card padding="lg" shadow="sm" radius="sm" className={styles.pageBlock} as="section">` IF Card supports `as="section"` (check Card.jsx in read_first); otherwise keep `<section className={styles.pageBlock}>` and let `.pageBlock` provide its own surface chrome via tokens in Task 2 — the local class will not be deleted, so the visual outcome is identical.

       **Decision:** verify the Card API's `as` prop. If Card supports it, use it for `.pageBlock` consistency. If not, document in SUMMARY that `.pageBlock` remains a self-styled section (still tokenized) — do NOT add an extra wrapper div around every page (would change DOM structure and break the `dangerouslySetInnerHTML` content tree).

    6. **Loading and error states.** `<div className={styles.state}>Cargando contenido...</div>` and `<div className={styles.state}>{error}</div>` stay verbatim; `.state` is tokenized in Task 2.

    7. **Preserve everything else byte-for-byte:**
       - All `publicFetch` calls and URL paths
       - `localStorage` `share-viewer-${token}` key
       - `typeHeadings` map (Página web compartida / Artículo compartido / FAQs compartidas / Brief compartido / Contenido compartido fallback)
       - Every Spanish copy string (per `05-UI-SPEC.md` §"Copywriting Contract")
       - Every state name (`viewer`, `name`, `email`, `comment`, `approvalComment`, `feedback`, `submitting`)
       - Every error fallback (`No se pudo abrir el brief` / `No se pudo enviar el comentario` / `No se pudo registrar la respuesta`)

    Do NOT introduce: a Modal, a Badge, a Select, an EmptyState component, new copy, new tooltips, English fallbacks, new dependencies, additional aria roles beyond what Phase 2 components emit, or any change to backend POST shapes.
  </action>
  <verify>
    <automated>
      cd frontend && npm run build 2>&1 | tail -20 &&
      grep -E "from '\\.\\./components/ui'" src/pages/SharePage.jsx &&
      ! grep -E "className=\\{styles\\.(primaryButton|secondaryButton|dangerButton|linkButton)\\}" src/pages/SharePage.jsx &&
      grep -E "<Button " src/pages/SharePage.jsx | grep -E "variant=\"(primary|secondary|danger|ghost)\"" &&
      grep -E "<Input " src/pages/SharePage.jsx | head -2 &&
      grep -E "publicFetch.*api/public/share" src/pages/SharePage.jsx &&
      grep -E "share-viewer-\\$\\{token\\}" src/pages/SharePage.jsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/SharePage.jsx` contains `import { Button, Input, Card } from '../components/ui'`
    - `grep -E "className=\\{styles\\.(primaryButton|secondaryButton|dangerButton|linkButton)\\}" frontend/src/pages/SharePage.jsx` returns zero hits
    - `grep -E "<Button " frontend/src/pages/SharePage.jsx` returns at least 5 hits (Exportar PDF, Continuar, Cambiar datos, Enviar comentario, Aprobar, Pedir cambios — at least 5 distinct Button elements)
    - `grep -E "<Input " frontend/src/pages/SharePage.jsx` returns exactly 2 hits (Nombre + Email in identity gate)
    - `grep -E "<textarea" frontend/src/pages/SharePage.jsx` returns exactly 2 hits (comment + approvalComment — preserved native)
    - `grep -E "publicFetch.*api/public/share" frontend/src/pages/SharePage.jsx` returns at least 3 hits (load + comments POST + approvals POST)
    - `grep -E "share-viewer-\\$\\{token\\}" frontend/src/pages/SharePage.jsx` returns at least 2 hits (read in init + write in handleIdentify + remove in clearViewer = 3, but allow ≥2 to tolerate refactor)
    - All Spanish CTA strings present verbatim: `Continuar`, `Cambiar datos`, `Enviar comentario`, `Aprobar`, `Pedir cambios`, `Exportar PDF` (greppable individually)
    - All feedback strings present verbatim: `Comentario enviado.`, `Aprobación registrada.`, `Pedido de cambios registrado.`
    - All error fallbacks present verbatim: `No se pudo abrir el brief`, `No se pudo enviar el comentario`, `No se pudo registrar la respuesta`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>SharePage.jsx consumes Button/Input/Card from `../components/ui`; native `<textarea>` preserved (Phase 2 has no textarea primitive); every backend call, localStorage key, Spanish string, and error path preserved verbatim; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize SharePage.module.css and add .printHide; preserve @media print fidelity</name>
  <files>frontend/src/pages/SharePage.module.css</files>
  <read_first>
    - frontend/src/pages/SharePage.module.css (current full file — 251 lines; classify every selector: keep as layout / tokenize / delete because delegated to Phase 2)
    - frontend/src/styles/tokens.css (target tokens; full inventory)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Spacing Scale" (Exception 2: `.pageBlock` remap 36/44 → var(--wb-space-8)/var(--wb-space-12) = 32/48)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Typography" Exception 1 (`.content` line-height 1.65 → var(--wb-leading-relaxed) = 1.75)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Color" Per-element token map (Share rows)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Print/PDF Protection" (locked .printHide wrapper pattern; print block keeps px literals)
    - .planning/phases/05-public-pages-verification/05-UI-SPEC.md §"Migration Strategy" success criteria 1–4 (per-cohort gates)
  </read_first>
  <action>
    Reduce `frontend/src/pages/SharePage.module.css` to layout + tokenized typography/color, deleting selectors now provided by Phase 2 components:

    1. **DELETE the following selector blocks entirely** (now provided by `<Button>` / `<Input>` from Phase 2):
       - `.primaryButton` (and any `:hover`/`:disabled` variant)
       - `.secondaryButton` (and any `:hover`/`:disabled` variant)
       - `.dangerButton` (and any `:hover`/`:disabled` variant)
       - `.linkButton` (and any `:hover` variant)
       - `.field input`, `.field textarea` (the raw `border/border-radius/padding/background/color/font` declarations) AND `.field input:focus`, `.field textarea:focus` (replaced for `<Input>`-rendered fields by Phase 2; the native `<textarea>` blocks needed by Comment/Approval get a NEW shared class — see Step 4 below).

    2. **KEEP and tokenize layout-bearing selectors** (replace every hardcoded value with a token):
       - `.page` — keep `min-height: 100vh`; replace `background: var(--wb-bg)` (already tokenized — verify); replace `color: var(--wb-text)` (already tokenized — verify); replace `padding: 32px 24px 72px` with `padding: var(--wb-space-8) var(--wb-space-6) var(--wb-space-12)` — accept the 8px shift on bottom (72→48) per UI-SPEC §"Spacing Scale" snap rule.
       - `.header`, `.identityCard`, `.feedbackPanel`, `.document` — keep `max-width: 980px; margin: 0 auto` (980 is a layout literal — width caps are not in the spacing scale; allowed).
       - `.header` — keep flex layout; replace `gap: 24px` → `var(--wb-space-6)`; replace `margin-bottom: 20px` → `var(--wb-space-5)`.
       - `.eyebrow`, `.viewerText` — replace `margin: 0 0 6px` → `0 0 var(--wb-space-1)` (4) — accept 2px shift from 6→4 (acceptable per UI-SPEC); replace `font-size: 13px` → `var(--wb-text-xs)` (12); keep `font-weight: 600` → `var(--wb-weight-semibold)`; keep `color: var(--wb-text-muted)` (already tokenized — verify).
       - `.title` — replace `font-size: 32px` → `var(--wb-text-3xl)` (30) per locked snap; `line-height: 1.1` → `var(--wb-leading-3xl)` (1.2); keep `font-weight: 600` → `var(--wb-weight-semibold)`; add `color: var(--wb-color-primary-900)`.
       - `.subtitle` — replace `margin: 8px 0 0` → `var(--wb-space-2) 0 0`; replace `color: var(--wb-text-muted)` (verify); replace `line-height: 1.5` → `var(--wb-leading-normal)`.
       - `.identityCard, .feedbackPanel, .pageBlock` shared block — these wrap `<Card>` from Phase 2 in JSX, so DELETE the surface declarations from this rule (border, background, box-shadow, border-radius come from `<Card padding="md" shadow="sm" radius="sm">`). KEEP only any layout properties needed (none in current rule — the whole block becomes obsolete; remove it).
       - `.identityCard` (instance) — keep `padding: 18px` → DELETE (Card provides padding via prop); keep `margin-bottom: 20px` → `var(--wb-space-5)`.
       - `.cardTitle` — replace `margin: 0 0 14px` → `0 0 var(--wb-space-3)` (12 — accept 2px shift); replace `font-size: 18px` → `var(--wb-text-lg)`; keep `font-weight: 600` → `var(--wb-weight-semibold)`; add `color: var(--wb-color-primary-900)`.
       - `.identityGrid` — keep grid layout; replace `gap: 12px` → `var(--wb-space-3)`; replace `margin-bottom: 14px` → `var(--wb-space-3)` (accept 2px shift).
       - `.field` (label) — keep flex/column; replace `gap: 7px` → `var(--wb-space-2)` (8 — accept 1px shift); replace `font-size: 14px` → `var(--wb-text-sm)`; keep `font-weight: 500` → `var(--wb-weight-medium)`; replace `color: var(--wb-text)` (verify).
       - `.feedbackPanel` (instance) — DELETE the surface declarations; keep grid layout (`display: grid; grid-template-columns: 1fr 1fr; gap: var(--wb-space-4); padding: var(--wb-space-4); margin-bottom: var(--wb-space-5)`). Card provides surface; the inner padding here matches the `<Card padding="md">` token (16px) — but since Card already adds its own padding, set `padding: 0` on the local class to avoid double-padding (verify Card's md padding token; UI-SPEC says Card "provides padding via prop"). **Lock:** since Card adds the padding, REMOVE local padding from `.feedbackPanel`.
       - `.feedbackForm`, `.approvalBox` — keep flex/column; replace `gap: 10px` → `var(--wb-space-2)` (accept 2px shift) or `var(--wb-space-3)` (accept 2px other direction). **Lock:** `var(--wb-space-2)` (8).
       - `.actions` — keep flex/wrap; replace `gap: 10px` → `var(--wb-space-2)`.
       - `.feedback` — replace `margin: 0`; keep `grid-column: 1 / -1`; replace `color: var(--wb-success)` (already tokenized teal alias — UI-SPEC locks this); replace `font-size: 14px` → `var(--wb-text-sm)`.
       - `.document` — keep flex/column; replace `gap: 18px` → `var(--wb-space-5)` (20 — accept 2px shift).
       - `.pageBlock` — replace `padding: 36px 44px` → `var(--wb-space-8) var(--wb-space-12)` (32/48 per UI-SPEC §"Spacing Scale" Exception 2). Surface chrome (border/background/box-shadow/border-radius) comes from `<Card>` IF Task 1 wrapped it; otherwise keep the surface tokenized: `border: 1px solid var(--wb-border); background: var(--wb-surface); box-shadow: var(--wb-shadow-sm); border-radius: var(--wb-radius-2)`.
       - `.pageBlock h2` — replace `margin: 0 0 18px` → `0 0 var(--wb-space-5)` (20 — accept 2px shift); replace `font-size: 22px` → `var(--wb-text-xl)` (20 per locked snap); replace `line-height: 1.2` → `var(--wb-leading-xl)` (1.4 — accept 0.2 shift on a heading); keep `font-weight: 600` → `var(--wb-weight-semibold)`; add `color: var(--wb-color-primary-900)`.
       - `.content` — replace `line-height: 1.65` → `var(--wb-leading-relaxed)` (1.75) per UI-SPEC Exception 1.
       - `.content [data-section-divider]` — keep `display: none`.
       - `.content [data-cta-button]` — keep `margin: 16px 0` → `margin: var(--wb-space-4) 0`.
       - `.content [data-cta-button] a` — replace `min-height: 38px` → keep as-is (control height, not on spacing scale; matches Phase 2 Button md height of 40 — accept 2px shift) OR set to `min-height: var(--wb-space-10)` if such a token exists; if not, leave 38px as a control-height literal (allowed per UI-SPEC §"Spacing Scale" Exception 5). **Lock:** keep `min-height: 38px` literal — control heights from Phase 2 components, not the page CSS, are explicitly allowed under Exception 5; the inline anchor here is server-rendered TipTap output, not a Phase 2 Button. Replace `padding: 0 16px` → `0 var(--wb-space-4)`; replace `border-radius: 8px` → `var(--wb-radius-2)`; replace `background: var(--wb-primary)` (already tokenized — verify); replace `color: #fff` → `var(--wb-surface)`; replace `font-weight: 500` → `var(--wb-weight-medium)`.
       - `.state` — keep `min-height: 100vh; display: grid; place-items: center`; replace `color: var(--wb-text-muted)` (verify).

    3. **`@media (max-width: 760px)` block.** Keep all rules; replace `padding: 24px` → `var(--wb-space-6)` inside the mobile `.pageBlock` rule. The grid → 1fr toggles stay literal (`1fr` is not a px scale).

    4. **Add a NEW class `.fieldNative` (or reuse `.field`) for the native `<textarea>` styling** so the comment + approval-comment textareas match the Phase 2 `<Input>` look:
       ```css
       .field textarea {
         width: 100%;
         border: 1px solid var(--wb-border);
         border-radius: var(--wb-radius-2);
         padding: var(--wb-space-2) var(--wb-space-3);
         background: var(--wb-surface);
         color: var(--wb-text);
         font: inherit;
         line-height: var(--wb-leading-normal);
         resize: vertical;
       }
       .field textarea:focus {
         outline: none;
         border-color: var(--wb-border-strong);
         box-shadow: 0 0 0 3px var(--wb-color-primary-200);
       }
       ```
       This keeps the existing `<label className={styles.field}>...<textarea/></label>` JSX wiring intact while matching Phase 2 Input visuals (per UI-SPEC §"Pages × Components Coverage Matrix" textarea note).

    5. **ADD `.printHide` class** for Task 1's wrapper around the Exportar PDF Button:
       ```css
       .printHide { /* default: visible */ }
       ```
       (No rules in default scope — visibility is the default; the `@media print` block adds `display: none`.)

    6. **`@media print` block (lines 233–250 currently).** UPDATE the selectors for the new structure but KEEP px literals (browser-compat per UI-SPEC §"Print/PDF Protection"):
       ```css
       @media print {
         .identityCard,
         .feedbackPanel,
         .printHide {
           display: none;
         }

         .page {
           padding: 0;
           background: #fff;
         }

         .pageBlock {
           border: none;
           box-shadow: none;
           page-break-after: always;
         }
       }
       ```
       The change from `.secondaryButton` → `.printHide` is the critical update: `<Button>` from Phase 2 doesn't expose `.secondaryButton` as a CSS class on the rendered DOM, so the old print rule would become dead. The `.printHide` wrapper from Task 1 is the cross-browser-safe replacement.

    7. After all edits, the file MUST satisfy `05-UI-SPEC.md` §"Migration Strategy" success criteria 1–4 for SharePage.module.css.
  </action>
  <verify>
    <automated>
      ! grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/pages/SharePage.module.css | grep -v "@media print" | grep -v "background: #fff" &&
      ! grep -E "^\\.(primaryButton|secondaryButton|dangerButton|linkButton)\\b" frontend/src/pages/SharePage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/SharePage.module.css &&
      grep -E "\\.printHide" frontend/src/pages/SharePage.module.css &&
      grep -E "@media print" frontend/src/pages/SharePage.module.css &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/SharePage.module.css` returns hits ONLY inside the `@media print` block (the `background: #fff` literal is justified per UI-SPEC §"Print/PDF Protection")
    - `grep -E "^\\.(primaryButton|secondaryButton|dangerButton|linkButton)\\b" frontend/src/pages/SharePage.module.css` returns zero hits (Phase 2 owns these)
    - `grep -E "z-index:\\s*[0-9]+" frontend/src/pages/SharePage.module.css` returns zero hits (the file has no z-index today; preserve)
    - `grep -E "\\.printHide" frontend/src/pages/SharePage.module.css` returns at least 2 hits (default class + inside `@media print` rule)
    - `grep -E "@media print" frontend/src/pages/SharePage.module.css` returns exactly 1 hit
    - `grep -E "\\.identityCard," frontend/src/pages/SharePage.module.css` (inside `@media print`) is present — print rule still hides the identity gate
    - `grep -E "\\.feedbackPanel," frontend/src/pages/SharePage.module.css` (inside `@media print`) is present — print rule still hides the feedback panel
    - `grep -E "page-break-after:\\s*always" frontend/src/pages/SharePage.module.css` returns 1 hit (preserved in `@media print`)
    - `cd frontend && npm run build` exits 0
    - Manual smoke (recorded in SUMMARY): open `/share/:token`, identify, comment, approve, request changes, click Exportar PDF — print preview hides identity + feedback + Exportar PDF, every page-block starts on a new page
  </acceptance_criteria>
  <done>SharePage.module.css is layout + tokens only; .printHide wraps the Exportar PDF Button; @media print hides identity + feedback + .printHide; per-cohort gates 1–4 pass for SharePage's CSS file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public viewer (unauthenticated) → WeBrief backend | Anyone with a share token can hit `/api/public/share/:token` and the comments/approvals POSTs. Migration changes only JSX/CSS — backend boundary is unchanged. |
| Browser → `dangerouslySetInnerHTML` | TipTap-rendered HTML for `page.contentHtml` is server-sanitized today (existing behavior); migration does not modify the dangerous-HTML path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | SharePage POSTs (`/comments`, `/approvals`) | accept | Migration preserves the existing fetch payload shape (authorName, authorEmail, body / reviewerName, reviewerEmail, status, comment) byte-for-byte. No new attack surface introduced. |
| T-05-02 | Information disclosure | `share-viewer-${token}` localStorage entry | accept | Existing behavior preserved — name + email already stored client-side per UI-SPEC §"Auth posture". No new persistence introduced. |
| T-05-03 | Spoofing | Public token route | accept | Token validation lives in the backend (`/api/public/share/:token`); refactor cannot change auth posture. CONTEXT.md decisions lock "no Supabase Auth introduced" for public flows. |
</threat_model>

<verification>
SharePage migration gates (per `05-UI-SPEC.md` §"Migration Strategy" success criteria 1–4, restricted to this plan's two files):

```bash
# Gate 1: zero hardcoded #hex outside @media print
grep -nE "#[0-9a-fA-F]{3,8}" frontend/src/pages/SharePage.module.css | grep -v "@media print" | grep -v "background: #fff"   # expect: zero

# Gate 2: zero inline styles in SharePage.jsx
grep -E "style=\\{" frontend/src/pages/SharePage.jsx   # expect: zero

# Gate 3: zero local Button/Input selectors that Phase 2 owns
grep -E "^\\.(primaryButton|secondaryButton|dangerButton|linkButton)\\b" frontend/src/pages/SharePage.module.css   # expect: zero

# Gate 4: print block intact and updated
grep -A 10 "@media print" frontend/src/pages/SharePage.module.css | grep -E "\\.identityCard|\\.feedbackPanel|\\.printHide|page-break-after"   # expect: 4 hits

# Gate 5: build green
cd frontend && npm run build
```

Manual QA (per `05-UI-SPEC.md` §"Per-page QA" SharePage row):
- Open `/share/:token` (valid token) → loading → renders document
- Identity gate appears for unidentified viewer; submit name+email → identity dismissed
- Submit comment → feedback "Comentario enviado."
- Click Aprobar → feedback "Aprobación registrada."
- Click Pedir cambios → feedback "Pedido de cambios registrado."
- Click Exportar PDF → browser print preview opens with identity/feedback hidden, Exportar PDF wrapper hidden, pageBlocks page-break correctly
- Click "Cambiar datos" → identity gate returns
- Open with invalid token → error state shows the server message OR `No se pudo abrir el brief`

Console-cleanliness gate: if `mcp__Claude_Preview__preview_console_logs` is available, zero new errors/warnings introduced; pre-existing tolerated warnings recorded in SUMMARY.
</verification>

<success_criteria>
- SharePage.jsx imports from `../components/ui` and consumes Button + Input + Card; native `<textarea>` preserved with tokenized CSS (no Phase 2 textarea primitive yet)
- SharePage.module.css is layout + tokens; .primaryButton / .secondaryButton / .dangerButton / .linkButton selectors deleted; .printHide added
- @media print block updated: identityCard + feedbackPanel + .printHide hidden; pageBlock page-break-after preserved
- All five gates above pass
- Atomic commit: `refactor(05): migrate SharePage to shared UI primitives + tokens`
- UI-08 partially satisfied (SharePage half; BriefPage covered by Plan 05-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-pages-verification/05-01-SUMMARY.md` recording:
- Gate results (grep outputs with counts)
- Selectors deleted from SharePage.module.css (full list)
- Visual deltas applied (eyebrow 13→12, h1 32→30, h2 22→20, padding 36/44 → 32/48, page padding bottom 72→48, content line-height 1.65→1.75)
- Print/PDF smoke test result (identity hidden ✓, feedback hidden ✓, Exportar PDF hidden ✓, page-break ✓, white bg ✓, console clean ✓)
- Any pre-existing warnings tolerated
- Screenshots before/after (if preview tools available)
</output>
