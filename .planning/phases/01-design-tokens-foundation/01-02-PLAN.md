---
phase: 01-design-tokens-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/styles/tokens.css
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "tokens.css declares the spacing, typography, shadow, radius and z-index scales from .planning/intel/decisions.md exactly"
    - "Legacy shadow tokens (--wb-shadow-sm, --wb-shadow-lg) and radius tokens (--wb-radius-sm/md/lg) keep their current computed values byte-for-byte"
    - "tokens.css contains zero hardcoded numeric or color literals outside token declarations and inline comments"
  artifacts:
    - path: "frontend/src/styles/tokens.css"
      provides: "Spacing (10), Typography (8 size + line-height pairs), Shadow (5), Radius (6), Z-index (8) scales plus preserved legacy aliases"
      contains: "--wb-z-toast"
  key_links:
    - from: "frontend/src/styles/tokens.css"
      to: "main.jsx import chain"
      via: "::root cascade — already imported by frontend/src/main.jsx"
      pattern: "import.*tokens.css"
---

<objective>
Append every non-color scale required by UI-01 to `frontend/src/styles/tokens.css`: spacing (4-96 px, 10 levels), typography (8 sizes with line-heights, modular ratio 1.25), shadows (xs-xl, 5 levels), radius (xs-full, 6 levels), and z-index (8 semantic tokens). Preserve `--wb-shadow-sm`, `--wb-shadow-lg`, `--wb-radius-sm`, `--wb-radius-md`, `--wb-radius-lg`, and `--wb-content-width` exactly as they exist today.

Purpose: This is the second half of the design-token foundation. Pairs with Plan 01 (color) to satisfy UI-01 in full so Phase 2 can build shared components against a stable token system.

Output: A `tokens.css` that, in order, declares: color palette + legacy color aliases (Plan 01) → spacing → typography → shadows → radius → z-index → preserved misc (`--wb-content-width`, etc.).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/decisions.md
@.planning/phases/01-design-tokens-foundation/01-CONTEXT.md
@.planning/phases/01-design-tokens-foundation/01-01-PLAN.md
@frontend/src/styles/tokens.css

<interfaces>
<!-- After Plan 01, these legacy tokens still need to resolve to their original literals: -->

```css
--wb-shadow-sm: 0 1px 3px rgba(15, 23, 42, 0.08);
--wb-shadow-lg: 0 24px 48px rgba(15, 23, 42, 0.18);
--wb-radius-sm: 10px;   /* was 10, decisions.md proposes 8 — keep 10 here as alias */
--wb-radius-md: 14px;   /* was 14, decisions.md proposes 12 — keep 14 here */
--wb-radius-lg: 18px;   /* was 18, decisions.md proposes 16 — keep 18 here */
--wb-content-width: 1180px;
```

**Invariant**: per decisions.md "Compatibility" note ("mantener aliases ... existentes como fallbacks") and CONTEXT.md specifics #2 ("preservar valores viejos exactos en alias names viejos para CERO regresión, y agregar nuevos tokens con nombres nuevos"), the new canonical tokens use new names (`--wb-radius-xs/.../--wb-radius-xl/--wb-radius-full`, `--wb-shadow-xs/md/xl`); the existing names stay frozen at their current values. Plans 03-05 of this milestone migrate consumers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append spacing, typography, shadow, radius and z-index scales to tokens.css</name>
  <files>frontend/src/styles/tokens.css</files>
  <action>
Edit `frontend/src/styles/tokens.css`. **Append, inside the existing `:root` block, AFTER the legacy color aliases produced by Plan 01 and BEFORE `--wb-content-width`**, the following scale declarations exactly (values from `.planning/intel/decisions.md`):

```css

  /* ============================================================
     Spacing scale (linear 4-96 px, 10 levels)
     Decisions: .planning/intel/decisions.md (Spacing scale)
     ============================================================ */
  --wb-space-1:  4px;
  --wb-space-2:  8px;
  --wb-space-3:  12px;
  --wb-space-4:  16px;
  --wb-space-5:  20px;
  --wb-space-6:  24px;
  --wb-space-8:  32px;
  --wb-space-12: 48px;
  --wb-space-16: 64px;
  --wb-space-24: 96px;

  /* ============================================================
     Typography scale (modular ratio 1.25, 8 levels)
     Each size is paired with a line-height token.
     Decisions: .planning/intel/decisions.md (Typography scale)
     ============================================================ */
  --wb-text-xs:    12px;
  --wb-text-sm:    14px;
  --wb-text-base:  16px;
  --wb-text-lg:    18px;
  --wb-text-xl:    20px;
  --wb-text-2xl:   24px;
  --wb-text-3xl:   30px;
  --wb-text-4xl:   36px;

  --wb-leading-xs:    1.5;
  --wb-leading-sm:    1.5;
  --wb-leading-base:  1.5;
  --wb-leading-lg:    1.5;
  --wb-leading-xl:    1.4;
  --wb-leading-2xl:   1.3;
  --wb-leading-3xl:   1.2;
  --wb-leading-4xl:   1.1;

  /* Typographic role tokens (semantic line-heights from decisions.md) */
  --wb-leading-tight:   1.1;
  --wb-leading-normal:  1.5;
  --wb-leading-relaxed: 1.75;

  /* Font weights (decisions.md: 400 / 500 / 600 / 700, no <400 for body) */
  --wb-weight-regular:  400;
  --wb-weight-medium:   500;
  --wb-weight-semibold: 600;
  --wb-weight-bold:     700;

  /* Font stack (no webfonts) */
  --wb-font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

  /* ============================================================
     Shadow scale (5 levels)
     Decisions: .planning/intel/decisions.md (Shadows)
     - xs/md are new
     - sm/lg/xl preserve current computed values
     ============================================================ */
  --wb-shadow-xs: 0 1px 2px rgba(15, 23, 42, 0.05);
  /* --wb-shadow-sm preserved as legacy below */
  --wb-shadow-md: 0 4px 6px rgba(15, 23, 42, 0.10);
  /* --wb-shadow-lg preserved as legacy below */
  --wb-shadow-xl: 0 24px 48px rgba(15, 23, 42, 0.18);

  /* ============================================================
     Radius scale (6 levels)
     New canonical tokens. Legacy --wb-radius-sm/md/lg keep their
     current computed values (10/14/18 px) below.
     Decisions: .planning/intel/decisions.md (Radius)
     ============================================================ */
  --wb-radius-xs:   4px;
  --wb-radius-2:    8px;   /* canonical "sm" replacement; legacy --wb-radius-sm stays at 10px */
  --wb-radius-3:    12px;  /* canonical "md" replacement; legacy --wb-radius-md stays at 14px */
  --wb-radius-4:    16px;  /* canonical "lg" replacement; legacy --wb-radius-lg stays at 18px */
  --wb-radius-xl:   24px;
  --wb-radius-full: 9999px;

  /* ============================================================
     Z-index scale (semantic, replaces arbitrary 20-9999 values)
     Decisions: .planning/intel/decisions.md (Z-index scale)
     ============================================================ */
  --wb-z-base:     1;
  --wb-z-dropdown: 100;
  --wb-z-sticky:   200;
  --wb-z-overlay:  900;
  --wb-z-modal:    1000;
  --wb-z-popover:  1100;
  --wb-z-tooltip:  1200;
  --wb-z-toast:    1300;
```

Critical placement rules:
- Insertion is INSIDE `:root { ... }`.
- `--wb-shadow-sm`, `--wb-shadow-lg`, `--wb-radius-sm`, `--wb-radius-md`, `--wb-radius-lg`, `--wb-content-width` lines are NOT touched. Place the new scales above them (so the legacy block stays as the closing trailer of `:root`).
- Use 2-space indentation to match the existing file.
- Comments are allowed; do not add prose outside comments.

Naming choices (compatibility footnote, must end up in SUMMARY):
- `--wb-radius-2/3/4` are the canonical replacements for the conceptual "sm/md/lg" of decisions.md (8/12/16 px). The legacy `--wb-radius-sm/md/lg` stay frozen at 10/14/18 px to guarantee zero regression in existing pages, exactly as CONTEXT.md specifics #2 prescribes. Phase 3-5 migrate consumers and may then reclaim the friendly names.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node -e "const fs=require('fs');const css=fs.readFileSync('src/styles/tokens.css','utf8');const need=['--wb-space-1: 4px','--wb-space-24: 96px','--wb-text-xs:    12px','--wb-text-4xl:   36px','--wb-leading-tight:   1.1','--wb-leading-relaxed: 1.75','--wb-weight-bold:     700','--wb-font-sans:','--wb-shadow-xs:','--wb-shadow-md:','--wb-shadow-xl:','--wb-radius-xs:   4px','--wb-radius-2:    8px','--wb-radius-3:    12px','--wb-radius-4:    16px','--wb-radius-xl:   24px','--wb-radius-full:','--wb-z-base:     1','--wb-z-dropdown: 100','--wb-z-sticky:   200','--wb-z-overlay:  900','--wb-z-modal:    1000','--wb-z-popover:  1100','--wb-z-tooltip:  1200','--wb-z-toast:    1300'];const missing=need.filter(n=>!css.includes(n));if(missing.length){console.error('Missing scale tokens:\n'+missing.join('\n'));process.exit(1);}console.log('OK all scale tokens present');"</automated>
  </verify>
  <done>Spacing (10), typography (8 sizes + 8 leading + 3 semantic leading + 4 weights + 1 font stack), shadow (5), radius (6), z-index (8) scales all declared with the exact values from `decisions.md`. The verify command exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Verify legacy shadow/radius/content-width tokens still resolve to original values</name>
  <files>frontend/src/styles/tokens.css</files>
  <action>
This is a verification task — no new edits expected. Read the file end-to-end and confirm:

1. The closing trailer of `:root { ... }` still contains, **verbatim**:

```css
  --wb-shadow-sm: 0 1px 3px rgba(15, 23, 42, 0.08);
  --wb-shadow-lg: 0 24px 48px rgba(15, 23, 42, 0.18);
  --wb-radius-sm: 10px;
  --wb-radius-md: 14px;
  --wb-radius-lg: 18px;
  --wb-content-width: 1180px;
```

(Order between these six lines doesn't matter; their literals do.)

2. There are no other declarations of those six token names earlier in the file (no shadowing).

3. There are zero raw color hex literals outside `--wb-color-*` declarations and explanatory comments. Bare numeric literals (e.g., `4px`, `1.5`, `100`) are allowed only as the right-hand side of a `--wb-*` declaration.

If any of those checks fails, fix the file (re-add the missing line, remove any duplicate declaration) so they all hold.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node -e "const fs=require('fs');const css=fs.readFileSync('src/styles/tokens.css','utf8');const expects={'--wb-shadow-sm':'0 1px 3px rgba(15, 23, 42, 0.08)','--wb-shadow-lg':'0 24px 48px rgba(15, 23, 42, 0.18)','--wb-radius-sm':'10px','--wb-radius-md':'14px','--wb-radius-lg':'18px','--wb-content-width':'1180px'};const fail=[];for(const[t,exp]of Object.entries(expects)){const re=new RegExp('^\\s*'+t.replace(/[-]/g,'\\\\-')+':\\s*([^;]+);','gm');const matches=[...css.matchAll(re)];if(matches.length!==1){fail.push(t+': expected exactly 1 declaration, found '+matches.length);continue;}const got=matches[0][1].trim();if(got!==exp)fail.push(t+': expected \"'+exp+'\" got \"'+got+'\"');}if(fail.length){console.error('Legacy scale invariants broken:\n'+fail.join('\n'));process.exit(1);}console.log('OK 6 legacy scale tokens preserved');"</automated>
  </verify>
  <done>The six legacy scale tokens still resolve to their original literal values and appear exactly once in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser | tokens.css is shipped as a static stylesheet; no runtime evaluation, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | tokens.css | accept | Static asset; integrity guaranteed by build pipeline |
| T-01-04 | Denial of service | z-index tokens | accept | Bounded constants (max 1300); no client-supplied values |

Phase 1 introduces no new trust boundaries.
</threat_model>

<verification>
- Task 1 + Task 2 automated verifies must pass.
- `grep -E '^\s*--wb-' frontend/src/styles/tokens.css | wc -l` should report ≥ 80 token declarations (45 colors + 12 legacy color aliases + 10 spacing + 8 sizes + 8 leading + 3 semantic leading + 4 weights + 1 font stack + 5 shadows + 6 radius + 8 z-index + 1 content-width = 111 lines, give or take comments).
- After this plan, the dev server should still start clean (Plan 03 confirms).
</verification>

<success_criteria>
- All scales from `.planning/intel/decisions.md` declared in `tokens.css` with the exact values in this plan (Task 1 verify passes).
- Six legacy scale tokens (`--wb-shadow-sm/lg`, `--wb-radius-sm/md/lg`, `--wb-content-width`) keep their original literal values and appear exactly once each (Task 2 verify passes).
- No file outside `frontend/src/styles/tokens.css` is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-tokens-foundation/01-02-SUMMARY.md` documenting: scales added, the `--wb-radius-2/3/4` naming compromise (vs decisions.md's `sm/md/lg`), and confirmation that legacy values are preserved.
</output>
