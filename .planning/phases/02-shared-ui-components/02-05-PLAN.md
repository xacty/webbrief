---
phase: 02-shared-ui-components
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - frontend/src/components/ui/index.js
autonomous: true
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - "frontend/src/components/ui/index.js re-exports all 6 components: Button, Input, Select, Modal, Card, Badge"
    - "index.js does NOT export the cn helper (UI-SPEC §Public Surface: cn is internal-only)"
    - "Every export is named via `export { default as <Name> } from './<Name>.jsx';` form (tree-shake friendly)"
    - "Importing { Button, Input, Select, Modal, Card, Badge } from the index returns six valid React component references"
    - "frontend/ vite build completes successfully — proves all six modules + index resolve and the components compile"
    - "No raw hex colors leak in any of the 6 *.module.css files (re-verified across the whole library)"
    - "No text-transform: uppercase leaks in any of the 6 *.module.css files (re-verified across the whole library)"
  artifacts:
    - frontend/src/components/ui/index.js
  key_links:
    - "Closes the public surface for Phase 3-5 page migrations"
    - "vite build acts as the integration smoke test for the entire Phase 2 library"
---

<objective>
Close Phase 2 by writing the public barrel `frontend/src/components/ui/index.js` that re-exports the six components, then run a `vite build` smoke test against the worktree's frontend to prove the whole library compiles, imports cleanly, and produces no Vite errors. This is the only Wave-3 plan — it depends on Plans 02-02 / 02-03 / 02-04 completing in Wave 2.

Purpose: UI-SPEC §"Public Surface" mandates `frontend/src/components/ui/index.js` re-exporting all 6 components without exposing `cn`. UI-03 explicitly lists "Exportados desde `components/ui/index.js`" as a requirement bullet. The build smoke is the integration gate: any prop-name typo, any missing import, any malformed CSS module surfaces here instead of in Phase 3 page migrations.

Output: One file (`index.js`) plus a verified `vite build` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-ui-components/02-CONTEXT.md
@.planning/phases/02-shared-ui-components/02-UI-SPEC.md
@frontend/package.json
@frontend/src/components/ui/cn.js
@frontend/src/components/ui/Button.jsx
@frontend/src/components/ui/Input.jsx
@frontend/src/components/ui/Select.jsx
@frontend/src/components/ui/Modal.jsx
@frontend/src/components/ui/Card.jsx
@frontend/src/components/ui/Badge.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.js barrel re-exporting all 6 components</name>
  <files>frontend/src/components/ui/index.js</files>
  <read_first>
    .planning/phases/02-shared-ui-components/02-UI-SPEC.md,
    frontend/src/components/ui/Button.jsx,
    frontend/src/components/ui/Input.jsx,
    frontend/src/components/ui/Select.jsx,
    frontend/src/components/ui/Modal.jsx,
    frontend/src/components/ui/Card.jsx,
    frontend/src/components/ui/Badge.jsx
  </read_first>
  <action>
Create `frontend/src/components/ui/index.js` with the EXACT contents below (preserve every line, including the leading comment and trailing newline):

```js
/**
 * Public surface for components/ui/.
 *
 * Re-exports the 6 shared primitives delivered in Phase 2 (UI-03).
 * The `cn` helper at ./cn.js is intentionally NOT re-exported — it is
 * internal-only per .planning/phases/02-shared-ui-components/02-UI-SPEC.md
 * (§"File Layout" + §"Public Surface").
 *
 * Consumers import from this barrel:
 *   import { Button, Modal, Input } from '../../components/ui';
 *
 * Tree-shake friendly: every component is its own file, every export is
 * a named re-export with no side effects in this index.
 */

export { default as Button } from './Button.jsx';
export { default as Input } from './Input.jsx';
export { default as Select } from './Select.jsx';
export { default as Modal } from './Modal.jsx';
export { default as Card } from './Card.jsx';
export { default as Badge } from './Badge.jsx';
```

Constraints:
- Use the EXACT 6-export form shown — UI-SPEC §"Public Surface" pins the order and naming. Do not invent groupings.
- Do NOT export `cn` — UI-SPEC §"File Layout" explicitly says it is "NOT exported from `index.js` — internal-only".
- ESM only (`export { default as X } from './X.jsx';`). No CJS, no namespace re-exports.
- The `.jsx` extension on every import is required because the project's vite config does not configure default extensions for source modules; matching the explicit-extension imports each component uses internally is consistent.
- Do NOT add side-effect imports (e.g., `import './something.css';`) here — keeping the index pure preserves tree-shaking for any consumer that only uses Button.
  </action>
  <verify>
test -f frontend/src/components/ui/index.js &&
grep -c "^export { default as " frontend/src/components/ui/index.js | grep -q '^6$' &&
grep -q "^export { default as Button } from './Button.jsx';" frontend/src/components/ui/index.js &&
grep -q "^export { default as Input } from './Input.jsx';" frontend/src/components/ui/index.js &&
grep -q "^export { default as Select } from './Select.jsx';" frontend/src/components/ui/index.js &&
grep -q "^export { default as Modal } from './Modal.jsx';" frontend/src/components/ui/index.js &&
grep -q "^export { default as Card } from './Card.jsx';" frontend/src/components/ui/index.js &&
grep -q "^export { default as Badge } from './Badge.jsx';" frontend/src/components/ui/index.js &&
! grep -q "cn" frontend/src/components/ui/index.js
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/index.js exists
    - Exactly 6 lines starting with `export { default as `
    - All 6 component names present in the canonical UI-SPEC order: Button, Input, Select, Modal, Card, Badge
    - Every re-export uses the `.jsx` extension on the source path
    - The string `cn` does NOT appear anywhere in index.js (cn is internal-only)
    - No side-effect imports (no bare `import './x';` lines) — verify with `! grep -q "^import " frontend/src/components/ui/index.js`
  </acceptance_criteria>
  <done>index.js written with exactly six named re-exports; verify command above exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run vite build smoke test + cross-library lint sweep</name>
  <files>frontend/dist/ (build output, not committed)</files>
  <read_first>
    frontend/package.json,
    frontend/src/components/ui/index.js
  </read_first>
  <action>
Run a full `vite build` against the frontend to confirm every component module + the new index resolve, every CSS module compiles, and the component tree type-checks at the React level. This is the integration gate — it catches typos in `cn` imports, missing default exports, malformed CSS modules, and any stray reference to a non-existent token before Phase 3 page migrations land.

Commands (run from the worktree root, NOT from main repo per AI_GLOBAL.md worktree note — the executor should `cd` to the worktree path the plan-phase orchestrator was invoked in):

```bash
cd frontend && npm run build
```

Expected outcome:
- Exit code 0.
- Output contains no `error` lines from Vite, no Rollup warnings about missing exports.
- `frontend/dist/` is regenerated; do NOT commit it (it is git-ignored already).

Cross-library lint sweep (run from `frontend/src/components/ui/`):
1. `grep -rEn '#[0-9a-fA-F]{3,8}' . | grep -vE 'cn\.js|index\.js' | grep -v '#ffffff' | grep -v 'rgba(15, 23, 42, 0.36)'` MUST return empty. Allowed exceptions: `#ffffff` (Button-primary/danger text per UI-SPEC) and the Modal overlay literal `rgba(15, 23, 42, 0.36)`.
2. `grep -rEn 'text-transform: *uppercase' .` MUST return empty.
3. `grep -rEn '^\s*z-index: *[0-9]+' Modal.module.css` MUST return empty (overlay must use `var(--wb-z-modal)` only).
4. `grep -rEn 'wb-select-(chevron|padding|inset|size)' Select.module.css Input.module.css` MUST return empty (those vars belong to base.css).

If any of the four greps returns matches, FAIL the task and ask the executor to fix the offending file(s) before the build retry.

Acceptance: build exits 0 AND all four cross-library greps return empty.
  </action>
  <verify>
cd frontend && npm run build 2>&1 | tail -40 &&
cd .. &&
test "$(grep -rEn '#[0-9a-fA-F]{3,8}' frontend/src/components/ui/ | grep -vE 'cn\.js|index\.js' | grep -v '#ffffff' | grep -v 'rgba(15,' | wc -l | tr -d ' ')" = "0" &&
test "$(grep -rEn 'text-transform: *uppercase' frontend/src/components/ui/ | wc -l | tr -d ' ')" = "0" &&
test "$(grep -rEn '^\s*z-index: *[0-9]+' frontend/src/components/ui/Modal.module.css | wc -l | tr -d ' ')" = "0" &&
test "$(grep -rEn 'wb-select-(chevron|padding|inset|size)' frontend/src/components/ui/Select.module.css frontend/src/components/ui/Input.module.css | wc -l | tr -d ' ')" = "0"
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits with status 0
    - Build output contains no `error` lines from Vite or Rollup
    - frontend/dist/index.html exists after the build
    - Cross-library raw-hex sweep returns 0 matches outside the allowed exceptions (#ffffff in Button, rgba(15, 23, 42, 0.36) in Modal)
    - Cross-library uppercase sweep returns 0 matches
    - Modal.module.css contains 0 numeric z-index values (only var(--wb-z-modal))
    - Input.module.css and Select.module.css contain 0 references to --wb-select-* vars
  </acceptance_criteria>
  <done>Vite build succeeds; cross-library greps all empty; Phase 2 library is integration-clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `frontend/src/components/ui/index.js` exists with exactly 6 named re-exports in UI-SPEC order
- [ ] `cn` is NOT re-exported (internal-only)
- [ ] `cd frontend && npm run build` exits 0
- [ ] Cross-library raw-hex / uppercase / z-index / wb-select-* greps all empty
- [ ] No new entries in `frontend/package.json` (zero new npm deps — UI-SPEC §Registry Safety contract)
- [ ] No edits to Phase 1 artifacts (`frontend/src/styles/tokens.css`, `frontend/src/styles/base.css`)
- [ ] No edits to existing pages (`frontend/src/pages/*.jsx`, `*.module.css`) — page migration is Phase 3-5
</verification>

<success_criteria>
- All 6 components compile via vite build.
- Public surface (`index.js`) matches UI-SPEC §"Public Surface" exactly.
- Cross-library token-purity gates pass — no leakage of raw hexes, uppercase styles, numeric z-indexes, or chevron variables.
- Phase 2 deliverable is ready for Phase 3 admin/auth migration to consume:
  `import { Button, Modal, Input, Select, Card, Badge } from '../../components/ui';`
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-ui-components/02-05-SUMMARY.md`. Include the build output's last 20 lines as evidence and a one-line per component summary (e.g., `Button: 4 variants × 3 sizes, ref-forwarded, loading state with Loader2`).
</output>
