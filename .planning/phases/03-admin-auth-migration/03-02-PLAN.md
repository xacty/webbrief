---
phase: 03-admin-auth-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/AccountSettingsPage.jsx
  - frontend/src/pages/AccountSettingsPage.module.css
  - frontend/src/pages/NewProject.jsx
  - frontend/src/pages/NewProject.module.css
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "AccountSettingsPage renders profile + password forms using shared <Card>, <Input>, <Button>; the change-password modal uses <Modal>"
    - "AccountSettingsPage's Phase 1 token adoption (already present) is preserved and EXTENDED — no regression on what was tokenized; remaining hardcoded values eliminated"
    - "NewProject renders the project-name <Input>, project-type / business-type / company <Select>s, and the project-type tag as <Badge>; the preview uses <Card>"
    - "NewProject company auto-prefer logic works (`watch`: prefer non-internal company when available; accept company from route query) — behavior unchanged"
    - "Both files contain zero local `.modalOverlay`/`.modalCard`/`.input`/`.primaryButton`/`.secondaryButton`/`.dangerButton` selectors and zero raw `#hex`/`z-index:<number>`"
  artifacts:
    - path: "frontend/src/pages/AccountSettingsPage.jsx"
      provides: "Account settings form using shared Button/Input/Card/Modal"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/AccountSettingsPage.module.css"
      provides: "Layout-only, fully tokenized CSS module"
    - path: "frontend/src/pages/NewProject.jsx"
      provides: "New-project form + preview using shared Button/Input/Select/Card/Badge"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/NewProject.module.css"
      provides: "Layout-only, fully tokenized CSS module"
  key_links:
    - from: "AccountSettingsPage.jsx"
      to: "Supabase Auth password update + profile update endpoint"
      via: "existing handlers — no behavioral change"
      pattern: "updateUser|updateProfile"
    - from: "NewProject.jsx"
      to: "POST /api/projects (template seeding by businessType)"
      via: "existing submit handler — no behavioral change"
      pattern: "fetch.*projects|createProject"
    - from: "NewProject.jsx"
      to: "preview re-render on businessType change"
      via: "existing memo/effect"
      pattern: "businessType"
---

<objective>
Cohort 2 (Standalone forms) — migrate AccountSettingsPage and NewProject to shared Phase 2 components and tokenize their CSS modules. AccountSettingsPage already has a Phase 1 token-adoption pilot — this plan COMPLETES that migration without regressing the partial work. NewProject has the full form + preview pattern that proves Select/Card/Badge composition outside an admin grid.

Purpose: Validates Input/Select/Modal/Card/Badge in account + onboarding flows before tackling the companies stack. Closes the second-largest CSS-duplication source (`.input` in NewProject + AccountSettings).
Output: Two standalone, fully-migrated pages with layout-only CSS modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/03-admin-auth-migration/03-CONTEXT.md
@.planning/phases/03-admin-auth-migration/03-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/index.js
@frontend/src/components/ui/Button.jsx
@frontend/src/components/ui/Input.jsx
@frontend/src/components/ui/Select.jsx
@frontend/src/components/ui/Modal.jsx
@frontend/src/components/ui/Card.jsx
@frontend/src/components/ui/Badge.jsx
@frontend/src/pages/AccountSettingsPage.jsx
@frontend/src/pages/AccountSettingsPage.module.css
@frontend/src/pages/NewProject.jsx
@frontend/src/pages/NewProject.module.css

<interfaces>
<!-- Shared UI public surface (from frontend/src/components/ui/index.js): -->
import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';

<!-- Modal props: open (required), onClose, title, ariaLabel, size ('sm' | 'md' | 'lg'), closeOnEscape (default true), closeOnBackdrop (default true), showCloseButton (default true), initialFocusRef, footer, ariaDescribedBy, children -->
<!-- Modal renders into a portal, locks body scroll with refcount (stacked-modal safe), traps focus, restores focus on close. -->
<!-- Select props: label, helperText, error, required, disabled, fullWidth (default true), id, placeholder, options ([{value, label, disabled?}]), children, name, value, defaultValue, onChange. options prop wins over children when both present. Select.Option ergonomic helper available for children form. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete AccountSettingsPage migration (extend pilot, swap to shared primitives, replace local modal)</name>
  <files>frontend/src/pages/AccountSettingsPage.jsx, frontend/src/pages/AccountSettingsPage.module.css</files>
  <read_first>
    - frontend/src/pages/AccountSettingsPage.jsx (current state — already partially adopts Phase 1 tokens; identify what is and isn't migrated)
    - frontend/src/pages/AccountSettingsPage.module.css (audit hardcoded values + selectors that survived the pilot)
    - frontend/src/components/ui/Modal.jsx (the Modal API replaces the local change-password overlay)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → AccountSettingsPage row in coverage matrix (uses Button primary/secondary/danger, Input name/email/current-pw/new-pw, Modal change-pw confirm, Card group cards)
  </read_first>
  <action>
    1. Imports — add `import { Button, Input, Modal, Card } from '../components/ui';` (Badge not used here per UI-SPEC matrix).

    2. Wrap each setting group ("Perfil", "Avatar", "Cambiar contraseña" trigger area) in `<Card padding="lg" shadow="sm" radius="lg" className={styles.groupCard}>`. KEEP `styles.groupCard` only if it adds layout-only flex/gap rules; otherwise remove.

    3. Replace EVERY `<input ... className={styles.input}>` with `<Input ... />`. Concrete swaps:
       - Name field → `<Input type="text" label="Nombre" required value={...} onChange={...} />`
       - Email field → `<Input type="email" label="Correo electrónico" required value={...} onChange={...} disabled={...} />` (preserve any "email is read-only when verified" rule)
       - Current password → `<Input type="password" label="Contraseña actual" required ... />`
       - New password → `<Input type="password" label="Contraseña nueva" required ... />`

    4. Replace EVERY local button with the Button primitive:
       - "Guardar cambios" submit per group → `<Button type="submit" variant="primary" size="md" loading={isSavingX}>Guardar cambios</Button>`
       - "Cancelar" → `<Button type="button" variant="secondary" size="md" onClick={...}>Cancelar</Button>`
       - "Cambiar contraseña" trigger → `<Button variant="secondary" size="md" onClick={() => setPwModalOpen(true)}>Cambiar contraseña</Button>`
       - Danger actions (e.g., "Eliminar cuenta" if present) → `<Button variant="danger" size="md" onClick={...}>...</Button>` (preserve confirmation flow)

    5. Replace the local change-password modal markup (any `<div className={styles.modalOverlay}>...<div className={styles.modalCard}>...`) with:
       ```jsx
       <Modal open={pwModalOpen} onClose={() => setPwModalOpen(false)} title="Cambiar contraseña" size="sm" footer={
         <>
           <Button variant="secondary" onClick={() => setPwModalOpen(false)}>Cancelar</Button>
           <Button variant="primary" type="submit" form="change-pw-form" loading={isSavingPw}>Confirmar</Button>
         </>
       }>
         <form id="change-pw-form" onSubmit={handleChangePassword}>
           {/* Inputs go here */}
         </form>
       </Modal>
       ```
       Preserve ALL existing handler logic (`handleChangePassword`, validation, toast on success/error, modal close on success).

    6. Avatar upload area: keep behavior intact. If it uses a custom file-input button, wrap with `<Button variant="secondary">` proxying click to a hidden `<input type="file">` (existing pattern).

    7. CSS module — `AccountSettingsPage.module.css`:
       - DELETE `.modalOverlay`, `.modalCard`, `.input`, `.primaryButton`, `.secondaryButton`, `.dangerButton` selectors and any `:hover`/`:focus`/`:disabled` derivatives.
       - Tokenize every remaining selector per the same rules as Plan 01 Task 2 (color/spacing/typography/radius/shadow tokens; no raw #hex; no raw z-index numerics).
       - Preserve `styles.groupCard` ONLY if it provides layout-only positioning that Card cannot give via props.

    Behavior preservation: avatar upload + preview, password validation rules, toast messages, save-button disabled-while-saving, "email read-only after verified" — all unchanged.
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|primaryButton|secondaryButton|dangerButton|modalOverlay|modalCard)\\}" frontend/src/pages/AccountSettingsPage.jsx &&
      ! grep -E "^\\.(modalOverlay|modalCard|input|primaryButton|secondaryButton|dangerButton)\\b" frontend/src/pages/AccountSettingsPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/AccountSettingsPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/AccountSettingsPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/AccountSettingsPage.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero `className={styles.(input|primaryButton|secondaryButton|dangerButton|modalOverlay|modalCard)}` references
    - CSS: zero `^\.(modalOverlay|modalCard|input|primaryButton|secondaryButton|dangerButton)\b` selectors
    - CSS: zero raw `#hex` colors and zero raw `z-index: <n>` numerics
    - JSX: imports from `../components/ui`
    - Build green: `cd frontend && npm run build` exits 0
    - Manual smoke (recorded in SUMMARY): edit name → save → toast; upload avatar → preview; change password → modal opens, submits, closes; avatar still uploads correctly
  </acceptance_criteria>
  <done>AccountSettingsPage fully migrated, pilot extended to completion, modal delegated to shared `<Modal>`.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate NewProject.jsx + tokenize NewProject.module.css</name>
  <files>frontend/src/pages/NewProject.jsx, frontend/src/pages/NewProject.module.css</files>
  <read_first>
    - frontend/src/pages/NewProject.jsx (current shape — preserve businessType→preview wiring, company-prefer logic, route-query company acceptance)
    - frontend/src/pages/NewProject.module.css (selector inventory)
    - frontend/src/components/ui/Select.jsx (options prop vs children — pick options for project type, business type, company list)
    - frontend/src/components/ui/Badge.jsx (project-type tag)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → NewProject row in coverage matrix
    - CONTEXT.min.md `target=new-project` invariants (business-type → preview linkage; prefer non-internal company; accept company from route query)
  </read_first>
  <action>
    1. Imports — add `import { Button, Input, Select, Card, Badge } from '../components/ui';`

    2. Project name field — replace `<input className={styles.input} ...>` with `<Input type="text" label="Nombre del proyecto" required value={name} onChange={...} placeholder="..." />` (preserve placeholder text).

    3. Project type selector (e.g., `Página Web` / `Artículo` / `FAQs`):
       - If currently a `<select>`, replace with `<Select label="Tipo de proyecto" required value={projectType} onChange={...} options={[{value:'website',label:'Página Web'},{value:'article',label:'Artículo'},{value:'faqs',label:'FAQs'}]} />` (use the EXACT existing values; do not invent labels).
       - If currently a tab/segmented control with hardcoded buttons, leave the structure but tokenize colors and replace any `.primaryButton` styling with Button primitive variants where it fits.
       - For the project-type indicator that shows the picked type elsewhere on the page (e.g., near the preview), render `<Badge variant="neutral" size="md">{labelForType}</Badge>`.

    4. Business type selector — replace with `<Select label="Tipo de negocio" required value={businessType} onChange={...} options={businessTypeOptions} />`. KEEP `businessTypeOptions` as the single source of truth (memoized list).

    5. Company selector — replace with `<Select label="Empresa" required value={selectedCompanyId} onChange={...} options={companyOptions} placeholder="Selecciona una empresa" />`. PRESERVE the `target=new-project` watch invariant: prefer non-internal companies and accept `?company=<id>` from the route query in the existing useEffect. Do NOT change the prefer/accept logic.

    6. Submit / cancel buttons:
       - `<Button type="submit" variant="primary" size="md" loading={isSubmitting}>Crear proyecto</Button>`
       - `<Button type="button" variant="ghost" size="md" onClick={handleCancel}>Cancelar</Button>` (preserve existing handler).

    7. Preview area — wrap the right-side preview block in `<Card padding="md" shadow="sm" radius="lg" className={styles.previewCard}>` (keep `styles.previewCard` only for layout-only sticky/scroll behavior if it exists today).

    8. CSS module — `NewProject.module.css`:
       - DELETE `.input`, `.select`, `.primaryButton` selectors and their states.
       - Tokenize every remaining selector. Preserve only layout-only classes (form grid, preview sticky positioning, decorative spacing).
       - Verify: zero `#hex`, zero raw `z-index:` numerics, zero deleted selector names.
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|select|primaryButton)\\}" frontend/src/pages/NewProject.jsx &&
      ! grep -E "^\\.(input|select|primaryButton)\\b" frontend/src/pages/NewProject.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/NewProject.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/NewProject.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/NewProject.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero `className={styles.(input|select|primaryButton)}` references
    - CSS: zero `^\.(input|select|primaryButton)\b` selectors
    - CSS: zero raw `#hex` and zero raw `z-index:` numerics
    - JSX: imports from `../components/ui`
    - Build green
    - Manual smoke (recorded in SUMMARY): pick `Página Web` / `Artículo` / `FAQs` → preview updates per existing logic; pick business type → preview updates; route `/new-project?company=<id>` preserves the company selection; submit → real backend create succeeds (non-internal company preferred when available)
  </acceptance_criteria>
  <done>NewProject fully migrated; CONTEXT.min.md `target=new-project` invariants preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WeBrief backend | AccountSettings password-change submits via Supabase Auth client; profile updates via `/api/auth/me` (or equivalent) |
| Browser → WeBrief backend | NewProject submits to `POST /api/projects` (creates project + seeds template pages by businessType) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | AccountSettings change-password modal | accept | Refactor preserves the existing Supabase password update path; `<Modal>` and `<Input>` do not log values; focus trap prevents accidental focus leak. |
| T-03-05 | Information disclosure | NewProject company selector | accept | Selector reads existing options provided by the page's loader; no new data exposure. The "prefer non-internal company" invariant is behavior-preserved. |
| T-03-06 | Elevation of privilege | NewProject submit | accept | Backend (`POST /api/projects`) is the security boundary; UI refactor does not change authorization. |
</threat_model>

<verification>
Cohort 2 gates (per `03-UI-SPEC.md` §"Migration Strategy" success criteria, restricted to this cohort's files):

```bash
# Gate 1: zero hardcoded colors
grep -E '#[0-9a-fA-F]{3,8}' frontend/src/pages/AccountSettingsPage.module.css frontend/src/pages/NewProject.module.css   # expect: zero

# Gate 2: zero raw z-index numerics
grep -E 'z-index:\s*[0-9]+' frontend/src/pages/AccountSettingsPage.module.css frontend/src/pages/NewProject.module.css   # expect: zero

# Gate 3: zero local .modalOverlay / .modalCard
grep -E '^\.(modalOverlay|modalCard)\b' frontend/src/pages/AccountSettingsPage.module.css frontend/src/pages/NewProject.module.css   # expect: zero

# Gate 4: zero local .input / .primaryButton / .secondaryButton / .dangerButton / .select
grep -E '^\.(input|select|primaryButton|secondaryButton|dangerButton)\b' frontend/src/pages/AccountSettingsPage.module.css frontend/src/pages/NewProject.module.css   # expect: zero

# Gate 5: build green
cd frontend && npm run build
```

Manual QA (per `03-UI-SPEC.md` §"Per-page QA"):
- AccountSettings: edit name → save → toast; upload avatar → preview; change password → modal opens, submits, closes
- NewProject: pick `Página Web`/`Artículo`/`FAQs`, business type, company → preview updates; submit → real backend create

Console-cleanliness gate: zero new warnings/errors.

Atomic-commit policy: ONE commit per page (two atomic commits in this cohort), so a single page rolling back via `git revert` does not touch the other.
</verification>

<success_criteria>
- AccountSettingsPage.jsx + .module.css fully migrated; existing pilot extended to completion
- NewProject.jsx + .module.css fully migrated; preview/business-type/company invariants preserved
- All five gates pass on both files
- Two atomic commits: `refactor(03): migrate AccountSettingsPage to shared UI` and `refactor(03): migrate NewProject to shared UI`
</success_criteria>

<output>
Create `.planning/phases/03-admin-auth-migration/03-02-SUMMARY.md` recording: gate results (with grep counts), screenshots before/after for each page, list of selectors deleted from each .module.css, any pre-existing console warnings tolerated, and explicit confirmation that the AccountSettingsPage Phase 1 pilot was extended (not regressed).
</output>
</content>
</invoke>