---
phase: 03-admin-auth-migration
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - frontend/src/components/layout/AppShell.jsx
  - frontend/src/components/layout/AppShell.module.css
autonomous: false
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "AppShell sidebar renders with token-driven colors; 248px width preserved as a local-scope CSS variable `--wb-shell-sidebar-width`"
    - "Sidebar nav items remain `<NavLink>` (react-router) — NOT wrapped in `<Button>` per the locked AppShell exception"
    - "Active-state styling reads `var(--wb-color-primary-900)` directly via the CSS module — no Button variant"
    - "AppShell sticky sidebar uses `var(--wb-z-sticky)` (= 200), modals from migrated pages use `var(--wb-z-modal)` (= 1000); modals render ABOVE sidebar (verified)"
    - "AppShell.module.css contains zero raw `#hex` and zero raw `z-index:<number>` numerics"
    - "Full golden-path QA passes after AppShell migration: login → companies → company → editor entry; admin → users; admin → security; archive/trash flows; logout returns to /login"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.jsx"
      provides: "Layout shell with sidebar + content area, role-based nav items"
      contains: "NavLink"
    - path: "frontend/src/components/layout/AppShell.module.css"
      provides: "Layout-only, fully tokenized; sidebar dark theme via tokens; 248px width as local CSS var"
  key_links:
    - from: "AppShell.jsx"
      to: "react-router NavLink active-route detection"
      via: "existing NavLink className callback (isActive ? styles.activeNavItem : styles.navItem)"
      pattern: "NavLink|isActive"
    - from: "AppShell.module.css"
      to: "tokens.css `--wb-color-primary-*` and `--wb-color-neutral-*`"
      via: "CSS variable references"
      pattern: "var\\(--wb-color-"
---

<objective>
Cohort 5 (Shell) — migrate AppShell LAST because it wraps every other page. Migrating it earlier would risk visual drift in already-migrated pages. After this plan, run a full golden-path QA across all admin + auth flows to confirm no cross-page regressions.

Per UI-SPEC AppShell exception (locked):
1. Sidebar nav items stay `<NavLink>`, NOT wrapped in `<Button>`
2. Active-state reads `var(--wb-color-primary-900)` directly via CSS module — no Button variant
3. 248 px width preserved as `--wb-shell-sidebar-width` local-scope variable inside `AppShell.module.css`
4. z-index 1000 (sticky sidebar) → `var(--wb-z-sticky)` — explicitly downshifted because sidebar is NOT a modal; verified that migrated modals (z=1000) render above

Purpose: Closes Phase 3 by tokenizing the layout chrome that visually frames every page.
Output: Tokenized sidebar + content area shell that does not regress any of the four prior cohorts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/03-admin-auth-migration/03-CONTEXT.md
@.planning/phases/03-admin-auth-migration/03-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/layout/AppShell.jsx
@frontend/src/components/layout/AppShell.module.css

<interfaces>
<!-- AppShell does NOT consume Phase 2 components per the locked exception. -->
<!-- It does consume Phase 1 tokens directly via the CSS module. -->

<!-- Tokens used in this plan (from frontend/src/styles/tokens.css): -->
<!-- - Sidebar surface: var(--wb-color-primary-900) (= #091223 — preserves the existing dark) -->
<!-- - Sidebar text default: light shade — pick var(--wb-color-neutral-200) or --wb-color-neutral-100 to match current; document in SUMMARY -->
<!-- - Sidebar text active: var(--wb-color-neutral-50) on a primary-700/800 background, OR a contrast-bumped shade — document choice in SUMMARY -->
<!-- - Content area background: var(--wb-bg) -->
<!-- - Spacing: var(--wb-space-*) for sidebar padding, item padding, gaps -->
<!-- - Typography: var(--wb-text-sm) for sidebar items, var(--wb-weight-medium)/semibold for active -->
<!-- - Z-index: var(--wb-z-sticky) (= 200) for sidebar, NOT --wb-z-modal -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize AppShell.module.css; preserve sidebar 248px as local CSS var; sticky z-index → token</name>
  <files>frontend/src/components/layout/AppShell.module.css</files>
  <read_first>
    - frontend/src/components/layout/AppShell.module.css (current full file — capture every hardcoded color, spacing, radius, shadow, z-index)
    - frontend/src/styles/tokens.css (target tokens; note legacy aliases preserved for back-compat)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → AppShell row of coverage matrix; AppShell exception (locked); §"Z-index Migration Map" AppShell row (z 1000 → `var(--wb-z-sticky)`)
  </read_first>
  <action>
    1. Define a local CSS variable at the top of the module for the sidebar width:
       ```css
       .shell {
         --wb-shell-sidebar-width: 248px;
         /* …rest of layout… */
       }
       ```
       Then everywhere the file currently references `248px` directly, replace with `var(--wb-shell-sidebar-width)`.

    2. Replace EVERY hardcoded color with a token. Concrete mappings (verify against current file):
       - Sidebar background `#091223` (or close) → `var(--wb-color-primary-900)`
       - Sidebar inactive text → `var(--wb-color-neutral-200)` or `--wb-color-neutral-100` (match current visual; if unsure pick `-200` and document)
       - Sidebar hover background → `var(--wb-color-primary-800)` or `--wb-color-primary-700` (match current visual; document choice)
       - Sidebar active background → `var(--wb-color-primary-700)` (or current shade)
       - Sidebar active text → `var(--wb-color-neutral-50)`
       - Content area background `#f8fafc` (or close) → `var(--wb-bg)` (alias of `--wb-color-neutral-50`)
       - Any border/separator → `var(--wb-border)` or `--wb-border-strong`

    3. Replace EVERY hardcoded spacing with a token (`var(--wb-space-*)`).
    4. Replace EVERY hardcoded radius with a token (`var(--wb-radius-*)` or legacy alias).
    5. Replace EVERY hardcoded shadow with a token (`var(--wb-shadow-*)`).
    6. Replace EVERY hardcoded font-size with a token (`var(--wb-text-*)`); replace `font-family` with `var(--wb-font-sans)`.

    7. **z-index: 1000 → `var(--wb-z-sticky)`** (= 200). This is an EXPLICIT downshift per UI-SPEC because the sidebar is sticky, NOT a modal. After the migration:
       - Sidebar at `--wb-z-sticky` (200)
       - Migrated modals at `--wb-z-modal` (1000)
       - Migrated modals MUST render above the sidebar — verify in QA Task 3.

    8. Apply the per-cohort gates to this file:
       - `grep -E '#[0-9a-fA-F]{3,8}' AppShell.module.css` → expect zero
       - `grep -E 'z-index:\s*[0-9]+' AppShell.module.css` → expect zero (only `var(--wb-z-*)` allowed)

    Do NOT delete sidebar nav-item classes (`.navItem`, `.navItemActive`, etc.) — they are the locked AppShell exception that lets `<NavLink>` style itself without going through `<Button>`.
  </action>
  <verify>
    <automated>
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/components/layout/AppShell.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/components/layout/AppShell.module.css &&
      grep -E "--wb-shell-sidebar-width:\\s*248px" frontend/src/components/layout/AppShell.module.css &&
      grep -E "var\\(--wb-z-sticky\\)" frontend/src/components/layout/AppShell.module.css &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#[0-9a-fA-F]{3,8}" AppShell.module.css` returns zero
    - `grep -E "z-index:\\s*[0-9]+" AppShell.module.css` returns zero
    - `AppShell.module.css` contains `--wb-shell-sidebar-width: 248px` as a local-scope variable
    - `AppShell.module.css` contains `var(--wb-z-sticky)` (sidebar uses sticky token, not modal)
    - Build green
    - Sidebar nav-item classes (`.navItem`, `.navItemActive` or equivalent) preserved (NavLink active styling still works)
  </acceptance_criteria>
  <done>AppShell.module.css fully tokenized with sidebar exception preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify AppShell.jsx imports/structure unchanged; minor tweaks if any visual drift</name>
  <files>frontend/src/components/layout/AppShell.jsx</files>
  <read_first>
    - frontend/src/components/layout/AppShell.jsx (verify NavLink usage, role-based nav item rendering, sidebar collapse rules if present)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → AppShell exception (NavLink stays NavLink)
    - CONTEXT.min.md (admin shell sidebar exposes `Empresas`, admin/manager `Usuarios`, admin-only `Seguridad`, `Archivados`, `Papelera`)
  </read_first>
  <action>
    Per the AppShell exception, AppShell.jsx generally does NOT need shared-component swaps — sidebar nav items remain `<NavLink>`. However:

    1. Verify the file does NOT add new hardcoded colors via inline styles. If any inline `style={{ color: '#...' }}` exists, replace with token reference (`style={{ color: 'var(--wb-color-...)' }}`).

    2. If the AppShell currently has any local `<button>` (e.g., a logout button, a sidebar toggle, a profile menu trigger), evaluate whether to swap to `<Button>`:
       - Logout button (if visible in shell, not on a separate page) → `<Button variant="ghost" size="sm">Cerrar sesión</Button>`
       - Sidebar collapse toggle (if exists) → `<Button variant="ghost" size="sm" icon={...} aria-label="Mostrar/ocultar barra lateral" />`
       - If no such buttons exist in JSX, no JSX changes are required.

    3. Add `import { Button } from '../ui';` ONLY if Task 2.2 actually swapped a `<button>`. Otherwise leave the imports unchanged.

    4. Preserve the role-based nav item visibility rules (admin/manager/user/qa) — do not change the conditional rendering logic. CONTEXT.min.md states: sidebar exposes `Empresas`, admin/manager `Usuarios`, admin-only `Seguridad`, `Archivados`, and `Papelera`.

    5. If the file has zero changes after applying 1-4, this task is a no-op — record that explicitly in the SUMMARY (Task 1 is sufficient).
  </action>
  <verify>
    <automated>
      ! grep -E "style=\\{\\{[^}]*#[0-9a-fA-F]{3,8}" frontend/src/components/layout/AppShell.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` for inline `style={{ ... #hex ... }}` returns zero in AppShell.jsx
    - If a `<Button>` was introduced, `import { Button } from '../ui'` (or `../components/ui` depending on path depth) is present
    - Role-based nav item visibility rules unchanged (verified via grep for the conditional render expressions)
    - Build green
  </acceptance_criteria>
  <done>AppShell.jsx free of inline hardcoded colors; sidebar role rules preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full golden-path QA across all migrated pages — verify no cross-page regression after AppShell migration</name>
  <files>frontend/src/components/layout/AppShell.module.css, frontend/src/components/layout/AppShell.jsx</files>
  <action>
    Run the manual QA suite below — Phase 3 is complete only when all 10 page flows pass with a clean console. AppShell.module.css fully tokenized + sidebar at `var(--wb-z-sticky)` is the only change since cohort 4; any visual or behavioral drift on migrated pages indicates a regression caused by AppShell.

    1. Run `cd frontend && npm run dev` and open the app in a browser.
    2. Per-page golden path (each MUST behave per CONTEXT.min.md `## Keep Stable` and the per-page QA list in `03-UI-SPEC.md`):
       - **Login** → Login with valid creds → redirect to `/companies`. "Olvidé mi contraseña" sends reset email. Eye toggle preserves caret.
       - **SetPassword** → Open invite link → form ready → submit → "Contraseña actualizada". Refresh expired link → "Enlace expirado" Badge visible.
       - **AccountSettingsPage** → `/account` → Edit name → save → toast. Upload avatar → preview. Change password → modal opens, submits, closes.
       - **NewProject** → Pick `Página Web`/`Artículo`/`FAQs`, business type, company → preview updates. Submit → real backend create.
       - **CompaniesPage** → Search, paginate, create company (manager email required), test-create (admin), archive, trash, restore (cross-link to Trash).
       - **CompanyPage** → Project list left, team sidecard right; invite member; archive company; restore from `/archive`.
       - **UsersPage** → Search, filter by active company, expand a row, edit profile, change role within company, invite user, admin self-delete blocked, last-manager downgrade blocked.
       - **TrashPage** → Toggle Empresas/Proyectos tabs in BOTH `/archive` and `/trash`; restore; permanent-delete with confirm (trash mode only).
       - **SecurityPage** → Switch tabs (Overview/Users/IPs/Events/Blocks); block IP with reason → audit row appears; revoke; auth-audit fallback warning when applicable.
       - **AppShell** → Active nav highlight per route. Logout returns to `/login`. Sidebar at 248 px on `>= md`. Modals from any page render ABOVE sidebar (no overlap or transparency issue).

    3. Run `mcp__Claude_Preview__preview_console_logs` (or open browser devtools) at the end of the run. Console MUST be clean of NEW errors/warnings introduced by Phase 3. Pre-existing warnings unrelated to the migration (e.g., known editor warnings, third-party deprecations) are tolerated and listed in SUMMARY.

    4. Modal-above-sidebar verification (specific): on CompaniesPage open the "Crear empresa" modal. The sidebar (z=200) MUST be visually behind the modal overlay (z=1000). On SecurityPage open the "Bloquear IP" modal — same expectation.

    5. Visual diff check: each migrated page should look pixel-equivalent to the baseline (per `03-UI-SPEC.md` §"Migration Strategy" success criterion 6: invariants from CONTEXT.min.md `## Keep Stable` preserved). Capture before/after screenshots for the SUMMARY.

    Resume signal: Type `approved` if all 10 page flows pass and console is clean. Otherwise list the failing flows / regressions and resume into a follow-up fix task.
  </action>
  <verify>
    Manual verification — record outcomes in `03-05-SUMMARY.md`:
    - All 10 page flows above pass (one line each: PASS / FAIL with notes)
    - Console clean (zero new warnings/errors; pre-existing list documented)
    - Modal-above-sidebar screenshot attached for CompaniesPage and SecurityPage
    - Sidebar visual look-and-feel matches baseline (before/after screenshots attached)
  </verify>
  <done>
    User types `approved`. All 10 flows PASS, console clean, modal-above-sidebar verified, sidebar visual unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → React app | AppShell renders role-based nav items (admin / manager / user / qa); the visibility check is client-side UX, not the security boundary |
| Browser → WeBrief backend | Each route enforced server-side; AppShell does not bypass any auth check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Elevation of privilege | AppShell role-based nav item visibility | accept | Refactor preserves the existing role-conditional rendering verbatim. Server remains authoritative for each `/admin/*` and `/security` route — hiding a sidebar link is UX, not a security boundary. |
| T-03-15 | Tampering | Sidebar z-index downshift to `var(--wb-z-sticky)` | mitigate | Modal-above-sidebar invariant explicitly verified in Task 3; if a future change accidentally raises sidebar z-index above modals, modals would be inert. Token-based ordering makes this a single-source change. |
</threat_model>

<verification>
Cohort 5 gates (per `03-UI-SPEC.md` §"Migration Strategy" success criteria, restricted to AppShell):

```bash
grep -E '#[0-9a-fA-F]{3,8}' frontend/src/components/layout/AppShell.module.css   # expect: zero
grep -E 'z-index:\s*[0-9]+' frontend/src/components/layout/AppShell.module.css  # expect: zero
grep -E "--wb-shell-sidebar-width" frontend/src/components/layout/AppShell.module.css   # expect: present (local-scope width var)
grep -E "var\\(--wb-z-sticky\\)" frontend/src/components/layout/AppShell.module.css   # expect: present (sidebar uses sticky token)

# Repo-wide gate (every in-scope file from cohorts 1-5):
grep -E '#[0-9a-fA-F]{3,8}' frontend/src/pages/{Companies,Company,Users,Security,Trash,NewProject,AccountSettings,AuthPage}*.module.css frontend/src/components/layout/AppShell.module.css   # expect: zero (baseline was 74)
grep -E 'z-index:\s*[0-9]+' frontend/src/pages/{Companies,Company,Users,Security,Trash,NewProject,AccountSettings,AuthPage}*.module.css frontend/src/components/layout/AppShell.module.css   # expect: zero (baseline was 5)
grep -E '^\.(modalOverlay|modalCard)\b' frontend/src/pages/{Companies,Company,Users,Security,Trash,NewProject,AccountSettings,AuthPage}*.module.css frontend/src/components/layout/AppShell.module.css   # expect: zero
grep -E '^\.(input|primaryButton|secondaryButton|dangerButton)\b' frontend/src/pages/{Companies,Company,Users,Security,Trash,NewProject,AccountSettings,AuthPage}*.module.css frontend/src/components/layout/AppShell.module.css   # expect: zero (UsersPage exception classes are NOT in this regex)

cd frontend && npm run build   # expect exit 0
```

Manual QA: Task 3 above is the gate.
Console-cleanliness gate: zero new warnings/errors across all 10 page flows.
</verification>

<success_criteria>
- AppShell.module.css fully tokenized; sidebar exception (NavLink + 248px local var + sticky z-index) preserved
- AppShell.jsx free of inline hardcoded colors; role-based nav rules preserved
- Repo-wide gates pass: zero `#hex` and zero raw `z-index:<number>` across the eight in-scope .module.css files plus AppShell.module.css
- Phase 3 success criteria from ROADMAP.md satisfied:
  1. CompaniesPage, CompanyPage, UsersPage, TrashPage, SecurityPage, NewProject, AccountSettingsPage usan shared components ✓
  2. Login y SetPassword usan shared components y `AuthPage.module.css` actualizado a tokens ✓
  3. Cero referencias a `.modalOverlay`/`.modalCard` definidas localmente en estas páginas (delegado a `Modal`) ✓
  4. Cero hardcoded colors (`#xxx`) en estos archivos — sólo tokens ✓
  5. Manual QA pasa: invitar usuario, crear empresa, archivar, restaurar, login, reset password — sin regresiones ✓ (Task 3)
- One atomic commit for this cohort: `refactor(03): migrate AppShell to tokens (final cohort)`
</success_criteria>

<output>
Create `.planning/phases/03-admin-auth-migration/03-05-SUMMARY.md` recording: gate results (per-file + repo-wide), screenshots before/after of every page touched in Phase 3 (sidebar visible in each), exact mapping of sidebar inactive/hover/active backgrounds and text shades to tokens, modal-above-sidebar verification screenshot, list of pre-existing console warnings tolerated, and confirmation that all 10 golden-path flows in Task 3 passed.
</output>
</content>
</invoke>