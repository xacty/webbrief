---
phase: 03-admin-auth-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - frontend/src/pages/CompaniesPage.jsx
  - frontend/src/pages/CompaniesPage.module.css
  - frontend/src/pages/CompanyPage.jsx
  - frontend/src/pages/CompanyPage.module.css
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "CompaniesPage renders the companies grid with each card as <Card>; primary `Abrir` CTA, search <Input>, type filter + page-size <Select>s, archive/trash/test-create <Modal>s, and per-card lifecycle <Button> icon-row are shared primitives"
    - "CompaniesPage admin-only test-create flow (`testMode=true`, `is_test=true`, `Test` <Badge>) preserved"
    - "CompanyPage renders without tabs: project cards left, team sidecard right; invite + archive + trash <Modal>s; member role <Badge> pills"
    - "Both pages preserve their sessionStorage stale-while-revalidate caches and lazy-loading boundaries"
    - "Both .module.css files contain zero local `.modalOverlay`/`.modalCard`/`.input`/`.primaryButton`/`.secondaryButton`/`.dangerButton` selectors and zero raw `#hex`/`z-index:<number>`"
  artifacts:
    - path: "frontend/src/pages/CompaniesPage.jsx"
      provides: "Companies home using shared Button/Input/Select/Modal/Card/Badge"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/CompaniesPage.module.css"
      provides: "Layout-only, fully tokenized — z-index 1000 → token (delegated to <Modal>)"
    - path: "frontend/src/pages/CompanyPage.jsx"
      provides: "Company detail using shared Button/Input/Select/Modal/Card/Badge"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/CompanyPage.module.css"
      provides: "Layout-only, fully tokenized — z-index 40 → token (delegated to <Modal>)"
  key_links:
    - from: "CompaniesPage.jsx"
      to: "GET /api/companies + sessionStorage cache"
      via: "existing loader + cache hydration"
      pattern: "sessionStorage|/api/companies"
    - from: "CompaniesPage.jsx"
      to: "POST /api/companies (manager email required; testMode=true bypasses)"
      via: "existing create handler — no flow change"
      pattern: "testMode|managerEmail"
    - from: "CompanyPage.jsx"
      to: "GET /api/companies/:id + member invite + archive/trash flows"
      via: "existing handlers"
      pattern: "invite-user|archive|trash"
---

<objective>
Cohort 3 (Companies stack) — migrate the core admin journey: CompaniesPage (companies grid + 4 modals) and CompanyPage (no-tabs detail + project cards + team sidecard + 3 modals). Validates Card+Modal+Badge composition at scale. CompaniesPage drives the largest hardcoded-color count in the cohort matrix and unifies the .modalOverlay z-index conflict (`1000` here vs `200` in editor) called out in `03-CONTEXT.md` §"Hot spots".

Purpose: Companies stack is the daily admin journey — proving the migration here de-risks the rest of the admin pages.
Output: Companies + Company pages fully migrated, sessionStorage cache and admin/test-mode preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/03-admin-auth-migration/03-CONTEXT.md
@.planning/phases/03-admin-auth-migration/03-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/index.js
@frontend/src/components/ui/Modal.jsx
@frontend/src/pages/CompaniesPage.jsx
@frontend/src/pages/CompaniesPage.module.css
@frontend/src/pages/CompanyPage.jsx
@frontend/src/pages/CompanyPage.module.css

<interfaces>
<!-- Same shared UI surface as Plans 01-02. -->

<!-- CompaniesPage modals to migrate (per UI-SPEC matrix): -->
<!-- 1. Create company (manager email REQUIRED unless testMode) -->
<!-- 2. Archive confirm -->
<!-- 3. Trash confirm -->
<!-- 4. Test-create (admin-only) -->

<!-- CompanyPage modals to migrate: -->
<!-- 1. Invite member (with role <Select>) -->
<!-- 2. Archive company -->
<!-- 3. Trash company -->

<!-- Modal usage pattern (from Plan 02): -->
<Modal open={modalOpen} onClose={closeModal} title="…" size="sm" footer={
  <>
    <Button variant="secondary" onClick={closeModal}>Cancelar</Button>
    <Button variant="primary" onClick={confirm} loading={isSubmitting}>Confirmar</Button>
  </>
}>
  {/* form/body */}
</Modal>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CompaniesPage.jsx + tokenize CompaniesPage.module.css (4 modals + grid + lifecycle icon row)</name>
  <files>frontend/src/pages/CompaniesPage.jsx, frontend/src/pages/CompaniesPage.module.css</files>
  <read_first>
    - frontend/src/pages/CompaniesPage.jsx (current shape — 4 modals, search, paginate, type filter, page-size, lifecycle icon-row per card, sessionStorage cache)
    - frontend/src/pages/CompaniesPage.module.css (selector inventory — confirms `z-index: 1000` for `.modalOverlay`)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → CompaniesPage row of coverage matrix; §"Z-index Migration Map" CompaniesPage row
    - CONTEXT.min.md `target=companies` (searchable/paginable, sidebar shell, company counts, primary `Abrir` CTA per card, compact lifecycle icon actions for admin, test-company mode for admin)
  </read_first>
  <action>
    1. Imports: `import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';`

    2. Each company card → `<Card padding="md" shadow="sm" radius="lg" interactive className={styles.companyCard}>`. Inside the card, preserve the existing structure (name, manager, count badges, primary "Abrir" CTA). The lifecycle icon row uses `<Button variant="ghost" size="sm" icon={<...icon/>} aria-label="..." />` — every icon-only Button MUST have `aria-label` per Phase 2 contract (e.g., aria-label="Archivar empresa", "Mover a papelera", "Editar empresa"). PRESERVE: lifecycle row visible only for admin role (existing role check intact).

    3. "Abrir" primary CTA per card → `<Button variant="primary" size="sm" onClick={() => navigate(\`/companies/\${id}\`)}>Abrir</Button>`.

    4. Top-right "Crear empresa" CTA → `<Button variant="primary" size="md" icon={<Plus size={16}/>} iconPosition="left" onClick={() => setCreateOpen(true)}>Crear empresa</Button>`.

    5. Admin-only "Crear empresa de prueba" → `<Button variant="secondary" size="md" onClick={() => setTestCreateOpen(true)}>Crear empresa de prueba</Button>` (existing role check intact).

    6. Search box → `<Input type="search" placeholder="Buscar empresa…" value={query} onChange={...} icon={<Search size={16}/>} iconPosition="left" />`.

    7. Type filter + page-size → `<Select label="Tipo" options={typeOptions} value={typeFilter} onChange={...} />` and `<Select label="Por página" options={pageSizeOptions} value={pageSize} onChange={...} />`. Preserve every existing option list verbatim.

    8. Test-mode pill on test companies → `<Badge variant="warning" size="sm">Test</Badge>` (or whatever variant maps best to existing color — pick one and document in SUMMARY).

    9. Archived placeholder badge (when shown) → `<Badge variant="neutral" size="sm">Archivada</Badge>`.

    10. Migrate ALL FOUR modals to `<Modal>`:
       a. Create company — title="Crear empresa", body has Inputs for name + slug + manager email (REQUIRED — preserve validation), footer = Cancelar (secondary) + Crear (primary, loading={isCreating}).
       b. Archive confirm — title="Archivar empresa", size="sm", body = confirmation copy verbatim, footer = Cancelar (secondary) + Archivar (primary or danger — match existing color).
       c. Trash confirm — title="Mover a papelera", size="sm", footer = Cancelar (secondary) + danger Button.
       d. Test-create (admin-only, `testMode=true`) — title="Crear empresa de prueba", body Inputs for name + slug (manager email is NOT required here — preserve invariant: testMode bypasses initial manager invite + sets `companies.is_test=true`).

    11. CSS module — `CompaniesPage.module.css`:
       - DELETE `.modalOverlay`, `.modalCard`, `.input`, `.primaryButton`, `.secondaryButton`, `.dangerButton` selectors and all derivatives.
       - Per UI-SPEC §"Z-index Migration Map": the deleted `.modalOverlay { z-index: 1000 }` is now delegated to `<Modal>` (which uses `var(--wb-z-modal)`). Verify no surviving local z-index numbers.
       - Tokenize every remaining selector. The 13 hardcoded colors flagged in `03-CONTEXT.md` MUST be replaced with `--wb-color-*` tokens from `tokens.css`.
       - Preserve `styles.companyCard` only for layout-only positioning (grid positioning, hover-only positioning shifts) that Card cannot deliver via props.

    Behavior preservation: search debounce, pagination, sessionStorage stale-while-revalidate cache (`target=companies` watch), admin role gating for lifecycle row + test-create.
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|primaryButton|secondaryButton|dangerButton|modalOverlay|modalCard)\\}" frontend/src/pages/CompaniesPage.jsx &&
      ! grep -E "^\\.(modalOverlay|modalCard|input|primaryButton|secondaryButton|dangerButton)\\b" frontend/src/pages/CompaniesPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/CompaniesPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/CompaniesPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/CompaniesPage.jsx &&
      grep -E "aria-label=" frontend/src/pages/CompaniesPage.jsx | head -5 &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero deleted-selector usages; imports from `../components/ui`
    - CSS: zero deleted selectors; zero `#hex`; zero `z-index:` numerics
    - Every icon-only `<Button>` has `aria-label` (verified via grep)
    - Build green
    - Manual smoke (recorded in SUMMARY): search; paginate; create company (manager email required); admin test-create succeeds; archive a company → toast + cache invalidation; trash + restore (cross-link to Trash) preserves URL params
    - sessionStorage cache key for companies still set/read on archive/trash/restore (`target=companies` watch invariant)
  </acceptance_criteria>
  <done>CompaniesPage fully migrated; admin/test-mode + cache + lifecycle gating preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CompanyPage.jsx + tokenize CompanyPage.module.css (no-tabs detail, project cards left, team sidecard right, 3 modals)</name>
  <files>frontend/src/pages/CompanyPage.jsx, frontend/src/pages/CompanyPage.module.css</files>
  <read_first>
    - frontend/src/pages/CompanyPage.jsx (current shape — preserve no-tabs layout, project list left, team sidecard right, 3 modals)
    - frontend/src/pages/CompanyPage.module.css (selector inventory — confirms `z-index: 40` for `.modalOverlay`)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → CompanyPage row of coverage matrix; §"Z-index Migration Map" CompanyPage row
    - CONTEXT.min.md `target=company-detail` (no tabs, project cards left, team sidecard right)
  </read_first>
  <action>
    1. Imports: `import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';`

    2. Header area (company name + lifecycle icon Buttons) — wrap in a layout block (keep existing layout class). Lifecycle icons use `<Button variant="ghost" size="sm" icon={...} aria-label="..." />` (icon-only must have aria-label). Primary "Acciones" or top-right archive/trash CTAs use `<Button variant="secondary" size="md">…</Button>`.

    3. Each project card on the left → `<Card as="button" padding="md" shadow="sm" radius="lg" interactive onClick={() => openProject(id)} className={styles.projectCard}>` (Card supports `as="button"` and auto-defaults `type="button"`). Project-type tag inside card → `<Badge variant="neutral" size="sm">{type}</Badge>`.

    4. Team sidecard on the right → `<Card padding="md" shadow="sm" radius="lg" className={styles.teamSidecard}>`. Member rows: each member's role pill → `<Badge variant="neutral" size="sm">Manager</Badge>` / `<Badge variant="neutral" size="sm">User</Badge>` / etc. (preserve existing role labels). Member kebab/icon → `<Button variant="ghost" size="sm" icon={<MoreVertical size={16}/>} aria-label="Acciones del miembro" />`.

    5. Modals (3):
       a. Invite member — title="Invitar miembro", body = `<Input type="email" label="Correo electrónico" required>` + `<Select label="Rol" options={inviteRoleOptions} required>`. Footer = Cancelar (secondary) + Enviar invitación (primary, loading=isInviting). PRESERVE memoization of `inviteRoleOptions` (existing useMemo) — do NOT inline the array, the memoization avoids the documented re-render loop.
       b. Archive — title="Archivar empresa", size="sm", footer = Cancelar (secondary) + Archivar (primary or danger).
       c. Trash — title="Mover a papelera", size="sm", footer = Cancelar (secondary) + danger Button.

    6. CSS module — `CompanyPage.module.css`:
       - DELETE `.modalOverlay`, `.modalCard`, `.input`, `.select`, `.primaryButton`, `.dangerButton` selectors and derivatives.
       - z-index 40 (modal overlay) → delegated to `<Modal>` token. Verify zero raw z-index after edit.
       - Tokenize all colors / spacing / radius / shadow / typography per the standard rules.
       - Preserve only layout-only classes (left/right grid, sticky sidecard if present, project-card hover lift if not provided by Card.interactive).
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|select|primaryButton|dangerButton|modalOverlay|modalCard)\\}" frontend/src/pages/CompanyPage.jsx &&
      ! grep -E "^\\.(modalOverlay|modalCard|input|select|primaryButton|dangerButton)\\b" frontend/src/pages/CompanyPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/CompanyPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/CompanyPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/CompanyPage.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero deleted-selector usages; imports from `../components/ui`
    - CSS: zero deleted selectors; zero `#hex`; zero `z-index:` numerics
    - Every icon-only `<Button>` has `aria-label`
    - Build green
    - Manual smoke (recorded in SUMMARY): project list left, team sidecard right, no tabs (CONTEXT.min.md `target=company-detail`); invite member with role select; archive company; restore from `/archive` preserves URL params; sessionStorage cache for company detail still functions
    - `inviteRoleOptions` memoization preserved (`useMemo` still in JSX — avoids documented re-render loop)
  </acceptance_criteria>
  <done>CompanyPage fully migrated; no-tabs layout + memoization + cache invariants preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WeBrief backend | CompaniesPage `POST /api/companies` (manager email REQUIRED unless `testMode=true`); admin role checked server-side |
| Browser → WeBrief backend | CompanyPage `POST /api/auth/invite-user`, archive / trash / restore endpoints; rate-limited |
| Browser sessionStorage | Companies + company-detail caches (stale-while-revalidate); not a security boundary but UI invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of privilege | CompaniesPage admin-only test-create button + lifecycle icon row | accept | Existing role gates remain in JSX (admin-only conditional rendering); backend enforces `is_admin` on `POST /api/companies?testMode=true`. Refactor does not move or alter the gate. |
| T-03-08 | Information disclosure | sessionStorage company cache | accept | Cache keys store non-sensitive listing data only; no tokens or PII beyond company names. Refactor does not change cache contents. |
| T-03-09 | Tampering | Invite member modal (CompanyPage) | mitigate | Server-side validation of role + email + company manager scope is unchanged. UI uses `<Input type="email" required>` and `<Select required>` to surface client-side validation; server remains authoritative. |
</threat_model>

<verification>
Cohort 3 gates (per `03-UI-SPEC.md` §"Migration Strategy" success criteria):

```bash
# Files in scope: Companies + Company .module.css and .jsx

grep -E '#[0-9a-fA-F]{3,8}' frontend/src/pages/CompaniesPage.module.css frontend/src/pages/CompanyPage.module.css   # expect: zero
grep -E 'z-index:\s*[0-9]+' frontend/src/pages/CompaniesPage.module.css frontend/src/pages/CompanyPage.module.css  # expect: zero
grep -E '^\.(modalOverlay|modalCard)\b' frontend/src/pages/CompaniesPage.module.css frontend/src/pages/CompanyPage.module.css   # expect: zero
grep -E '^\.(input|select|primaryButton|secondaryButton|dangerButton)\b' frontend/src/pages/CompaniesPage.module.css frontend/src/pages/CompanyPage.module.css   # expect: zero

cd frontend && npm run build   # expect exit 0
```

Manual QA (per `03-UI-SPEC.md` §"Per-page QA"):
- CompaniesPage: search, paginate, create company (manager email required), test-create (admin), archive, trash, restore (cross-link to Trash)
- CompanyPage: project list left, team sidecard right; invite member; archive company; restore from `/archive` (URL params preserved)

Console-cleanliness gate: zero new warnings/errors.

Atomic-commit policy: ONE commit per page (two atomic commits in this cohort).
</verification>

<success_criteria>
- CompaniesPage and CompanyPage fully migrated
- All five gates pass on both files
- `target=companies` and `target=company-detail` invariants preserved
- sessionStorage caches preserved (no behavioral regression)
- Admin role gating (lifecycle icon row, test-create) preserved
- Two atomic commits: `refactor(03): migrate CompaniesPage to shared UI` and `refactor(03): migrate CompanyPage to shared UI`
</success_criteria>

<output>
Create `.planning/phases/03-admin-auth-migration/03-03-SUMMARY.md` recording: gate results, screenshots before/after for both pages (companies grid + company detail), exact list of selectors deleted from each .module.css, hardcoded-color count delta (was 13+ on CompaniesPage → 0), and confirmation that admin role gates and sessionStorage cache invariants are preserved.
</output>
</content>
</invoke>