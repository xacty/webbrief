---
phase: 03-admin-auth-migration
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - frontend/src/pages/UsersPage.jsx
  - frontend/src/pages/UsersPage.module.css
  - frontend/src/pages/TrashPage.jsx
  - frontend/src/pages/TrashPage.module.css
  - frontend/src/pages/SecurityPage.jsx
  - frontend/src/pages/SecurityPage.module.css
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "UsersPage migrates only OUTER controls (filters, modals, pagination) and tokenizes row colors/borders — expandable-row table CSS stays per `03-UI-SPEC.md` UsersPage exception"
    - "UsersPage admin/manager scope rules and platform-role admin-only gating preserved (`target=users` watch); admin self-delete blocked; last-manager downgrade blocked; archived/trashed companies hidden"
    - "TrashPage covers BOTH archive (`/archive`) and trash (`/trash`) modes; archive mode has NO permanent-delete; trash mode has the permanent-delete <Modal>"
    - "SecurityPage tabs (Overview/Users/IPs/Events/Blocks) preserved; block IP and block user <Modal>s REQUIRE a reason (textarea Input); admin-only access; no token/payload exposure"
    - "All three .module.css files contain zero raw `#hex`/`z-index:<number>` and zero local `.modalOverlay`/`.modalCard`/`.input`/`.primaryButton`/`.secondaryButton`/`.dangerButton`"
  artifacts:
    - path: "frontend/src/pages/UsersPage.jsx"
      provides: "Users admin table using shared Button/Input/Select/Modal/Card/Badge for outer controls; expandable-row table preserved"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/UsersPage.module.css"
      provides: "Layout-only outer controls + tokenized row colors/borders; z-index 40 → token (delegated to <Modal>)"
    - path: "frontend/src/pages/TrashPage.jsx"
      provides: "Archive + trash modes using shared primitives; permanent-delete <Modal> only in trash mode"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/TrashPage.module.css"
      provides: "Layout-only, fully tokenized"
    - path: "frontend/src/pages/SecurityPage.jsx"
      provides: "Security tabs + reason-required block modals using shared primitives"
      contains: "from '../components/ui'"
    - path: "frontend/src/pages/SecurityPage.module.css"
      provides: "Layout-only, fully tokenized; z-index 5000 → token (delegated to <Modal>)"
  key_links:
    - from: "UsersPage.jsx"
      to: "GET /api/auth/me + admin/manager scoped user listing + platform_role mutation endpoint"
      via: "existing handlers — no behavioral change"
      pattern: "platform_role|active_company"
    - from: "TrashPage.jsx"
      to: "GET /api/trash?state=archived|trashed + restore + permanent-delete endpoints"
      via: "existing handlers"
      pattern: "/api/trash|state=archived|state=trashed"
    - from: "SecurityPage.jsx"
      to: "/api/security/* (overview/users/ips/events/blocks) + block endpoints (require reason)"
      via: "existing handlers"
      pattern: "/api/security|reason"
---

<objective>
Cohort 4 (Complex tables) — migrate the highest cyclomatic-risk admin pages: UsersPage (table + memoized invite options + admin/manager scoping), TrashPage (archive + trash dual mode), and SecurityPage (tabs + reason-required block modals + audit-fallback warning). All three are done together because the migration recipe is now proven and they share the same modal/Select/Badge patterns.

Purpose: After this cohort, all admin pages consume shared primitives and are tokenized. Only AppShell remains.
Output: Three pages fully migrated with their security and authorization invariants intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/decisions.md
@.planning/phases/03-admin-auth-migration/03-CONTEXT.md
@.planning/phases/03-admin-auth-migration/03-UI-SPEC.md
@frontend/src/styles/tokens.css
@frontend/src/components/ui/index.js
@frontend/src/pages/UsersPage.jsx
@frontend/src/pages/UsersPage.module.css
@frontend/src/pages/TrashPage.jsx
@frontend/src/pages/TrashPage.module.css
@frontend/src/pages/SecurityPage.jsx
@frontend/src/pages/SecurityPage.module.css

<interfaces>
<!-- Same shared UI surface. -->

<!-- UsersPage modals (per UI-SPEC matrix): -->
<!-- 1. Invite user (email + role <Select> using memoized inviteRoleOptions) -->
<!-- 2. Edit profile (name + email + platform role admin-only) -->
<!-- 3. Delete account confirm (admin self-delete blocked) -->

<!-- UsersPage Selects: active-company filter, order, page-size, role-per-company-access -->

<!-- TrashPage Selects: range preset (date filter); archive vs trash mode toggle is the tab strip -->
<!-- TrashPage Modal: permanent-delete confirm — TRASH MODE ONLY -->

<!-- SecurityPage Selects: range, type filter -->
<!-- SecurityPage Modals (BOTH require a reason textarea Input): block IP, block user -->
<!-- SecurityPage Badges: event severity, block status, audit-fallback warning -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate UsersPage outer controls + 3 modals; preserve expandable-row table CSS (UsersPage exception)</name>
  <files>frontend/src/pages/UsersPage.jsx, frontend/src/pages/UsersPage.module.css</files>
  <read_first>
    - frontend/src/pages/UsersPage.jsx (preserve `inviteRoleOptions` useMemo to avoid documented re-render loop; preserve admin/manager scoping; preserve admin self-delete + last-manager downgrade blocks)
    - frontend/src/pages/UsersPage.module.css (selector inventory — confirms `z-index: 40` for `.modalOverlay`)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → UsersPage row of coverage matrix; **UsersPage exception (locked):** expandable-row CSS stays
    - CONTEXT.min.md `target=users` (admin global visibility of profiles + platform roles including Admin/QA; manager scope limited to active companies; platform roles admin-only; archived/trashed hidden; no admin self-delete; no last-manager downgrade)
  </read_first>
  <action>
    1. Imports: `import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';`

    2. Outer controls (above the table):
       - Search → `<Input type="search" placeholder="Buscar usuario…" value={...} onChange={...} icon={<Search size={16}/>} />`
       - Active-company filter → `<Select label="Empresa" options={companyFilterOptions} value={...} onChange={...} placeholder="Todas" />`. PRESERVE the rule that archived/trashed companies are excluded from the option list.
       - Order → `<Select label="Orden" options={orderOptions} value={...} onChange={...} />`
       - Page-size → `<Select label="Por página" options={pageSizeOptions} value={...} onChange={...} />`
       - Top-right primary CTA → `<Button variant="primary" size="md" icon={<UserPlus size={16}/>}>Invitar usuario</Button>`

    3. Wrap the table in `<Card padding="sm" shadow="sm" radius="lg" className={styles.tableCard}>` (or whatever existing layout class wraps the table — keep the wrapper so the expandable-row CSS still applies). DO NOT migrate the inner table cells, headers, or expandable-row markup — that custom structure is preserved per the UsersPage exception.

    4. Per-row controls inside the table:
       - Edit icon Button → `<Button variant="ghost" size="sm" icon={<Pencil size={16}/>} aria-label="Editar usuario" onClick={...} />`
       - Delete icon Button → `<Button variant="danger" size="sm" icon={<Trash2 size={16}/>} aria-label="Eliminar usuario" onClick={...} disabled={isAdminSelf} />` — the `disabled` MUST preserve the admin-self-delete block (`target=users` watch).
       - Platform role pill (Admin / Manager / User / QA) → `<Badge variant="primary|neutral|success|warning" size="sm">{label}</Badge>` (pick variants matching current colors; document mapping in SUMMARY).
       - Per-company-access role pill → `<Badge variant="neutral" size="sm">{role}</Badge>`.
       - Per-company-access role-change `<Select>` (admin-only or manager-scoped per existing rule) → `<Select size="sm" options={roleOptions} value={...} onChange={...} disabled={isLastManager} />` — `disabled` preserves the no-last-manager-downgrade invariant.

    5. Migrate THREE modals to `<Modal>`:
       a. Invite user — title="Invitar usuario", body has `<Input type="email" label="Correo electrónico" required>` + `<Select label="Rol" options={inviteRoleOptions} required>` (PRESERVE the `inviteRoleOptions` useMemo identity — do NOT inline). Footer = Cancelar (secondary) + Enviar (primary, loading=isInviting).
       b. Edit profile — title="Editar perfil", body has `<Input label="Nombre">`, `<Input label="Correo">`, and admin-only `<Select label="Rol de plataforma" options={platformRoleOptions}>` (the `Select` MUST be conditionally rendered ONLY for admins per `target=users` keep). Footer = Cancelar + Guardar.
       c. Delete account confirm — title="Eliminar cuenta", size="sm", body = confirmation copy verbatim, footer = Cancelar (secondary) + Eliminar (danger). The trigger Button on the row is already `disabled` for admin-self.

    6. CSS module — `UsersPage.module.css`:
       - DELETE `.input`, `.primaryButton`, `.secondaryButton`, `.dangerButton` selectors and derivatives.
       - DO NOT delete the expandable-row table classes (`.userRow`, `.userRowExpanded`, `.userDetails`, `.companyAccessTable`, etc.) — these are the locked UsersPage exception. ONLY tokenize their hardcoded values.
       - z-index 40 (modal overlay) → delegated to `<Modal>`. Verify zero raw z-index numerics after edit.
       - Tokenize all hardcoded colors / spacing / radius / shadow / typography in surviving selectors.
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|primaryButton|secondaryButton|dangerButton)\\}" frontend/src/pages/UsersPage.jsx &&
      ! grep -E "^\\.(input|primaryButton|secondaryButton|dangerButton)\\b" frontend/src/pages/UsersPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/UsersPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/UsersPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/UsersPage.jsx &&
      grep -E "useMemo.*inviteRoleOptions|inviteRoleOptions.*useMemo" frontend/src/pages/UsersPage.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero deleted-selector usages; imports from `../components/ui`; `inviteRoleOptions` still wrapped in `useMemo`
    - CSS: zero deleted selectors; expandable-row classes (`.userRow`, etc.) preserved per UsersPage exception; zero `#hex`; zero `z-index:` numerics
    - Build green
    - Manual smoke (recorded in SUMMARY): search; filter by active company (archived/trashed companies hidden); order; expand a row; edit profile (admin sees platform-role select; manager does not); change role within company (last-manager downgrade blocked → toast); invite user; admin self-delete button is disabled
  </acceptance_criteria>
  <done>UsersPage outer controls migrated; expandable-row table CSS preserved; all `target=users` invariants intact.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TrashPage.jsx + tokenize TrashPage.module.css (archive + trash dual mode)</name>
  <files>frontend/src/pages/TrashPage.jsx, frontend/src/pages/TrashPage.module.css</files>
  <read_first>
    - frontend/src/pages/TrashPage.jsx (preserve dual-mode handling: `/archive` shows only `archived_at not null AND trashed_at is null`; `/trash` shows only `trashed_at not null`; permanent-delete only in trash mode)
    - frontend/src/pages/TrashPage.module.css (selector inventory)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → TrashPage row of coverage matrix
    - CONTEXT.min.md `target=trash` and `target=archive` (sessionStorage company caches after archive/trash/restore/delete; unnumbered Empresas/Proyectos tabs and date filter)
  </read_first>
  <action>
    1. Imports: `import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';`

    2. Tab strip (Empresas / Proyectos — unnumbered per `target=archive` watch):
       - Keep the existing tab structure but render each tab as `<Button variant={isActive ? 'primary' : 'ghost'} size="sm" onClick={...}>Empresas|Proyectos</Button>` OR a tokenized custom element if the page uses a non-button tab pattern. Pick whichever is closest to current visual; document choice in SUMMARY.

    3. Date filter inputs → `<Input type="date" label="Desde" value={fromDate} onChange={...} />` and `<Input type="date" label="Hasta" value={toDate} onChange={...} />`. Range preset → `<Select label="Rango" options={rangeOptions} value={...} onChange={...} />`.

    4. Item list — wrap each item in `<Card padding="sm" shadow="sm" radius="lg" className={styles.trashItem}>`. State pill per row → `<Badge variant="warning" size="sm">Archivado</Badge>` (archive mode) or `<Badge variant="danger" size="sm">Papelera</Badge>` (trash mode) per the row's actual state.

    5. Per-row actions:
       - "Restaurar" (visible in BOTH modes) → `<Button variant="secondary" size="sm" onClick={handleRestore}>Restaurar</Button>` (PRESERVE: restore clears archive/trash columns per `target=trash` keep).
       - "Eliminar definitivamente" (TRASH MODE ONLY) → `<Button variant="danger" size="sm" onClick={() => openPermanentDelete(row)}>Eliminar</Button>`. ENFORCE: this button MUST be conditionally rendered only in trash mode (per `target=archive` keep: "no permanent delete action there").

    6. Permanent-delete `<Modal>` (trash mode only):
       - title="Eliminar definitivamente", size="sm", body has confirmation copy verbatim, footer = Cancelar (secondary) + Eliminar (danger, loading=isDeleting). Existing handler is destructive — preserve it.

    7. CSS module — `TrashPage.module.css`:
       - DELETE `.modalOverlay`, `.modalCard`, `.input`, `.primaryButton`, `.dangerButton` selectors and derivatives.
       - Tokenize all values; preserve only layout-only classes.
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|primaryButton|dangerButton|modalOverlay|modalCard)\\}" frontend/src/pages/TrashPage.jsx &&
      ! grep -E "^\\.(modalOverlay|modalCard|input|primaryButton|dangerButton)\\b" frontend/src/pages/TrashPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/TrashPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/TrashPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/TrashPage.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero deleted-selector usages; imports from `../components/ui`
    - CSS: zero deleted selectors; zero `#hex`; zero `z-index:` numerics
    - Build green
    - Manual smoke (recorded in SUMMARY): toggle Empresas/Proyectos tabs in BOTH `/archive` and `/trash`; restore from each mode (clears archive/trash columns); permanent-delete with confirm visible ONLY in `/trash`; sessionStorage company cache invalidation after archive/trash/restore/delete still triggers
  </acceptance_criteria>
  <done>TrashPage fully migrated with dual-mode invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SecurityPage.jsx + tokenize SecurityPage.module.css (5 tabs + reason-required block modals)</name>
  <files>frontend/src/pages/SecurityPage.jsx, frontend/src/pages/SecurityPage.module.css</files>
  <read_first>
    - frontend/src/pages/SecurityPage.jsx (preserve admin-only access; tab structure Overview/Users/IPs/Events/Blocks; reason-required block modals; audit-fallback warning when applicable)
    - frontend/src/pages/SecurityPage.module.css (selector inventory — confirms `z-index: 5000` for `.modalOverlay`)
    - .planning/phases/03-admin-auth-migration/03-UI-SPEC.md → SecurityPage row of coverage matrix
    - CONTEXT.min.md `target=security` (admin-only; block modal requires reason; do not expose tokens, full payloads, full brief content, or files)
  </read_first>
  <action>
    1. Imports: `import { Button, Input, Select, Modal, Card, Badge } from '../components/ui';`

    2. Tab strip (Overview / Users / IPs / Events / Blocks) — same pattern as TrashPage tabs (Button variant primary/ghost or tokenized custom). Preserve admin-only gate that wraps the entire page (`target=security` keep).

    3. Filters above each list:
       - Search → `<Input type="search" placeholder="Buscar…" value={...} onChange={...} />`
       - IP literal → `<Input type="text" label="IP" placeholder="0.0.0.0" value={...} onChange={...} />`
       - Range → `<Select label="Rango" options={rangeOptions} value={...} onChange={...} />`
       - Type filter → `<Select label="Tipo" options={typeOptions} value={...} onChange={...} />`

    4. Overview tiles → each tile is a `<Card padding="md" shadow="sm" radius="lg">` with the existing metric markup inside (just tokenize colors).

    5. Event rows / blocks rows → wrap each row in `<Card padding="sm" shadow="sm" radius="lg" className={styles.eventRow}>`. Per-row Badges:
       - Event severity → `<Badge variant={severityVariant} size="sm">{severity}</Badge>` (`info|warning|danger` mapped to `neutral|warning|danger`).
       - Block status → `<Badge variant={isActive ? 'danger' : 'neutral'} size="sm">{status}</Badge>`.
       - Audit-fallback warning (when Supabase Auth audit logs unavailable) → `<Badge variant="warning" size="md">Auditoría parcial</Badge>` (preserve existing warning text; place in the same prominent slot).

    6. Action Buttons:
       - "Bloquear" (primary) → `<Button variant="primary" size="md" onClick={() => openBlockModal('ip'|'user')}>Bloquear</Button>`
       - "Revocar" (secondary) per row → `<Button variant="secondary" size="sm" onClick={...}>Revocar</Button>`
       - Per-row icon → `<Button variant="ghost" size="sm" icon={...} aria-label="..." />`

    7. TWO `<Modal>`s (block IP and block user) — BOTH REQUIRE A REASON (per `target=security` keep). Use the same Modal pattern with a textarea-style Input:
       ```jsx
       <Modal open={blockOpen} onClose={...} title="Bloquear IP" size="sm" footer={
         <>
           <Button variant="secondary" onClick={...}>Cancelar</Button>
           <Button variant="danger" onClick={submitBlock} loading={isBlocking} disabled={!reason.trim()}>Bloquear</Button>
         </>
       }>
         <Input type="text" label="IP" value={ip} onChange={...} required />
         <Input type="text" label="Razón" required value={reason} onChange={...} placeholder="Explicar motivo del bloqueo…" />
         {/* If the existing UI uses a multi-line <textarea>, render a <textarea> with className=styles.reasonTextarea (layout-only class) — Phase 2 has no Textarea primitive. Document the choice in SUMMARY. */}
       </Modal>
       ```
       The "Bloquear" submit Button MUST be `disabled` while reason is empty (preserves the reason-required invariant client-side; backend still authoritative).

    8. CSS module — `SecurityPage.module.css`:
       - DELETE `.modalOverlay`, `.modalCard`, `.input`, `.primaryButton`, `.dangerButton` selectors and derivatives.
       - z-index 5000 (modal overlay) → delegated to `<Modal>`. Verify zero raw z-index after edit.
       - Tokenize all values; preserve layout-only classes (tab strip layout, list layout, reason-textarea sizing).

    Authorization preservation: page-level admin gate (existing role check) intact; do not expose tokens, full payloads, full brief content, or files in any modal or tile (`target=security` keep).
  </action>
  <verify>
    <automated>
      ! grep -E "className=\\{styles\\.(input|primaryButton|dangerButton|modalOverlay|modalCard)\\}" frontend/src/pages/SecurityPage.jsx &&
      ! grep -E "^\\.(modalOverlay|modalCard|input|primaryButton|dangerButton)\\b" frontend/src/pages/SecurityPage.module.css &&
      ! grep -E "#[0-9a-fA-F]{3,8}" frontend/src/pages/SecurityPage.module.css &&
      ! grep -E "z-index:\\s*[0-9]+" frontend/src/pages/SecurityPage.module.css &&
      grep -E "from '\\.\\./components/ui'" frontend/src/pages/SecurityPage.jsx &&
      cd frontend && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - JSX: zero deleted-selector usages; imports from `../components/ui`; reason-required Block button is `disabled` when reason empty
    - CSS: zero deleted selectors; zero `#hex`; zero `z-index:` numerics
    - Build green
    - Manual smoke (recorded in SUMMARY): switch tabs (Overview/Users/IPs/Events/Blocks); block IP with reason → audit row appears; revoke; auth-audit fallback warning Badge shows when Supabase Auth audit logs unavailable; admin-only access preserved (non-admin gets the existing redirect/error)
  </acceptance_criteria>
  <done>SecurityPage fully migrated with admin gate + reason-required + no-token-exposure invariants intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WeBrief backend | UsersPage `/api/auth/me`, role-mutation endpoints; admin/manager scoping enforced server-side |
| Browser → WeBrief backend | TrashPage `GET /api/trash?state=...`, restore + permanent-delete endpoints; rate-limited |
| Browser → WeBrief backend | SecurityPage `/api/security/*` (admin-only); block IP/user POSTs require reason; rate-limited and audited via `security_events` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Elevation of privilege | UsersPage admin self-delete + last-manager downgrade buttons | mitigate | Existing client-side `disabled` states preserved (admin-self delete, last-manager downgrade); server remains authoritative. Refactor does not move or weaken these gates. |
| T-03-11 | Information disclosure | SecurityPage event/block rows | accept | Refactor is JSX/CSS only; no new fields surfaced. UI-SPEC explicitly forbids exposing tokens/payloads/brief content/files — no such fields are added. |
| T-03-12 | Repudiation | SecurityPage block modal | mitigate | The reason field stays REQUIRED (`disabled` until non-empty trim) so every block has an audit trail; `security_events` writes preserved by backend. |
| T-03-13 | Denial of service | SecurityPage filters / TrashPage pagination | accept | Existing rate-limiting + cache behavior preserved; refactor does not change request volumes. |
</threat_model>

<verification>
Cohort 4 gates (per `03-UI-SPEC.md` §"Migration Strategy" success criteria):

```bash
# Files in scope: Users + Trash + Security .module.css and .jsx

grep -E '#[0-9a-fA-F]{3,8}' frontend/src/pages/UsersPage.module.css frontend/src/pages/TrashPage.module.css frontend/src/pages/SecurityPage.module.css   # expect: zero
grep -E 'z-index:\s*[0-9]+' frontend/src/pages/UsersPage.module.css frontend/src/pages/TrashPage.module.css frontend/src/pages/SecurityPage.module.css  # expect: zero
grep -E '^\.(modalOverlay|modalCard)\b' frontend/src/pages/UsersPage.module.css frontend/src/pages/TrashPage.module.css frontend/src/pages/SecurityPage.module.css   # expect: zero
grep -E '^\.(input|primaryButton|secondaryButton|dangerButton)\b' frontend/src/pages/UsersPage.module.css frontend/src/pages/TrashPage.module.css frontend/src/pages/SecurityPage.module.css   # expect: zero (UsersPage expandable-row classes survive — they are NOT in this regex)

cd frontend && npm run build   # expect exit 0
```

Manual QA (per `03-UI-SPEC.md` §"Per-page QA"):
- UsersPage: search, filter by active company (archived/trashed companies hidden), expand a row, edit profile, change role within company (last-manager downgrade blocked), invite user, admin self-delete blocked
- TrashPage: toggle Empresas/Proyectos tabs in `/archive` AND `/trash`; restore; permanent-delete with confirm (trash mode only)
- SecurityPage: switch all 5 tabs; block IP with reason → audit row appears; revoke; audit-fallback warning when applicable

Console-cleanliness gate: zero new warnings/errors.

Atomic-commit policy: ONE commit per page (three atomic commits in this cohort).
</verification>

<success_criteria>
- UsersPage outer controls + 3 modals migrated; expandable-row table CSS preserved per UI-SPEC exception
- TrashPage migrated with archive + trash dual-mode + sessionStorage cache invariants preserved
- SecurityPage migrated with admin gate + reason-required + no-token-exposure invariants preserved
- All five gates pass on all three .module.css files
- Three atomic commits: `refactor(03): migrate UsersPage to shared UI`, `refactor(03): migrate TrashPage to shared UI`, `refactor(03): migrate SecurityPage to shared UI`
</success_criteria>

<output>
Create `.planning/phases/03-admin-auth-migration/03-04-SUMMARY.md` recording: gate results per file, screenshots before/after for each page, list of selectors deleted from each .module.css, explicit confirmation that UsersPage expandable-row classes survived per UI-SPEC exception, mapping of platform-role labels → Badge variants, and explicit confirmation that admin-only / admin-self-delete / last-manager-downgrade / reason-required invariants remain enforced.
</output>
</content>
</invoke>